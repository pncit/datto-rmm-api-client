## reviser — round 4

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| engineer-r3-f1 | Fixed | `walkAllSchemaNodes` (`patch-spec.mjs`) was the lone content-type-narrow, `HTTP_METHODS`-filter-free traversal among four siblings that all walk `spec.paths`. Rather than patching it in place (the recommendation's minimal option), took the "better" option: extracted the shared `for (paths) -> for (methods, HTTP_METHODS filter) -> operation` boilerplate into one `forEachOperation(spec, visit)` helper in `scripts/lib/schema-walk.mjs`, and routed all four traversals through it — `patch-spec.mjs`'s `computeReachableComponentNames`, `patchMissingSuccessResponses`, and `walkAllSchemaNodes`, plus `widen-response-enums.mjs`'s `buildReachabilityMaps`. `walkAllSchemaNodes` now iterates every content type of `requestBody`/response (not just `application/json`) and inherits the `HTTP_METHODS` filter automatically, closing both drifts in one place instead of one-off patching the third call site. `HTTP_METHODS` is no longer imported directly by either pipeline script (only by `schema-walk.mjs` itself, inside `forEachOperation`). Verified: `npm run typecheck`, `npm run lint`, `npm test` (78/78) all green; `npm run generate` against the real committed spec followed by `git diff --exit-code -- src/generated` confirms byte-identical output (no behavior change today, since `patchMissingSuccessResponses` already pre-empts every `*/*`-only response by synthesizing an `application/json` `200` first — the fixed traversal only changes behavior for a hypothetical future inline-schema-under-`*/*` case, exactly as the finding anticipated). |
| typescript-cop-r2-f1 | Fixed | Added `StrictOpenApiSpecFragment` to `tests/generated/strict-fixture-types.ts` (`{ openapi?: string; paths?: Record<string, Record<string, StrictOpenApiOperation>>; components?: { schemas?: Record<string, StrictSchemaNode> } }`, no index signature — `openapi` included, unlike the leaf types, because `patch-spec.test.ts`'s `buildValidSpecFragment()` carries it as a realistic top-level field) and applied it via `satisfies` to every hand-written top-level spec literal the finding named: `buildValidSpecFragment()`'s return in `patch-spec.test.ts`, and every ad-hoc `spec = {...}` in `widen-enums.test.ts`'s `verifyNoSharedEnumBearingSchemas`/`verifyWideningHappened` describe blocks plus `specWith(...)`. Verified directly with throwaway probes (reverted after each): a `pahts`-for-`paths` typo in one of `verifyNoSharedEnumBearingSchemas`'s spec literals now fails with `TS2561: ... Did you mean to write 'paths'?`, and the same typo in `buildValidSpecFragment`'s literal fails with both `TS2561` (the `satisfies` check) and `TS2741` (the pre-existing `SpecFragment` return-type annotation) — the exact one-level-up hazard the finding's probe demonstrated is now closed. `npm run typecheck` / `npm test` / `npm run generate` + reproducibility all green afterward. |
| typescript-cop-r3-f1 | Fixed | Enabled `checkJs: true` in `tsconfig.test.json` (the project that already has `allowJs` + `scripts/**/*.mjs` in its `include`, and is what `npm run typecheck`/CI actually gates on — confirmed `tsconfig.tools.json` doesn't list the `.mjs` scripts and needs no change). This immediately surfaced 50 errors across `scripts/patch-spec.mjs`, `scripts/widen-response-enums.mjs`, and `scripts/dedupe-generated-index.mjs` — implicit-`any` parameters on essentially every internal (non-exported) helper, plus a handful of real null/undefined-safety gaps — exactly as the finding's `checkJs: true` probe predicted. Added `@param`/`@returns` JSDoc to every internal helper named in the finding's Location column (`getAtPath`, `patchTimestamps`, `patchRequestResponseSplits`, `computeReachableComponentNames`/its `followRefs` closure, `pruneOrphanedContextSchemas`'s queue-typed access, `responseSchema`, `deepEqual`, `patchMissingSuccessResponses`'s `distinctSchemas`, `walkAllSchemaNodes`, `fixMalformedNonStringConstraints`, `patchSpec`'s `missing` array; `toPascalCase`, `collectComponentRefs`, `componentHasEnum`, `schemaHasEnum`, `buildReachabilityMaps`/`addReach`, `parseGeneratedFile`, `widenEnumAliasLine`, `isRequestRootName`, `expandExcludedNames`, `walkTsFiles`; `normalizeExportLine`, `main`'s `catch (error)`). Fixed the two genuine possible-undefined gaps `checkJs` surfaced rather than silencing them: (1) `schema-walk.mjs`'s `root[keyword] ?? []` read `unknown` (not iterable) off `SchemaNode`'s index signature — narrowed via an explicit `/** @type {SchemaNode[] | undefined} */` cast at the one read site, documented as intentional; (2) `widen-response-enums.mjs`'s `applyWidening` read `primaryNamesByFile.get(fileName)` (`Set<string> | undefined`) and spread it directly — previously a silent `undefined is not iterable` runtime crash if the maps were ever mismatched; now an explicit fail-loud `throw` naming the offending file and the two-map invariant it depends on, rather than a cryptic native TypeError. Also fixed three smaller `queue.shift()`-returns-`T | undefined` narrowing gaps (`pruneOrphanedContextSchemas`, `expandExcludedNames`) with documented non-null assertions justified by the enclosing `while (queue.length > 0)` guard, and a `Map.get()`-after-`.has()` pattern in `verifyNoSharedEnumBearingSchemas` restructured to bind the looked-up value once instead of re-deriving it un-narrowed. Verified directly, reproducing the finding's own two probes (both reverted after): `refName(12345)` (wrong-type argument to an internal helper) now fails `TS2345`; the argument-swapped `patchTimestamps(missing, spec)` (the exact runtime-crashing bug the finding demonstrated) now fails `TS2345` too. Ran the full gate afterward — `npm run typecheck` (clean), `npm run lint` (clean, pre-existing `src/` warnings only, unrelated), `npm test` (78/78 green), and `npm run generate` against the real committed spec followed by `git diff --exit-code -- src/generated` (byte-identical) — no behavior change, only newly-provable internal type safety. |
