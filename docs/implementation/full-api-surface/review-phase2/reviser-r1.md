## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | `scripts/patch-spec.mjs`'s `fixMalformedNonStringConstraints` now guards the array-enum deletion with `Array.isArray(node.items?.enum)` in addition to the existing `node.type === "array" && Array.isArray(node.enum)` check, so the top-level `enum` is only stripped when `items` genuinely carries its own enum (the precondition that makes the top-level one redundant) — an array-typed schema whose only enum sits at the array level is now left untouched. Updated the module doc (l.16-25) to state this precondition explicitly instead of just describing the intent. Added a new unit test in `tests/generated/patch-spec.test.ts` ("leaves an array-level enum untouched when items carries no enum of its own (not redundant)") asserting the array-level `enum` survives and `items.enum` stays `undefined` when no `items.enum` is present, alongside the existing test for the genuinely-redundant case. Verified: `vitest run tests/generated/patch-spec.test.ts` (10/10 pass), `npm run typecheck` clean, and `npm run generate` against the real committed spec re-run end-to-end with the fix in place — `git status` shows zero diff under `src/generated/**` afterward, confirming the fix is behavior-preserving on the current frozen spec (the one real occurrence, the `entities` query parameter, still has `items.enum` and is still stripped) while closing the gap for a future spec refresh. |
