## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | `patch-spec.mjs` now prunes the component schemas `patchAlertContext`'s rewrite orphans. `patchAlertContext` (l.226-253) returns the component names the old `alertContext.oneOf` `$ref`'d before being overwritten; the new `pruneOrphanedContextSchemas` (l.354-385) expands that set transitively through each candidate's own `allOf` base (catching the shared `AlertContext` base class), computes full spec reachability via the new `computeReachableComponentNames` (l.288-333, a $ref-following BFS over every operation's parameters/requestBody/responses), and deletes exactly the candidates that are no longer reachable — anchored and fail-loud against the new `EXPECTED_ORPHANED_COMPONENTS` list (l.133-162, the 27 concrete `*Context` schemas + their `AlertContext` base, verified against the real spec): an orphan not in that list is reported as drift (`missing.push(...)`, l.376-380) rather than silently deleted, and a candidate still reachable some other way is left untouched. Scoped deliberately to the `alertContext` blast radius (not a whole-spec "delete anything unreachable" sweep) so a schema like `ProxySettings` — which legitimately loses reachability from one ref location via `REQUEST_RESPONSE_SPLITS` while staying reachable via its response operations — is never a candidate. Verified against the real spec: patching now removes exactly the 28 expected schemas (113 → 86 minus the split's +1 clone), and re-running the full `npm run generate` pipeline confirms the corresponding 71 dead generated type files (and 14 dead widened-enum files, since those enums no longer exist) are gone from the committed `src/generated/types/**`. New tests: `tests/generated/patch-spec.test.ts` "orphaned alertContext schema pruning" (3 cases — prunes an anchored orphan chain via `allOf`, throws on an undocumented orphan, leaves a still-reachable component alone). |
| architect-r1-f2 | Fixed | `widen-response-enums.mjs` `main()` now calls `verifyWideningHappened` (l.429-456) after the widening pass, asserting two invariants derived from the **patched spec** (not from the same generated-file text `ENUM_ALIAS_RE`/`IMPORT_RE` parse, so a format drift in that text can't defeat its own check): (1) if any response-reachable component schema carries an enum — which `verifyNoSharedEnumBearingSchemas` already proves is response-exclusive — `changedCount` must be > 0, catching the exact "0-match format drift" scenario named in the finding; (2) if the spec-derived request-only-component set is non-empty, at least one of those names must have resolved to a declared type name in some generated file (`rootExcludedNames.size > 0`), catching a `computeRequestOnlyComponentNames`-vs-Orval-naming mismatch. This is intentionally a coarser invariant than "derive the exact should-widen set and diff it" (which would require reimplementing Orval's per-field naming algorithm) — documented as such in `verifyWideningHappened`'s own doc; it proves the mechanism engaged on both its inclusion and exclusion sides rather than silently no-op'ing, which is the concrete failure mode the finding demonstrated. Verified: still 0 throws against the real patched spec (11/11 real widen files unaffected in shape), and a deliberately-zeroed `changedCount` throws as expected in the new unit tests. New tests: `tests/generated/widen-enums.test.ts` "verifyWideningHappened" (5 cases). |
| architect-r1-f3 | Fixed | `tests/generated/reproducibility.test.ts` is no longer picked up by the default `vitest run`: `vitest.config.ts`'s `test.exclude` now adds it alongside vitest's own `configDefaults.exclude`, so `npm test`/`prepublishOnly` never shells out to `npm run generate` + git as a side effect. It has its own dedicated `vitest.repro.config.ts` (included in `tsconfig.tools.json`'s typecheck) and a new `npm run test:repro` script, wired into CI explicitly (`.github/workflows/validate.yml` and `npm-publish.yml` both gained a `Reproducibility (R15)` / `test:repro` step after their existing `Test` step) so the R15 guard still runs on every PR and pre-publish validation, just decoupled from the default local `npm test` path. The test itself also now `skipIf`s when `.git`/git isn't available (`hasGit()`, not just an absent spec), per the finding's secondary note. Verified: `npm test` now runs 7 files/59 tests (down from 8/60, reproducibility excluded); `npm run test:repro` passes standalone against a committed tree (verified via a temporary local commit + `git reset --soft` to confirm without leaving the commit in place). |
| engineer-r1-f1 | Fixed | Same `reproducibility.test.ts` rewrite as architect-r1-f3 also fixes this finding's specific complaint: the test now `rmSync(GENERATED_DIR, { recursive: true, force: true })` before `npm run generate`, then asserts `git status --porcelain -- src/generated` (not `git diff --exit-code`) is empty — `git status --porcelain` reports untracked *and* deleted paths, not just modified-tracked ones, so both regression classes the finding named (a spec change producing a new untracked file; one removing a schema and orphaning a committed file) are now caught. This exposed a real, deeper version of the same defect while implementing the fix: `npm run generate` itself (not just the test) never deleted stale output — Orval only writes files a given pass produces, so a schema removal left the old file on disk untouched. Fixed at the root by adding `output.clean: true` to both Orval targets in `orval.config.ts` (l.37-44, l.46-51, with a new doc block explaining why), so every `npm run generate` now starts from a genuinely empty output directory. Verified end-to-end: `rm -rf src/generated && npm run generate` run twice in a row produces byte-identical, file-for-file identical output (idempotent), and after staging, `npm run test:repro` passes against the real repo state. |
| engineer-r1-f2 | Fixed | The dead `fileNamesByPrimaryName` `Map` (built via `.set(...)` but never read) is gone — removed as part of the `widenGeneratedTypes` refactor (see engineer-r1-f3) rather than left in place; its replacement, `primaryNamesByFile` (returned by the new `computeRootExclusion`, l.337-354), *is* read (by `applyWidening`, l.356-366), so no unused state remains. |
| engineer-r1-f3 | Fixed | `widenGeneratedTypes` no longer parses each file twice. Extracted `computeRootExclusion` (l.337-354, exported — also reused by `main()`'s post-condition check, see architect-r1-f2) to parse every file exactly once, caching each file's `primaryNames` in the returned `primaryNamesByFile` map; the former second loop that re-called `parseGeneratedFile` solely to recompute `primaryNames` for the exclusion check now reads from that cache via the new `applyWidening` (l.356-366) instead. `widenGeneratedTypes` itself (l.378-386) is now a thin composition of `computeRootExclusion` + `expandExcludedNames` + `applyWidening`. Verified: existing `widenGeneratedTypes` unit tests (idempotency, exclusion-by-suffix, exclusion-by-spec-derived-set) all still pass unchanged against the new implementation, and the real pipeline still produces the expected 11 widened files. |
| engineer-r1-f4 | Fixed | Folded into the same `verifyWideningHappened` post-condition described under architect-r1-f2: its second invariant is exactly this finding's suggested check — when `requestOnlyComponentNames.size > 0`, assert `rootExcludedNames.size > 0` (a known request root must resolve to at least itself) — implemented at l.448-455, tested in `tests/generated/widen-enums.test.ts`'s "verifyWideningHappened" describe block (the two `Warranty`-named cases). Addressed together with architect-r1-f2 rather than as a separate mechanism since both findings converged on the same function and the same call site in `main()`. |
| engineer-r1-f5 | Fixed | Extracted the shared subschema-traversal shape (`properties`/`items`/object-valued `additionalProperties`/`allOf`/`oneOf`/`anyOf`, including the `["allOf","oneOf","anyOf"]` keyword list previously duplicated three times) into a new shared module `scripts/lib/schema-walk.mjs`, exporting `walkSchema(root, visit, visited)` and the `SUBSCHEMA_KEYWORDS` constant, plus the shared `SchemaNode`/`OpenApiOperation`/`OpenApiSpecFragment` JSDoc typedefs (also used for typescript-cop-r1-f1, below). All three original call sites now use it: `patch-spec.mjs`'s `walkAllSchemaNodes` (l.501-524) is a thin wrapper calling `walkSchema` per entry point; `widen-response-enums.mjs`'s `collectComponentRefs` (l.132-145) and `schemaHasEnum` (l.154-170) both delegate their traversal to `walkSchema`, keeping only their own per-node action ($ref-follow vs enum-detect) as the `visit` callback — exactly the "one walker, per-call-site `visit` callback" shape the recommendation asked for, going beyond the "at minimum hoist the keyword list" fallback. A fourth, newly-added traversal (`computeReachableComponentNames`, for architect-r1-f1's pruning) also reuses `walkSchema` rather than becoming a fifth reimplementation. Verified: all existing and new unit tests for both scripts pass unchanged in behavior. |
| project-lead-r1-f1 | Fixed | Added `patchMissingSuccessResponses` to `patch-spec.mjs` (l.409-493), called from `patchSpec` alongside the other anchored corrections. For every operation whose `responses` has no `200`/`204`, it collects the schema from every error-code response (checking both `application/json` and `*/*` content types — the real spec uses both) and: synthesizes a `200` when exactly one distinct schema is found (verified directly against the real spec: 40 of the 50 affected operations fall in this case, zero have conflicting schemas across error codes); leaves the operation alone, *only* if it's in the new documented `VOID_WRITE_OPERATIONS` anchor list (l.175-186, the 10 operations verified to have no schema anywhere — legitimate void writes matching Phase 5's own `WriteOpKey` table, e.g. `alert-resolve`, `device-udf-set`); or fails loud naming the operation, for either an undocumented no-schema operation or an inconsistent-schema case (neither exists in the current spec, but both are real hazards a future refresh could introduce). Verified end-to-end: re-running `npm run generate` now produces zod response schemas for all 40 previously-empty operations — a new `-v2-user` tag directory appears (previously entirely absent, since `resetApiKeys` had no schema at all) and the existing 8 tag files (`-v2-account`, `-v2-alert`, `-v2-audit`, `-v2-device`, `-v2-filter`, `-v2-job`, `-v2-site`, `-v2-system`) all gained response schemas for their previously-empty read operations — confirmed by inspecting the regenerated `src/generated/schemas/**` diff directly, not assumed. All three severity classes the recommendation named are covered by new tests in `tests/generated/patch-spec.test.ts`'s "missing success responses" describe block (5 cases: single-schema synthesis via `application/json`, synthesis via `*/*`, a documented void write passing through untouched, an undocumented no-schema operation throwing, and inconsistent schemas throwing). |
| typescript-cop-r1-f1 | Fixed | Added JSDoc `@param`/`@returns` annotations to all four flagged functions, matching the precedent already set by `dedupeExportLines`: `patchSpec` (`patch-spec.mjs` l.557-560), `computeRequestOnlyComponentNames` (`widen-response-enums.mjs` l.217-223), `verifyNoSharedEnumBearingSchemas` (l.233-240), and `widenGeneratedTypes` (l.368-377), plus the newly-added `computeRootExclusion` and `verifyWideningHappened`. `spec` parameters are typed against a shared `OpenApiSpecFragment`/`SchemaNode`/`OpenApiOperation` JSDoc typedef defined once in the new `scripts/lib/schema-walk.mjs` and imported by both scripts via `import('./lib/schema-walk.mjs').TypeName` (per the recommendation's "a shared `@typedef` importable by both the .mjs scripts and the .ts tests" — the .mjs side of that sharing; see below for the .ts side), rather than duplicated. `fileContentsByName`/`requestOnlyComponentNames`/return types are typed explicitly as `Map<string, string>`/`Set<string>`/etc. Verified directly (not assumed): re-running the reviewer's own `Parameters<typeof X>`/`ReturnType<typeof X>` probes now shows every parameter non-optional and correctly typed (`spec: OpenApiSpecFragment`, not `spec?: any`), closing the exact `patchSpec()`/`verifyNoSharedEnumBearingSchemas()` zero-argument compile hole the finding demonstrated. This surfaced a real, previously-latent structural-typing mismatch in `tests/generated/patch-spec.test.ts`'s own local `SchemaNode`/`SpecFragment` interfaces once the scripts' parameters stopped being `any` — `npm run typecheck:test` now genuinely checks every `patchSpec(spec)` call site against the real parameter type. Fixed by having the test file's `SchemaNode`/`SpecFragment` types reuse the same shared typedefs from `scripts/lib/schema-walk.mjs` (l.9-25) instead of a second, diverging local approximation — realizing the recommendation's shared-typedef suggestion on the `.ts` side too, not just the `.mjs` side. Verified: `npm run typecheck` (all three projects) and `npm test` are clean. |
