## project-lead — round 2

Re-verified `project-lead-r1-f1` against the current diff (scoped via `git diff 9c8dd39`, the
Phase 1 checkpoint, matching round 1's scoping) and the reviser's disposition, then did a fresh
exhaustive pass over everything that changed since round 1 — `scripts/patch-spec.mjs`'s new
`patchMissingSuccessResponses`/`pruneOrphanedContextSchemas`, `scripts/widen-response-enums.mjs`'s
`verifyWideningHappened`, `scripts/lib/schema-walk.mjs`, the CI/vitest reproducibility-decoupling
changes, and the regenerated `src/generated/**` — for new requirements/behavior/scope/risk issues,
not just the one I raised.

Re-verification was done directly against the real spec and generated output, not just read off
the disposition's prose: cross-checked `VOID_WRITE_OPERATIONS` (10 entries) against every
operation in the committed `spec/openapi.json` that has no `200`/`204` and no inferable
error-code schema — all 10 match exactly, and every other operation that lacked `200`/`204`
(40 of them) has exactly one distinct schema across its error codes (none of the "inconsistent
schema" branch fires in the real spec, as the notes claim); cross-checked `EXPECTED_ORPHANED_COMPONENTS`
(28 entries) against the real `Alert.alertContext.oneOf`'s 27 `$ref`'d branches plus their shared
`AlertContext` base — exact match; and spot-checked the regenerated `-v2-device`/`-v2-site`
zod files directly, confirming `getByUidResponse`/`getSiteResponse`/etc. (previously entirely
absent per r1-f1) now exist, and the new `-v2-user` tag directory (previously absent because
`resetApiKeys` had no schema at all) now exports `resetApiKeysResponse`. No discrepancies found.

### Requirements Coverage
| Requirement | Status | Notes |
|---|---|---|
| R4 — response/request schemas generated by Orval (two targets) | Fully Met | `project-lead-r1-f1`'s gap (51 read operations with zero generated response validator) is closed: `patchMissingSuccessResponses` synthesizes the missing `200` for every operation where it's safely inferable and is anchored fail-loud against the 10 genuine void writes; verified directly against the real spec and the regenerated `schemas/**`. |
| R5 — response leniency incl. compile-time enum widening | Fully Met (for what Phase 2 owns) | Unaffected by this round's changes; `verifyWideningHappened`'s new post-condition strengthens the guard without changing its behavior. |
| R8 — timestamps/alertContext corrected, survive regeneration | Fully Met | Same defect class as R4 (a spec response-schema attachment defect), now corrected deterministically and fail-loud per the pattern the phase already established for `TIMESTAMP_FIELDS`. |
| R15 — spec + generated committed, reproducible | Fully Met | The reproducibility test now regenerates from a genuinely empty directory and checks `git status --porcelain` (catching added/removed files, not just modified ones) rather than `git diff --exit-code`; `orval.config.ts` gained `output.clean: true` on both targets so `npm run generate` itself no longer leaves stale files behind. Decoupled from default `npm test` into `npm run test:repro`, but both CI workflows (`validate.yml` on every PR, `npm-publish.yml`'s `validate` job gating publish) now run it explicitly — the R15 gate still fires on every PR and pre-publish, just off the default local `npm test` path. |

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation |
|----|----------|--------|----------|----------|---------|----------------|
| project-lead-r1-f1 | Blocker | Closed | Requirements | — | — | ratified: `patchMissingSuccessResponses` + `VOID_WRITE_OPERATIONS` verified directly against the real spec (all 10 void writes match exactly, all other 40 previously-empty operations get a single unambiguous synthesized schema) and against the regenerated `src/generated/schemas/**` (previously-empty tag files now populated, new `-v2-user` tag present). R4/R8 are now Fully Met. |
