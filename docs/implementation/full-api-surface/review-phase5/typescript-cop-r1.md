## typescript-cop — round 1

Scoped to the Phase 5 diff vs. `origin/main`: `src/rate-limit/{rate-limits,rate-limiter}.ts`,
`src/http/{http-client,axios-augment.d}.ts`, `src/auth/{auth-manager,token-store}.ts`, and their
`tests/unit/{rate-limit,http,auth}/*.test.ts`. `errors/*`, `defaults.ts`, and
`client/datto-client-config.ts` are read for context only (owned by earlier phases, unchanged
here).

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation |
|----|----------|--------|----------|----------|---------|----------------|
| typescript-cop-r1-f1 | High | Open | BoundaryValidation | `src/rate-limit/rate-limiter.ts:110-122` (`writeWindowFor`) | `opKey in WRITE_LIMITS` is used to gate `WRITE_LIMITS[opKey as WriteOpKey]`, but `in` also matches **inherited** keys, and `WRITE_LIMITS` is a plain object literal (inherits `Object.prototype`). For `opKey` equal to `"toString"`, `"constructor"`, `"valueOf"`, `"hasOwnProperty"`, `"isPrototypeOf"`, `"propertyIsEnumerable"`, `"toLocaleString"`, or `"__proto__"`, the check passes and `WRITE_LIMITS[opKey]` returns a **function/object**, not a `number` — verified directly (`"toString" in WRITE_LIMITS` → `true`, `WRITE_LIMITS["toString"]` → `[Function: toString]`). That value is then passed as `limit` to `new SlidingWindow(limit, this.windowMs)`, so `this.timestamps.length < this.limit` is permanently `false` (numeric coercion of a function yields `NaN`) and `msUntilRoom` returns `NaN` (indexing an empty `timestamps` array). `acquire` then calls `sleep(NaN)`, which Node coerces to a ~0/1ms timer, producing a tight busy-loop that never resolves — for the one caller path (the untyped `opKey?: string` fallback) this code's own doc comment says exists specifically "as defence-in-depth for a hypothetical direct/untyped caller." The defense is unsound for exactly the malformed/adversarial input it's meant to guard against. No test exercises a prototype-chain key. | Use an own-property check instead of `in`: `Object.hasOwn(WRITE_LIMITS, opKey)` (or `Object.prototype.hasOwnProperty.call(WRITE_LIMITS, opKey)` if the target doesn't support `Object.hasOwn`), then the existing `WRITE_LIMITS[opKey as WriteOpKey]` cast is sound because the check now proves own-membership, not prototype-chain membership. |
| typescript-cop-r1-f2 | High | Open | BoundaryValidation | `src/auth/auth-manager.ts:113-137` (`refreshToken`) | `this.grantClient.post<TokenResponse>(...)` types the OAuth2 grant response as `TokenResponse` (`{ access_token: string; expires_in: number }`) purely via the axios generic — a compile-time assertion with **no runtime check** that the body actually has that shape. If the auth server ever returns a body missing `access_token`/`expires_in`, or `expires_in` as a non-numeric value, `response.data.access_token` silently becomes `undefined` (typed `string`) and `expiresAt = issuedAt + response.data.expires_in * 1000` becomes `NaN`. `needsRefresh` then computes `totalTtlMs = NaN`, `totalTtlMs <= 0` is `false` (NaN comparisons are always false), `remainingPct = NaN`, and `NaN < this.refreshPct` is `false` — so a malformed grant response is silently cached as a permanently "never needs refresh" token carrying `accessToken: undefined`, and every subsequent request goes out with `Authorization: Bearer undefined` instead of failing loudly. This is unvalidated external input (the auth server's JSON body) treated as strongly typed data at a trust boundary. No test supplies a malformed/incomplete 200 grant response. | Validate the grant response body before trusting it (e.g. a small zod schema `z.object({ access_token: z.string().min(1), expires_in: z.number().positive() })`, or explicit `typeof`/`Number.isFinite` guards) and throw `DattoApiError` on failure, mirroring how `refreshToken`'s own `catch` already maps transport failures — so a malformed 200 fails the same way a malformed error response does, rather than caching an unusable token. |
| typescript-cop-r1-f3 | Medium | Open | Exhaustiveness | `src/rate-limit/rate-limiter.ts:130-135` (`acquire`) | `const windows = descriptor.kind === "read" ? [this.readWindow] : [...]` treats anything that isn't `"read"` as `"write"` via the `else` branch of a ternary, rather than exhaustively matching the `"read" \| "write"` union. If `RateDescriptor.kind` ever gains a third variant (a plausible future addition alongside `opKey`), this silently routes the new kind through the write-window logic with no compile error to catch the gap. | Replace the ternary with a `switch (descriptor.kind)` with explicit `case "read":`/`case "write":` and a `default:` that assigns to a `never`-typed variable (e.g. `const _exhaustive: never = descriptor.kind;`), so adding a new `kind` value is a compile error here until handled. |

