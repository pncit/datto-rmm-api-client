## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Added `sanitizeAxiosErrorCause`/`SanitizedAxiosErrorCause` to `src/errors/datto-api-error.ts`, returning only `{name, message, code, status, method, url}` from a failed `AxiosError` — never `config.data`/`config.auth`/`config.headers`. `DattoApiError.fromAxiosError` now sets `cause: sanitizeAxiosErrorCause(err)` instead of `cause: err`, so `AuthManager.refreshToken`'s (now `performRefresh`'s) axios-error catch branch, which rethrows via `fromAxiosError`, is covered too. Updated `tests/unit/errors/datto-api-error.test.ts` (the old `cause).toBe(axiosErr)` assertion no longer holds now that cause is redacted; replaced with a dedicated test asserting the cause is not the raw error and contains no header data) and added `tests/unit/auth/auth-manager.test.ts`'s "does not expose apiKey/apiSecret … on a failed grant" test. |
| architect-r1-f2 | Fixed | `build403Error` (`src/http/http-client.ts`) now parses `Retry-After` via the existing `parseRetryAfterMs`/`readHeader` and sets `retryAfterMs` on the `ip-block` branch (`undefined` for `forbidden`). Added a test asserting an ip-block 403's `retryAfterMs` is populated from the header. |
| architect-r1-f3 | Fixed | `AuthManager.refreshToken` (`src/auth/auth-manager.ts`) now coalesces: it caches the in-flight `performRefresh()` promise on `this.pendingRefresh` and returns the same promise to every concurrent caller until it settles. Added `tests/unit/auth/auth-manager.test.ts`'s "coalesces concurrent getToken calls … into a single grant round-trip" test (three parallel `getToken()` calls against a `nock(...).once()` mock). |
| architect-r1-f4 | Fixed | Added `DEFAULT_TIMEOUT_MS = 30_000` to `src/defaults.ts`. Both `axios.create` call sites now set a timeout: `createHttpClient` (`src/http/http-client.ts`, via a new `HttpClientConfig.timeoutMs` override) and `AuthManager`'s `grantClient` (`src/auth/auth-manager.ts`, via a new `AuthManagerConfig.timeoutMs` override). Added a test per module asserting a stalled request past `timeoutMs` becomes a retryable/throwable transport failure instead of hanging. |
| architect-r1-f5 | Fixed | Gave `AuthManager.invalidate()` its intended caller without coupling `http-client.ts` to `AuthManager`: added an optional `HttpClientConfig.onUnauthorized` hook; `handleResponseError` now invokes it and retries the failing request exactly once on a 401 (tracked via a hidden `__dattoUnauthorizedRetried` config flag to prevent a loop), falling through to the existing generic-error path when the hook is unset or already used. Documented the intended wiring (`onUnauthorized: () => authManager.invalidate()`, at the Phase 7 client scaffold) in both modules' doc comments and on `invalidate()` itself. Added three tests: hook invoked + single retry succeeds, hook invoked but not retried twice, and no-hook-configured 401 still throws plainly. |
| architect-r1-f6 | Fixed | Extracted `sleep` (`src/util/sleep.ts`) and `isRecord` (`src/util/is-record.ts`) into one shared definition each, replacing the duplicated copies in `src/http/http-client.ts`, `src/rate-limit/rate-limiter.ts`, and `src/errors/datto-api-error.ts`. Also folded `readHeader`'s case-insensitive lookup into a single correct implementation (see `engineer-r1-f6`) rather than leaving three near-identical header readers; `extractRequestId`/`extractErrorMessage` already shared `firstNonEmptyString`, so no further consolidation was needed there. |
| engineer-r1-f1 | Fixed | Same fix as `architect-r1-f1` (`sanitizeAxiosErrorCause`, applied at `fromAxiosError` and both of `http-client.ts`'s direct constructors, `build403Error`/`buildRateLimitError`). |
| engineer-r1-f2 | Fixed | Same fix as `architect-r1-f3` (single-flight `pendingRefresh` coalescing in `AuthManager.refreshToken`). |
| engineer-r1-f3 | Fixed | Added an optional `logger?: DattoLogger` to `HttpClientConfig`, `RateLimiterOptions`, and `AuthManagerConfig`. `handleResponseError` now emits a `debug` at each backoff/429 retry wait and a `warn` when a 429 wait is abandoned (bounded-out or attempts-exhausted); `MultiWindowRateLimiter.acquire` emits a `debug` on every throttle wait (`kind`/`opKey`/`waitMs`, no bodies/headers); `AuthManager` emits a `debug` on each refresh attempt and a `warn` on a failed/malformed grant. Added a test per module asserting the logger is invoked with the expected shape. |
| engineer-r1-f4 | Fixed | Same fix as `architect-r1-f6` — `sleep` now lives once in `src/util/sleep.ts`, imported by `http-client.ts` and `rate-limiter.ts`. |
| engineer-r1-f5 | Fixed | Same fix as `architect-r1-f6` — `isRecord` now lives once in `src/util/is-record.ts`, imported by `http-client.ts` and `datto-api-error.ts`. |
| engineer-r1-f6 | Fixed | Rewrote `readHeader` (`src/http/http-client.ts`) to iterate the headers object and compare keys lowercased against the target, rather than probing three fixed casings (dropping the dead `.toUpperCase()` branch and the missed-mixed-case gap). Covered by the existing `Retry-After`/`x-request-id` tests plus the new header-alone `isRateLimitBlock` test (`project-lead-r1-f3`). |
| project-lead-r1-f1 | Fixed | Exported `extractRequestId` from `src/errors/datto-api-error.ts` (and re-exported it from `src/errors/index.ts`); `build403Error` and `buildRateLimitError` (`src/http/http-client.ts`) now pass `requestId: extractRequestId(error.response?.headers)`. Added tests asserting a request-id header round-trips into `error.requestId` on both a 403 (ip-block and forbidden) and an exhausted 429. |
| project-lead-r1-f2 | Fixed | Same fix as `architect-r1-f1`/`engineer-r1-f1` — the redacted `sanitizeAxiosErrorCause` now backs every `DattoApiError` construction on both the v2 transport path and `AuthManager`'s grant/refresh path, so neither the live Bearer token nor the raw `apiKey`/`apiSecret` reach `error.cause`. |
| project-lead-r1-f3 | Fixed | Added a direct `isRateLimitBlock` unit test asserting the `Retry-After`-header-alone branch resolves to `true` even when the body doesn't match the rate-limit wording, plus an end-to-end `createHttpClient` test with the same shape (body says "insufficient scope", header carries `Retry-After: 5`) asserting the thrown `DattoApiError.code` is `'ip-block'` with `retryAfterMs: 5000`. |
| typescript-cop-r1-f1 | Fixed | `writeWindowFor` (`src/rate-limit/rate-limiter.ts`) now checks `Object.hasOwn(WRITE_LIMITS, opKey)` instead of `opKey in WRITE_LIMITS`, so an inherited `Object.prototype` key (`toString`, `constructor`, `__proto__`, …) can no longer resolve to a non-numeric "limit" and silently produce an unbounded `sleep(NaN)` loop. Added a regression test acquiring 100 writes with `opKey: "toString"` and asserting the 101st still throttles like any other unlisted opKey (falls back to `DEFAULT_WRITE_LIMIT`). |
| typescript-cop-r1-f2 | Fixed | Added a zod schema (`tokenResponseSchema` in `src/auth/auth-manager.ts`: `access_token: z.string().min(1)`, `expires_in: z.number().positive()`) and `safeParse` the grant's 200 body before trusting it; a malformed body now throws `DattoApiError` instead of silently caching `accessToken: undefined`/`expiresAt: NaN`. Added tests for a missing `access_token` and a non-numeric `expires_in`. |
| typescript-cop-r1-f3 | Fixed | Replaced the `kind === 'read' ? … : …` ternary in `MultiWindowRateLimiter.acquire` with a new `windowsFor` method using a `switch` over `descriptor.kind` and a `default` branch assigning to a `never`-typed variable, so a future third `RateDescriptor.kind` value fails to compile here rather than silently routing through the write path. Added a runtime regression test forcing an unrecognized `kind` via a type-assertion cast and asserting it throws instead of silently matching a window set. |
