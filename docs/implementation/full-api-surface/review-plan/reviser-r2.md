## reviser — round 2

Disposition of the round-2 PlanAuditor `Open` findings against `full-api-surface/plan.md`.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r2-f1 | Fixed | Pinned the `retry` sub-object shape in Phase 3 Step 4 (`z.strictObject({ maxAttempts, baseDelayMs, maxDelayMs })`, all optional) and exported the defaults as a named constant `DEFAULT_RETRY = { maxAttempts: 3, baseDelayMs: 250, maxDelayMs: 5000 }` — the same determinism fix as `DEFAULT_TOKEN_REFRESH_PCT`. Phase 5 Step 3 now backs off/retries within `retry.maxAttempts ?? DEFAULT_RETRY.maxAttempts` with backoff bounded by the `base/maxDelayMs` defaults, and the Phase 5 http-client test asserts a 5xx retries exactly `DEFAULT_RETRY.maxAttempts` times against the imported constant (plus an override-is-honored case). Also pinned `rateLimit?` as a strict override sub-object (`readLimit`/`writeAggregateLimit`/`windowSeconds`/`defaultWriteLimit`) that falls back to the committed `rate-limits.ts` table constants rather than replacing it. |
| plan-auditor-r2-f2 | Fixed | Added the concrete discrimination rule to Phase 2 Step 3: the codemod scans only `src/generated/types/**` and widens enum unions only inside exported type declarations whose name does **not** end in Orval's request-side suffixes (`Body`/`Params`/`Parameter(s)`/`Query`/`QueryParams`/`Header(s)`/`PathParameters`), captured as a documented constant; component-schema/response DTOs are widened, request/param/body enums stay closed. Strengthened `widen-enums.test.ts` to assert a `*Body` and a `*Params` enum are left closed while a component-schema (response) enum is widened — catching the over-widen that Phase 9's response-only alignment test would miss. |
| plan-auditor-r2-f3 | Fixed | Dropped `axiosInstance?` from `dattoRmmClientConfigSchema` (Phase 3 Step 4). No phase consumed it and the design's public-surface config block never listed it, so it was exactly the dead/unwired config knob R14 retires. The client always constructs its own axios instance (Phase 5) so auth/rate-limit/retry interceptors are guaranteed wired; added an explicit note stating a caller-supplied instance is deliberately not accepted. `.strict()` on the config schema rejects it as an unknown key. |
| plan-auditor-r2-f4 | Fixed | Folded the residual prose-only Exit-Gate assertions into the fenced blocks. Phase 2: `git ls-files --error-unmatch` for both committed spec files, `! git ls-files --error-unmatch spec/openapi.patched.json`, a `.gitignore` grep for the patched spec, and non-empty/`schemas/*/*.zod.ts` + `types/` checks on `src/generated`. Phase 10: `test -f dist/index.js && test -f dist/index.d.ts`, and noted the README-content is guarded by `readme.test.ts` under `npm test`. |

All four `Open` findings resolved by plan edits; no escalations. Self-review confirmed the `axiosInstance` removal leaves no dangling references (remaining "axios instance" mentions all refer to the internally-constructed instance), and the new `DEFAULT_RETRY` constant is referenced consistently across Phase 3 (definition), Phase 5 Step 3 (use), and the Phase 5 test.
