## reviser — round 6

Mode B, Phase 2 feedback. Disposition of every `Open` finding from `architect-r1` and `engineer-r1`.
An internal self-review (phasing / specificity / exit gates / edge cases / hidden complexity /
dependencies / design alignment) was folded into the same revision; it produced no separate findings
beyond what is dispositioned below. All findings accepted and folded into `plan.md`; none escalated.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r1-f1 | Accept | Reconciled entities are now the single source of truth: Phase 6 Step 3 exports `Device`/`Alert` (and any override-touched entity) as `z.infer` of the override schemas via `schema-overrides/types.ts`; Phase 7/8 method signatures and the barrel use those, so the exported type matches the runtime value (udf record, open `alertContext`). |
| architect-r1-f2 | Accept | Phase 8 Step 7 replaces `export * from './generated/types'` with a hand-maintained curated `src/public-types.ts`; `surface.test.ts` asserts generated-only names (dead `*Context`, `*Body`/`*Params`) are absent and every curated name resolves, so a regeneration rename fails a test instead of silently changing the `1.0.0` surface. |
| architect-r1-f3 | Accept | Phase 5 Step 4 now mandates a **separate bare axios instance** for the OAuth token round-trip (no Bearer/rate-limit/retry interceptors); only the shared instance carries the v2 stack, so the token call cannot consume the read window or attach v2 auth. |
| architect-r1-f4 | Accept | Phase 1 Step 3 drops the committed/consumed endpoints layer: Orval still emits endpoints (spec has paths) but they are git-ignored (`src/generated/endpoints/`, Step 6), no `override.mutator` is set, and `axios-mutator.ts` is removed — the shared instance is built directly in Phase 5's `http-client.ts`. Committed generated tree is exactly `types/**` + `schemas/**`; resource paths are the single source, reconciled by the coverage-map test. |
| architect-r1-f5 | Accept | Phase 6 Step 3 changes `pageDetailsSchema` from `z.strictObject` to `z.object({...}).catchall(z.unknown())` — strict on required fields/types, tolerant of a benign added envelope key; the throw is reserved for missing/mistyped cursor fields, not unknown keys. Test updated to assert an extra key does not abort the walk. |
| architect-r1-f6 | Accept | Phase 5 Step 1 exports `WriteOpKey = keyof typeof WRITE_LIMITS`; Phase 6 Step 1 types the `http*` write primitives' `opKey: WriteOpKey`, so a mistyped opKey is a compile error rather than a silent fallback to `DEFAULT_WRITE_LIMIT`. |
| architect-r1-f7 | Accept | Phase 6 Step 3 widens the udf value schema to `z.union([string, number, boolean, record, array]).nullable()`, so a real non-string UDF validates instead of dropping the whole device; this reconciles the schema with the mask decorator's non-string defense (both sites now agree UDFs may be non-string). |
| architect-r1-f8 | Accept | Phase 1 Step 4 pins `moduleResolution: "Bundler"` explicitly and forbids `NodeNext` (which would demand `.js` import extensions the old surface lacks, breaking the coexistence typecheck gate). |
| engineer-r1-f1 | Accept | Phase 5 Step 3 adds `src/http/axios-augment.d.ts` (`declare module 'axios'` adding `rateDescriptor?: RateDescriptor`) referenced from `http-client.ts`, so attach sites and interceptor reads pass `tsc --noEmit`. |
| engineer-r1-f2 | Accept | Resolved by architect-r1-f4: one shared interceptor-bearing axios instance, endpoints layer unused/uncommitted. Phase 6 Step 1 states BaseResource uses that single instance so nothing bypasses auth/rate-limit/retry. |
| engineer-r1-f3 | Accept | Phase 3 Step 1 pins `DattoValidationError(zodError, stage, opts?: { payload?, context? })`; Phase 5/6 examples (2- and 3-arg) conform to it. |
| engineer-r1-f4 | Accept | Phase 3 Step 1 pins the `DattoApiError(message, opts)` constructor (with `cause?`), and states `fromAxiosError` delegates to the same constructor, so the 403 direct-construction path and `fromAxiosError` agree. |
| engineer-r1-f5 | Accept | Phase 2 Step 2 makes `patch-spec.mjs` collect missing anchors and `throw` (non-zero exit) so a drifting refresh fails at the patch step; sketch and `patch-spec.test.ts` updated with a missing-anchor throw case. |
| engineer-r1-f6 | Accept | Phase 2 Step 4 adds a guard (run in the codemod / asserted in `widen-enums.test.ts`) that throws if any request body `$ref`s a component schema also used as a response, verifying the load-bearing suffix-heuristic assumption instead of trusting it. |
| engineer-r1-f7 | Accept | Phase 8 coverage-map test now drives each mapped `client[ns][method]` under a nock intercept scoped to the exact verb+path and asserts it is hit, so a drifted/missing resource path fails the guard — verifying resources, not just the map. |
| engineer-r1-f8 | Accept | Removed the stray trailing `</content>`/`</invoke>` lines at the end of the plan. |
| engineer-r1-f9 | Accept | Phase 8 Step 8 moves the retained real captures to `tests/fixtures/` and removes `src/__tests__/`; Phase 9 Step 1 references the single fixture home; exit gate adds `test ! -d src/__tests__`. |
| engineer-r1-f10 | Accept | Phase 1 Step 1 pins `@types/node@^22` (supported Node ≥ 20 floor) instead of fuze-api's `^26`. |
| engineer-r1-f11 | Accept | Phase 3 Step 4 introduces `src/client/defaults.ts` holding the cross-cutting scalars (`DEFAULT_RETRY`, `DEFAULT_TOKEN_REFRESH_PCT`); config, http-client, and auth-manager import from it. Rule stated: cross-cutting scalars → `defaults.ts`; single-subsystem domain constants (rate-limit table) stay in their module. |
| engineer-r1-f12 | Accept | Phase 5 Step 3 + example now parse `Retry-After` in both delta-seconds and HTTP-date forms (falling back to computed backoff when unparseable); a nock test for the HTTP-date and unparseable cases is added. |
| engineer-r1-f13 | Accept | Phase 3 mask test adds a numeric udf case (`udf3: 12345 → '[redacted - 5 characters]'`) and asserts the sink never receives `12345`. |
| engineer-r1-f14 | Accept | Phase 5 Step 3 places `isRateLimitBlock(response)` as a named exported predicate in `http-client.ts`, one documented/tested source consumed by the 403 path. |
| engineer-r1-f15 | Accept | Phase 2 Step 1 reworded to a single deterministic serialization (2-space indent, trailing newline, key order preserved — no re-sorting), noting reproducibility derives from the frozen committed file. |
| engineer-r1-f16 | Accept | Phase 6 Step 2 prose now states the exact signature `paginate(startPath, arrayKey, itemSchema, params?, context?)`, matching the example. |
