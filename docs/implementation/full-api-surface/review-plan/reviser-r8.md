## reviser — round 8

Dispositions for all `Open` findings from architect-r3 and engineer-r3. Internal self-review folded
into the same revision (no separate output). All edits made to `plan.md` directly; no revision markers.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r3-f13 | Accept | Relocated the cross-cutting scalars module from `src/client/defaults.ts` to a top-level, layer-neutral `src/defaults.ts`. `src/http/http-client.ts` and `src/auth/auth-manager.ts` now depend **downward** on it instead of importing *upward* into `src/client/`, breaking the `client → http → client` directory import cycle. Updated Phase 3 Step 4 (both the `DEFAULT_RETRY` line and the "cross-cutting scalar defaults" line, with an explicit rationale for the placement), the Files list, the coexistence-rule new-paths list, and the Phase 5 Step 3(b)/Step 4 import references. The single-source rule and the "single-subsystem domain constants stay with their subsystem" rule are preserved verbatim. |
| engineer-r3-f1 | Accept | Scoped the coverage-map sample-body requirement to write ops that **actually declare a request-body override**. Phase 8 coverage-map step now: (a) requires a minimal valid sample body only for body-carrying writes; (b) explicitly exempts bodiless writes (`filter-delete` DELETE; path/verb-only POSTs `alert-resolve`/`alert-mute`/`alert-unmute`/`user-reset-keys`/`device-move`), which reach the nock intercept with placeholder path params only; (c) restates the "fail if a write op lacks a sample body" rule as applying only to override-declaring ops, so a bodiless write with no factory entry is not a failure. The 75-op intercept-hit guarantee is retained. |
| engineer-r3-f2 | Accept | Pinned the auth-path error-mapping site. Phase 5 Step 4 "Transport isolation" now states `AuthManager` wraps its own grant/refresh call in `try/catch` and rethrows `DattoApiError.fromAxiosError(err)` — the same construction path as Step 3 — because the bare axios instance has no response-error interceptor. This gives the "a failed grant throws `DattoApiError`" test a single defined source and closes the gap where a raw `AxiosError` could otherwise escape. |
