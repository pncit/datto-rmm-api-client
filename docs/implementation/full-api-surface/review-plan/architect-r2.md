## architect — round 2

Plan Review Mode, round 2 (in-progress review of `full-api-surface/plan.md` against `design.md`).

Prior turn: `architect-r1` raised f1–f8, all dispositioned **Accept** by `reviser-r6`. I re-verified each
fix against the current plan text and ratified them (→ `Closed`); details in the table. No earlier-closed
findings to re-list.

Re-verification of ratified fixes:
- **f1** — Phase 6 Step 3 exports reconciled `Device`/`Alert` as `z.infer` of the override schemas via
  `schema-overrides/types.ts`; Phase 7 example (l.485) and Phase 8 barrel (l.532, l.547) consume those. Single source restored.
- **f2** — Phase 8 Step 7 replaces `export *` with curated `src/public-types.ts`; `surface.test.ts` (l.554) asserts generated-only names absent; exit-gate grep (l.573) forbids the wildcard.
- **f3** — Phase 5 Step 4 (l.362) mandates a separate bare axios instance for the OAuth round-trip; only the shared instance carries the v2 stack.
- **f4** — Phase 1 Step 3 (l.67) git-ignores the endpoints artifact, sets no mutator, removes `axios-mutator.ts`; committed tree is exactly `types/**`+`schemas/**`.
- **f5** — Phase 6 Step 3 (l.424) uses `z.object(...).catchall(z.unknown())` for `pageDetailsSchema`.
- **f6** — Phase 5 Step 1 (l.351) exports `WriteOpKey = keyof typeof WRITE_LIMITS`; Phase 6 Step 1 (l.417) types write primitives' `opKey: WriteOpKey`.
- **f7** — Phase 6 Step 3 (l.422) widens the udf value schema to a nullable union.
- **f8** — Phase 1 Step 4 (l.69) pins `moduleResolution: "Bundler"` and forbids `NodeNext`.

New findings this round arise from the revisions themselves (axis d/e and public-API surface).

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation / update |
|----|----------|--------|----------|----------|---------|-------------------------|
| architect-r1-f1 | High | Closed | DataModel | Phase 6 Step 3 / Phase 8 | — | ratified: reconciled `z.infer` entity types are the single source; barrel + method signatures use them. |
| architect-r1-f2 | Medium | Closed | PublicAPI | Phase 8 Step 7 | — | ratified: curated `public-types.ts` + `surface.test.ts` gate; `export *` forbidden by exit gate. |
| architect-r1-f3 | High | Closed | Boundaries | Phase 5 Step 4 | — | ratified: separate bare axios instance isolates the token transport from the v2 stack. |
| architect-r1-f4 | High | Closed | Boundaries | Phase 1 Step 3 | — | ratified: endpoints layer git-ignored/unconsumed; hand-written paths are the single source, reconciled by the coverage-map test. |
| architect-r1-f5 | Medium | Closed | DataModel | Phase 6 Step 3 | — | ratified: `.catchall(z.unknown())` tolerates benign added envelope keys; throw reserved for missing/mistyped cursor fields. |
| architect-r1-f6 | Medium | Closed | Boundaries | Phase 5 Step 1 | — | ratified: `WriteOpKey` union types the write primitives; a mistyped opKey is a compile error. |
| architect-r1-f7 | Medium | Closed | DataModel | Phase 6 Step 3 | — | ratified: udf value schema widened to a nullable union; schema and masker now agree. |
| architect-r1-f8 | Medium | Closed | Migration | Phase 1 Step 4 | — | ratified: `moduleResolution: "Bundler"` pinned, `NodeNext` forbidden. |
| architect-r2-f9 | Medium | Open | PublicAPI | Phase 5 Step 3 — `src/http/axios-augment.d.ts` (`declare module 'axios' { interface AxiosRequestConfig { rateDescriptor?: RateDescriptor } }`) | The `rateDescriptor` threading is implemented as a **global module augmentation** of the third-party `axios` package, and `http-client.ts` (reachable from the `src/index.ts` entry) references it. With `tsup dts:true` rolling declarations up from the entry graph, an ambient `declare module 'axios'` reached through that graph can be emitted into the published `index.d.ts` — which then **globally pollutes every consumer's `AxiosRequestConfig`** with `rateDescriptor` the moment they import both this package and axios. This is exactly the "internal build detail leaking into the published artifact" the package-quality bar forbids, and it silently widens a dependency's public surface for downstream users. The plan pins the augmentation but never scopes its emission. | State that the augmentation is an **internal-only** typecheck aid that must **not** appear in the published types: either exclude `axios-augment.d.ts` from the emitted `.d.ts` (keep it in the typecheck program but out of the tsup dts rollup / entry graph), or drop the global augmentation entirely and carry the descriptor via a module-local `WeakMap<AxiosRequestConfig, RateDescriptor>` (or a typed wrapper) so the axios global interface is never mutated. Add a Phase 8 assertion that `dist/index.d.ts` contains no `declare module 'axios'`. |
| architect-r2-f10 | Medium | Open | Architecture | Phase 8 Step 8 — `tests/unit/client/coverage-map.test.ts` (drive each `client[ns][method](…sample args)` under a scoped nock intercept) | The coverage-map test — the plan's stated authoritative R1 guard — drives **every** mapped operation, including the ~10 write ops (`setUdf`, `resolve`, `move`, `site-create`, `user-reset-keys`, …), and asserts the request reaches nock. But writes run `validateRequest` (strict, `.strict()` + required-field marks) **before** the request is sent; a placeholder/empty body will fail strict validation and **throw inside the client before any request line reaches nock**, so the assertion "the intercept is hit" can never pass for write operations. The plan specifies placeholder *path ids* but says nothing about supplying a valid body per write op — so as written the guard either can't cover writes (leaving the write half of R1 unverified) or silently degrades to reads only. | Specify that the coverage map carries a **minimal valid sample body** for each write operation (satisfying that op's write-body override), so `validateRequest` passes and the request reaches nock; or explicitly scope the nock-drive assertion to reads and add a separate mechanism (e.g. per-resource write tests already in Phase 7/8) as the write-side coverage guarantor. State which, so the R1 guard actually covers all 75 operations. |
| architect-r2-f11 | Medium | Open | Performance | Phase 4 Step 3 (aggregation "for strips/widenings … not [drops]") + Phase 6 Step 1/2 `validateArrayResponse` (per-item drop logged at `warn`) | The plan deliberately aggregates unknown-key strips and enum widenings to "keep a fully-walked 848-device / 1500-alert page from producing thousands of lines or running the masker in a per-row hot path," but **per-item drops are explicitly left un-aggregated** ("logged at `warn` by `validateArrayResponse`, not here"). A systematic schema drift (one required field mistyped across a page) drops *every* item, producing N `warn` lines — each invoking the deep-walk UDF masker on the full, UDF-bearing dropped item. That is precisely the per-row masker hot path + log flood the design/plan says it is avoiding for the benign paths, now on the *drop* path where the payload is guaranteed UDF-bearing. | Bound the drop path too: aggregate/cap per-call drops into one `warn` summary (`dropped 50/848 items on <context>; first K errors: …`, with a count) rather than one masker-invoked line per dropped item. Preserve visibility (a drop is real data loss) while keeping the masker off the per-row path, consistent with the strip/widen aggregation rationale. |
| architect-r2-f12 | Medium | Open | Migration | Phase 1 Step 4 (`tsconfig.json`: set `moduleResolution: "Bundler"`) | Pinning `moduleResolution: "Bundler"` (the f8 fix) is only valid when `compilerOptions.module` is `"ESNext"`/`"Preserve"`; with any other `module` value `tsc` errors (`Option 'moduleResolution' can only be "Bundler" when 'module' is set to …`), breaking Phase 1's own `typecheck` exit gate and the coexistence invariant. The step changes `moduleResolution` but is silent on the companion `module` setting, leaving a required constraint implicit — a build-config hazard for an implementor whose current `module` is `NodeNext`/`CommonJS`. | In Phase 1 Step 4, state that `module` must also be `"ESNext"` (or `"Preserve"`) alongside `moduleResolution: "Bundler"`, and set it if the repo's current value isn't one of those — so the typecheck gate holds. Note the pairing explicitly rather than leaving it to inference. |
