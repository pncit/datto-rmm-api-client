## reviser — round 4

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r4-f1 | Fixed | Phase 9 Step 3 now scopes the secret-scan to all three committed spec+fixture roots — `spec/`, `tests/fixtures/`, **and `src/__tests__/fixtures/`** — so the retained real device captures the Assumptions promise the scan will confirm are actually inspected. The Phase 9 Exit-Gate note and the Assumption (formerly line 15) were updated to state the confirmed-by-scan claim is now mechanically enforced over those exact files, and `scan-secrets.test.ts` asserts a planted secret under `src/__tests__/fixtures/` is caught. |
| plan-auditor-r4-f2 | Fixed | Reconciled the "encode a non-null `udf300`" mandate (Step 1) with the "reject cleartext udf secrets" heuristic (Step 3) via a documented allowlisted synthetic sentinel `SYNTHETIC-UDF-<n>`. Step 1 now sets the fixture's `udf300` to `SYNTHETIC-UDF-300`; Step 3's heuristic flags a `udf\d+` value only when it is a BitLocker/credential/high-entropy pattern **or** a non-null udf string that is *not* the sentinel, so the mandated leniency fixture passes while `S3CR3T`/BitLocker keys fail. The Step-3 note also clarifies `sanitize-fixtures.mjs` (real-sweep redaction to null) is separate from the sentinel-bearing synthetic fixtures, so the udf-set leniency assertion still has a non-null value. `scan-secrets.test.ts` now asserts the sentinel passes and a real secret fails. |
