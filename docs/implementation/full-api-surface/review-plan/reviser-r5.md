## reviser — round 5

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r5-f1 | Fixed | Confirmed on disk: the retained real captures `src/__tests__/fixtures/device.json`, `devicesPage.json`, and `devicesPage1.json` carry non-null, non-sentinel udf strings (`udf1:"value1"`/`"value2"`), which the round-4 Step-3 heuristic ("non-null non-sentinel udf ⇒ fail") would flag — failing Phase 9's `node scripts/scan-secrets.mjs` gate on committed files and breaking the Assumption that the scan mechanically *confirms* those captures safe. Adopted the auditor's recommendation option (a): split the Step-3 heuristic into two root-scoped branches (plan.md Phase 9 Step 3, lines 550–553). The **real-secret-pattern branch** (BitLocker/credential/high-entropy/`password`-`secret`-key) applies to all roots and is the *sole* gate on the real-capture root `src/__tests__/fixtures/`, so benign `value1`/`value2` pass while a planted credential-shaped value there is still caught. The **sentinel-strictness branch** (non-sentinel non-null udf ⇒ fail) is scoped to the synthetic `tests/fixtures/` root, which we author and can hold to the `SYNTHETIC-UDF-<n>` sentinel form. Updated the Assumption (line 15) and the Exit-Gate note (line 587) so the "mechanically confirmed"/"exits 0" claims match what the scan now does over the real captures. Extended `scan-secrets.test.ts` (line 574) to assert the retained real fixtures pass, that a BitLocker/credential-shaped value planted under `src/__tests__/fixtures/` is still caught, and — proving the branches are root-scoped — that a benign non-sentinel `value1` fails under the synthetic root while passing under the real root. |
