## engineer — round 2

Plan Review Mode, round 2 (in-progress review). Read my `engineer-r1` turn and `reviser-r6`'s
dispositions, then re-verified each against the current `plan.md`. **All sixteen `engineer-r1`
findings were `Accept`ed and I confirmed each fix is actually present in the plan text** — they are
ratified `Closed` below (carried forward by ID, no re-raise). I then re-swept the five plan-review
axes for new issues introduced or exposed by the round-1 revisions (notably the new `WriteOpKey`
closed-union contract, the nock-driven coverage-map guard, and the `Retry-After` HTTP-date path).
Five new findings follow. Architecture-level (module boundaries, public-surface shape) and
requirements-coverage concerns remain deferred to `/architect` and `/project-lead`.

### Ratification of engineer-r1 findings (all Accepted → verified in plan)
- f1 `axios-augment.d.ts` present (P5 Step 3) · f2 single shared instance, endpoints uncommitted (P1 Step 3) ·
  f3 `DattoValidationError` signature pinned (P3 Step 1) · f4 `DattoApiError(message, opts)` pinned (P3 Step 1) ·
  f5 patch-spec throws on drift (P2 Step 2) · f6 shared-schema guard added (P2 Step 4) ·
  f7 coverage-map drives resources under nock (P8) · f8 stray markup removed (file ends clean) ·
  f9 fixtures consolidated to `tests/fixtures/` (P8 Step 8) · f10 `@types/node@^22` (P1 Step 1) ·
  f11 `src/client/defaults.ts` (P3 Step 4) · f12 `Retry-After` HTTP-date parsed (P5 Step 3) ·
  f13 numeric-udf mask test (P3 tests) · f14 `isRateLimitBlock` homed in `http-client.ts` (P5 Step 3) ·
  f15 serialization wording fixed (P2 Step 1) · f16 `paginate` signature aligned (P6 Step 2).

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation / update |
|----|----------|--------|----------|----------|---------|-------------------------|
| engineer-r1-f1 | High | Closed | ErrorHandling | Phase 5 Step 3 | Ratified — `src/http/axios-augment.d.ts` module augmentation now specified and referenced. | — |
| engineer-r1-f2 | Medium | Closed | Complexity | Phase 1 Step 3 / Phase 6 Step 1 | Ratified — one shared interceptor-bearing instance; endpoints layer git-ignored/uncommitted. | — |
| engineer-r1-f3 | Medium | Closed | ErrorHandling | Phase 3 Step 1 | Ratified — `DattoValidationError(zodError, stage, opts?)` pinned; P5/P6 examples conform. | — |
| engineer-r1-f4 | Medium | Closed | ErrorHandling | Phase 3 Step 1 | Ratified — `DattoApiError(message, opts)` with `cause?` pinned; `fromAxiosError` delegates. | — |
| engineer-r1-f5 | Medium | Closed | ErrorHandling | Phase 2 Step 2 | Ratified — patch-spec collects missing anchors and throws; drift test added. | — |
| engineer-r1-f6 | Medium | Closed | Complexity | Phase 2 Step 4 | Ratified — shared-schema `$ref` guard added with throwing test. | — |
| engineer-r1-f7 | Medium | Closed | DeadCode | Phase 8 coverage-map | Ratified — map now verified against resources via scoped nock intercepts. | — |
| engineer-r1-f8 | Low | Closed | DeadCode | end of file | Ratified — stray `</content>`/`</invoke>` removed. | — |
| engineer-r1-f9 | Low | Closed | Naming | Phase 8 Step 8 | Ratified — real fixtures moved to `tests/fixtures/`, `src/__tests__/` removed (exit gate asserts). | — |
| engineer-r1-f10 | Low | Closed | MagicValues | Phase 1 Step 1 | Ratified — `@types/node@^22`. | — |
| engineer-r1-f11 | Low | Closed | MagicValues | Phase 3 Step 4 | Ratified — `src/client/defaults.ts` holds cross-cutting scalars. | — |
| engineer-r1-f12 | Low | Closed | ErrorHandling | Phase 5 Step 3 | Ratified — both `Retry-After` forms parsed, tests added. | — |
| engineer-r1-f13 | Low | Closed | Logging | Phase 3 tests | Ratified — numeric-udf mask case added. | — |
| engineer-r1-f14 | Low | Closed | Naming | Phase 5 Step 3 | Ratified — `isRateLimitBlock` is a named export in `http-client.ts`. | — |
| engineer-r1-f15 | Low | Closed | Documentation | Phase 2 Step 1 | Ratified — single deterministic serialization; contradiction removed. | — |
| engineer-r1-f16 | Low | Closed | Naming | Phase 6 Step 2 | Ratified — prose states the exact `paginate` signature. | — |
| engineer-r2-f1 | Medium | Open | ErrorHandling | Phase 5 Step 1 (`WRITE_LIMITS` table + `WriteOpKey`) vs Phase 7 Steps 2–4 & Phase 8 Steps 1–5 (variable / proxy / warranty / mute / audit / delete writes) | The round-1 fix made `opKey: WriteOpKey = keyof typeof WRITE_LIMITS` a **closed union**, so every write method across all ten namespaces must pass an `opKey` literal that exists as a key in `WRITE_LIMITS` or it is a **compile error**. But Phase 5 Step 1 only enumerates a partial key set (`'device-udf-set'`, `'site-create'`, `'alert-resolve'`, `'device-move'`, `'device-job-create'`, `'user-reset-keys'`) and then a **prose bucket** "variable/proxy/warranty mutations: 100" that names no concrete keys. The plan later requires concrete write ops for site/account variables, device warranty & proxy (Phase 7 Step 3), alert muting (Phase 7 Step 4), and any user/filter writes (Phase 8) — none of which have enumerated table keys. As written the `WriteOpKey` contract cannot compile for those writes. | Enumerate **every** concrete write `opKey` used by any resource method as an explicit key in `WRITE_LIMITS` (replace the "variable/proxy/warranty mutations" prose with the actual keys, e.g. `'site-variable-set'`, `'device-warranty-set'`, `'device-proxy-set'`, `'alert-mute'`, …). Add a note that adding a write method requires adding its key here first (single binding). |
| engineer-r2-f2 | Medium | Open | MagicValues | Phase 3 Step 4 (`rateLimit.defaultWriteLimit`) + Phase 5 Step 1–2 (`DEFAULT_WRITE_LIMIT` "fallback for any unlisted write opKey") | Once `WriteOpKey` is a **closed** union that must cover every write (see f1), there is no "unlisted write opKey" reachable through a resource call — so both `DEFAULT_WRITE_LIMIT` and its `config.rateLimit.defaultWriteLimit` override become unreachable via the typed path, i.e. a **dead config knob**, the exact anti-pattern the plan invokes R14 to avoid (it explicitly drops `axiosInstance` for this reason). The fallback is only exercisable through the limiter's untyped `acquire(opKey?: string)` boundary — which is what the Phase 5 rate-limiter test ("an unlisted write opKey falls back to 100") relies on, but no real resource can produce that path. | Resolve the tension explicitly: state that `DEFAULT_WRITE_LIMIT`/`defaultWriteLimit` are a **limiter-level defensive fallback only** (reachable via direct untyped `acquire`, never via a resource), and keep the limiter's `opKey?: string` open on purpose — or, if no untyped callers are intended, drop `defaultWriteLimit` from the config schema as a dead knob. Pick one so config surface and the closed-union contract agree. |
| engineer-r2-f3 | Medium | Open | Complexity | Phase 8 Step (coverage-map test) `tests/unit/client/coverage-map.test.ts` | The f7-hardened coverage-map now drives **every** mapped operation — including writes — via `client[ns][method](…sample args)` under a nock intercept, asserting the request line is hit. But write methods run strict `validateRequest` / the required-field write-body override (Phase 6) **before** the request is sent, so a placeholder/empty sample body will throw `DattoValidationError` and the request never reaches nock — making the guard impossible to satisfy for the write operations it is meant to cover. The step specifies placeholder **path params** but says nothing about valid request **bodies**. | Specify how write ops are probed: supply a per-op **minimal valid sample body** (a small fixtures/factory keyed by `opKey` or path) that passes the write-body override, or drive the probe through a path that bypasses request validation. State that the coverage map must include a valid sample body for every write operation. |
| engineer-r2-f4 | Low | Open | ErrorHandling | Phase 5 Step 3(b) + example (429 `Retry-After` handling) | The honored `Retry-After` wait is **unbounded**: `maxDelayMs` bounds only the *computed* exponential backoff, not the server-supplied value. A large delta-seconds (`Retry-After: 86400`) or a far-future HTTP-date makes `sleep(waitMs)` block the call for hours with no ceiling — a hang on a malformed or hostile header, inside the `retry.maxAttempts` loop. | Cap the honored `Retry-After` wait at a sane, documented ceiling (e.g. `min(waitMs, retry.maxDelayMs)` or a dedicated `MAX_RETRY_AFTER_MS` in `defaults.ts`); beyond the cap, surface the `DattoApiError` (with `retryAfterMs` populated) rather than sleeping indefinitely. Add a nock case for an over-large `Retry-After`. |
| engineer-r2-f5 | Low | Open | Logging | Phase 3 Step 3 (`withUdfMasking` — "single logger boundary … no call site can leak a raw UDF value") vs Phase 4 Step 3 (diagnostics emit "one summarized debug line per `(context, field, value)`", e.g. `widened deviceClass=rmmnetworkdevice`) | The masking decorator scrubs only the `meta` object (`scrub(meta)`); it never inspects the log **message string**. The plan's guarantee that "no call site can leak a raw UDF value" therefore holds only for values passed via `meta`. Phase 4's aggregated diagnostics interpolate `field=value` **into the message string**; any future diagnostic (or log call) that formats a wire value into the message text bypasses masking entirely, silently weakening the R20 guarantee the plan states categorically. | Tighten the invariant: require that all wire-derived values (including diagnostic `field=value` pairs) be passed through `meta` (so they hit the masker), never interpolated into the message string; or have the masker also scrub message strings. State this rule where `withUdfMasking` is described so the "single boundary" claim is actually true. |
