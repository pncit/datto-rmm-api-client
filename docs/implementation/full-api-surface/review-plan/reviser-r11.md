## reviser — round 11

Mode B, Phase 2 feedback. Read the latest Architect (`architect-r6`) and Engineer (`engineer-r6`)
turns. Engineer raised no new `Open` findings (only ratified `engineer-r5-f1` as `Closed`). Architect
raised one `Open` finding, `architect-r6-f16`. An internal self-review (phasing / specificity / exit
gates / edge cases / hidden complexity / dependencies / design alignment) surfaced no additional
issues beyond f16 and was folded into the same edit.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r6-f16 | Accept | Real defect: the top-level `Omit<z.infer<override>, DEVICE_ENUM_FIELDS> & Pick<GeneratedDevice, DEVICE_ENUM_FIELDS>` graft with `DEVICE_ENUM_FIELDS = ['deviceClass']` widens only the top-level `deviceClass`, leaving `Device`'s **nested** enums (`antivirus.antivirusStatus`, `patchManagement.patchStatus`, both really present per design l.188) typed **closed** while `parseLenient` widens them at runtime — reviving the exact R5 hazard for the flagship override-touched entity, and the r10 completeness guard either misses it (top-level-only enumeration) or deadlocks (recurses but no top-level `Pick` key can widen a nested field). Fixed via the architect's option that keeps the existing base/graft structure intact: redefined `ENUM_FIELDS` so the constant lists the **containing top-level property** whose subtree holds an open enum *anywhere* (`DEVICE_ENUM_FIELDS = ['deviceClass', 'antivirus', 'patchManagement'] as const`). Because `Pick<GeneratedDevice, 'antivirus'>` re-adds the **whole** already-fully-widened `antivirus` sub-object, the nested `antivirusStatus`/`patchStatus` enums widen together with the top-level `deviceClass` — no nested-field `Pick` key is needed, so the graft neither misses nor deadlocks. Phase 6 Step 3 now states the containing-top-level rule, records the soundness constraint (reconciled fields `udf`/`alertContext` carry no enums, so they never collide with a grafted subtree), and points to the recursive guard for any future reconciled-field-with-nested-enum case. Phase 9 Step 3 now (a) drives the truly-novel-value compile assertion over top-level **and** nested enum fields (`Device['antivirus']['antivirusStatus']`, `Device['patchManagement']['patchStatus']`, each `Alert` field) and (b) replaces the completeness guard with a **recursive** one that enumerates every enum field at every depth and asserts its containing top-level property is in `ENUM_FIELDS` (with `enumFieldPaths`/`topLevelOf` in the updated example). l.161 note now says "at every nesting depth." This converts the "same field set at every depth" property into a verified gate for the concrete nested `Device` enums. |
