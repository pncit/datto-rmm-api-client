## reviser — round 7

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r7-f1 | Fixed | Adopted the auditor's option (b), which keeps the canonical `S3CR3T` example true against the pinned predicate rather than weakening the security guarantee. Added a fourth, documented, allowlist-independent sub-pattern `SECRET_KEYWORD_VALUE_PATTERN` to the real-secret-pattern branch (Phase 9 Step 3, line 551): case-insensitive literal/leetspeak secret markers (`S3CR3T`/`SECRET`/`P4SSW0RD`/`bitlocker`/`recovery key`/`apikey`, with `3→e`/`4→a`/`0→o`/`1→l` substitutions), a documented single-point-of-update constant. This deliberately catches the short, low-entropy literal placeholders the entropy gate (len ≥ 20, H ≥ 3.5) intentionally skips — `S3CR3T` is len 6, H 2.25. Updated the independence note (line 555) to list the new sub-pattern among those unscoped by the allowlist/entropy. Corrected the inaccurate "`S3CR3T`, a BitLocker recovery key" label in the narrative (line 557): now `S3CR3T` is caught by the keyword sub-pattern and a **separate** true BitLocker-format key (`XXXXXX-XXXXXX-…`, 8 dash-separated groups) is the BitLocker example, making "fails under any root" genuinely true. Strengthened the scan test (line 578) to assert `udf5:"S3CR3T"` planted under the real root `src/__tests__/fixtures/` fails **specifically via `SECRET_KEYWORD_VALUE_PATTERN`** (not the entropy or sentinel branch), plus a true BitLocker-format key case. Committed fixtures remain unaffected (all benign; `value1`/`value2` and `SYNTHETIC-UDF-300` do not match the keyword pattern). The Phase 3 mask-test use of `S3CR3T` (line 262) is left intact — masking redacts any non-null udf regardless of content, so it is correct there. |
