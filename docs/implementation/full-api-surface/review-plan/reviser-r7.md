## reviser — round 7

Mode B, Phase 2 feedback. Read the latest Architect (`architect-r2`) and Engineer (`engineer-r2`)
turns. All prior findings (architect-r1-f1…f8, engineer-r1-f1…f16) were already `Closed`/ratified;
this round dispositions the nine new `Open` findings across both reviewers. An internal self-review
was also applied (no separate file): its issues were folded into the same edits — chiefly propagating
the value-in-`meta` masking rule into the Phase 4 illustrative example and the Phase 6 array-drop
tests, and threading `MAX_RETRY_AFTER_MS` through `defaults.ts`, the Phase 5 step, example, and test
so the cap is single-sourced.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r2-f9 | Accept | Phase 5 Step 3 now marks the `axios-augment.d.ts` `declare module 'axios'` augmentation as an **internal-only** typecheck aid: kept in the typecheck program via `tsconfig` `include` but **not** imported from any value module in the `src/index.ts` entry graph, so `tsup dts:true` (which rolls declarations by following the entry import graph) never pulls the ambient into `dist/index.d.ts`. Added a Phase 8 exit-gate assertion `! grep -qn "declare module 'axios'" dist/index.d.ts` so the no-leak property is verified, not hoped. |
| architect-r2-f10 | Accept | Phase 8 coverage-map test now specifies that each write op carries a **minimal valid sample body** (a factory keyed by `opKey`/path) satisfying its write-body override, so strict `validateRequest` passes and the request reaches the nock intercept; the test fails if a write op lacks a sample body. The R1 guard now covers all 75 operations, reads and writes alike. |
| architect-r2-f11 | Accept | Phase 6 Step 1 now has `validateArrayResponse` **aggregate drops per call** into one `warn` summary (`meta { dropped, total, firstErrors[≤K] }`) instead of one masked line per dropped row, bounding the systematic-drift flood and keeping the deep-walk masker off the per-row path — matching the strip/widen aggregation rationale. Phase 4 Step 3 cross-references it; the Phase 6 base-resource test asserts exactly one `warn` even when every item is invalid. |
| architect-r2-f12 | Accept | Phase 1 Step 4 now states `module` must be set to `"ESNext"` (or `"Preserve"`) in the same edit as `moduleResolution: "Bundler"`, calling out that `tsc` errors otherwise and breaks this phase's own typecheck gate. The pairing is stated as required, not left to inference. |
| engineer-r2-f1 | Accept | Phase 5 Step 1 now enumerates **every** concrete write `opKey` as an explicit table key (`device-udf-set` 600; `site-create`, `site-variable-set`, `account-variable-set`, `alert-resolve`, `alert-mute`, `alert-unmute`, `device-move`, `device-job-create`, `device-warranty-set`, `device-proxy-set`, `user-reset-keys`, `filter-create`, `filter-delete` at 100), replacing the "variable/proxy/warranty mutations" prose bucket, and adds the rule that a new write method requires adding its key here first (single binding). The closed `WriteOpKey` union now compiles for all writes. |
| engineer-r2-f2 | Accept | Resolved the tension explicitly. Phase 5 Steps 1–2 state `DEFAULT_WRITE_LIMIT` is a **limiter-level defensive fallback only**, reachable solely via the limiter's intentionally-untyped `acquire({ opKey?: string })` boundary and never via a typed resource call. Phase 3 Step 4 **drops `defaultWriteLimit`** from the `rateLimit` config sub-object as dead surface (the same R14 anti-pattern that dropped `axiosInstance`). The Phase 5 rate-limiter test still exercises the fallback via a direct untyped `acquire`. |
| engineer-r2-f3 | Accept | Same fix as architect-r2-f10 (duplicate): the Phase 8 coverage-map test now supplies a per-write-op minimal valid sample body so writes pass strict validation and reach nock. |
| engineer-r2-f4 | Accept | Added `MAX_RETRY_AFTER_MS = 30_000` to `src/client/defaults.ts`. Phase 5 Step 3(b), the example, and the test now bound the honored `Retry-After` wait: when the parsed delta/HTTP-date exceeds the cap the client throws `DattoApiError` (`429`, `retryAfterMs` populated) instead of sleeping, preventing a multi-hour hang on a large or hostile header. A nock case for an over-large `Retry-After` was added. |
| engineer-r2-f5 | Accept | Made the masking invariant explicit at `withUdfMasking` (Phase 3 Step 3): because the decorator scrubs only `meta`, **every wire-derived value must be passed through `meta`, never interpolated into the message string.** Phase 4 Step 3 (leniency diagnostics) and Phase 6 Step 1 (drop summary) now put `field=value` pairs in `meta` with the message carrying only static text + field name/counts, so the "single boundary / no raw UDF leak" guarantee is literally true. |
