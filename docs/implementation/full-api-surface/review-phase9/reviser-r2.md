## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Established a mechanical lockstep between the three independent "what is a UDF key" definitions instead of leaving them as unlinked literals. Exported `UDF_KEY` from `src/logging/mask.ts` (was module-private) and extracted the inline regex in `src/schema-overrides/device-overrides.ts` into a new exported `UDF_KEY_PATTERN` constant (re-exported from `src/schema-overrides/index.ts`), each now doc-commented pointing at the other two sites and the new test. Added `tests/unit/security/udf-key-pattern-consistency.test.ts`, which imports `UDF_KEY` (mask.ts), `UDF_KEY_PATTERN` (schema-overrides), and `SECRET_KEY_PATTERNS` (sanitize-fixtures.mjs) and asserts all three agree on a representative key set (`udf1`, `udf300`, `udf0`, `uid`, `udfDescription`, `apiSecretKey`, case variants, etc.), plus a non-vacuity check. A future divergence between the at-rest, in-log, or schema-shape control now fails the build instead of shipping silently. Neither exported symbol is re-exported from `src/index.ts`, so no new public-package surface was introduced. |
| architect-r1-f2 | Fixed | Added a `.gitignore` pattern `*raw-sweep.json` (matches any path ending in that suffix, at any depth) with a comment explaining the risk it guards and pointing at the sanitizer. `scripts/sanitize-fixtures.mjs`'s own usage example already names its input `raw-sweep.json`, so no example change was needed; added one sentence to the script's header doc noting the convention and that `.gitignore` now enforces it mechanically. |
| engineer-r1-f1 | Fixed | Added a second test in `tests/integration/fixtures.test.ts`'s `WIDENED_FIELDS completeness guard` block that asserts the reverse direction: for every `OVERRIDE_ENTITIES` entry, every listed `widenedFields` entry is the top level of at least one path `enumFieldPaths` returns for that entity's schema (via an explicit, documented `NO_ENUM_WIDENED_FIELDS` allowlist for any future legitimately enum-free entry — empty today, since both `DEVICE_WIDENED_FIELDS` and `ALERT_WIDENED_FIELDS` are enum-motivated end to end). A stale/mistaken `WIDENED_FIELDS` entry with no real enum now fails this gate instead of silently passing. |
| engineer-r1-f2 | Fixed | Removed the inert `validator.validateOne(...)` call from the R20 masking test in `tests/integration/fixtures.test.ts` (the fixture validates cleanly, so that call emitted no diagnostic and proved nothing about masking) and replaced the surrounding comment to accurately state the test proves masking via the direct `maskedLogger.debug` call against the fixture's genuine `udf` payload, not via the validation path. |
| engineer-r1-f3 | Fixed | Folded the `it.each(ALERT_CONTEXT_FIXTURES)` `not.toThrow` case and the separate manual `for`-loop field-preservation case in `tests/integration/fixtures.test.ts` into one `it.each(ALERT_CONTEXT_FIXTURES)` that validates and asserts each fixture's `@class`-specific fields survive, restoring per-fixture isolation (a failure now names the specific fixture) and removing the duplicate loop. |
| engineer-r1-f4 | Fixed | Added a same-resolved-path guard at the top of `main()` in `scripts/sanitize-fixtures.mjs`: `resolve(inputPath) === resolve(outputPath)` now prints an error and exits 1 before any read/write, so `sanitize-fixtures.mjs raw.json raw.json` can no longer destroy the raw capture. Added two CLI tests to `tests/unit/scripts/sanitize-fixtures.test.ts`: identical-path invocation exits 1 with the new error message and leaves the input byte-for-byte unchanged, and a same-file-different-spelling path (`dir/raw-sweep.json` vs `dir/./raw-sweep.json`) is also rejected, proving the check is resolve-based, not a naive string compare. |
| typescript-cop-r1-f1 | Fixed | Added `/** @type {unknown} */` immediately before `JSON.parse(readFileSync(inputPath, "utf8"))` in `scripts/sanitize-fixtures.mjs`'s `main()`, so `checkJs` enforces the `unknown`-first boundary `sanitizeValue`'s own signature already declares, matching the pattern already used at `sanitizeValue`'s own `Record<string, unknown>` cast in the same file. |

No Open findings from `project-lead-r1` (zero findings raised).

Verification after all fixes: `npm run typecheck` (src+test+tools), `npm run lint`, `npm test` (466/466 passing across 37 files), `npm run build`, and `npx prettier --check` on every touched file are all clean.
