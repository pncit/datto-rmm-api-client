## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | Added a `describe("CLI (main())", ...)` block to `tests/unit/scripts/sanitize-fixtures.test.ts` that runs the actual script as a subprocess (`execFileSync(process.execPath, [SCRIPT_PATH, ...])`) against a temp-dir input file: asserts the sanitized output file has the correct redacted shape, ends with a trailing newline, and that the input file is left byte-for-byte unchanged (never overwritten in place); a second test asserts the missing-args branch prints `Usage:` to stderr and exits with status `1`. This exercises argv parsing, `readFileSync`/`JSON.parse`, `writeFileSync`, and the exit-code branch that were previously untested. `npm run typecheck` and the full test file (9/9) are green. Updated `implementation-phase9-notes.md` §11 to note the CLI path is now covered, keeping the notes' remaining-risk framing accurate. |
| implementation-auditor-r1-f2 | Rejected | The auditor's own analysis already establishes this is plan-consistent, not a gap: Phase 9 Step 1's enumerated examples are device/alert only, and the design's "Reality findings" defects this phase exists to guard against are concentrated on those two entities. Broader real-shape validation across sites/users/jobs/audits/filters/activityLogs/system is explicitly named as the plan's separate Deferred Validation item ("devices, sites, users, alerts, audits" real sweep), not a Phase 9 deliverable. Expanding the synthesized corpus to entities with no known defect pattern and no plan mandate would be scope creep beyond this phase's stated intent. |
| implementation-auditor-r1-f3 | Rejected | `eslint src` predates this phase: `tests/**` (37 pre-existing test files) and every existing `scripts/*.mjs` (`patch-spec.mjs`, `widen-response-enums.mjs`, `dedupe-generated-index.mjs`) were already outside the lint gate's glob before Phase 9 added `scripts/sanitize-fixtures.mjs` and two test files — confirmed via `git log --oneline -- package.json` (no phase-9-authored change to the `lint` script) and by the pre-existing untested/unlinted sibling scripts. Widening `package.json`'s `lint` script to cover `tests/**`/`scripts/**` repo-wide is an out-of-scope, cross-cutting convention change (would surface pre-existing lint debt in 37 unrelated files), not a Phase 9 fix. Type coverage (`typecheck:test`/`typecheck:tools`, both clean) and prettier formatting already apply to these files per the notes. |
| implementation-auditor-r1-f4 | Fixed | `implementation-phase9-notes.md` §4 said "Six describe blocks" while `tests/integration/fixtures.test.ts` has five (`describe(...)` grep confirms: leniency, per-item drop, enum-widening alignment, completeness guard, UDF masking). Changed the sentence to "Five describe blocks cover the plan's named assertions" to match the file and the five items already enumerated beneath it. |
