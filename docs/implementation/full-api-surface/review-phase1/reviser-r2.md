## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Added `tsconfig.test.json` (extends the base config, `noEmit`, `rootDir:"."`, `types:["vitest/globals","node"]`, `include:["src/**/*.test.ts","src/__tests__/**/*.ts","tests/**/*.ts"]`) and wired it into the `typecheck` script (`package.json`: split into `typecheck:src`/`typecheck:test`/`typecheck:tools`, chained under `typecheck`). Verified with `tsc -p tsconfig.test.json --noEmit --listFiles`: all four `src/__tests__/*.test.ts` files are now picked up and typecheck clean (exit 0), and `npm run typecheck` runs all three passes. This closes the gap the finding identified — no command previously typechecked test code, so the plan's later compile-time type assertions (R5) would have compiled away silently; now `npm run typecheck` fails on a bad test-file type. |
| architect-r1-f2 | Rejected | Built an isolated reproduction of this repo's exact `tsconfig.json` (`moduleResolution:"Bundler"`, `paths:{"@/*":["./src/*"]}`) and `tsup.config.ts` shape (`format:["esm"]`, `dts:true`, single entry) with a `package.json` `"type":"module"`, plus a two-file `src/` with an `@/util` cross-module import/re-export. `npx tsup` produced `dist/index.d.ts` and `dist/index.js` with the `@/util` import fully inlined/resolved — `grep -R "@/" dist` found zero matches in either the JS bundle or the declaration file. This matches the code path in `node_modules/tsup/dist/index.js:1349` (`rollupDtsFile(dtsInputFilePath, dtsOutputFilePath, tsconfig)`), which hands the dts-rollup step the *same* `tsconfig.json` used for the JS bundle (not a separate/divergent resolution path as the finding claims), so `paths` governs both. There is no unresolved-`@/`-specifier risk to guard against with the current toolchain versions, so no `tsup.config.ts` change or dist-grep gate was added. |
| architect-r1-f3 | Rejected | No phase in the plan (grepped the full `plan.md`) references a coverage-threshold or coverage-percentage gate anywhere — Phase 1's Exit Gate doesn't invoke coverage at all, and no later phase's Exit Gate does either. The current `include`/`exclude` shape is an exact match of `fuze-api`'s own `vitest.config.ts` (also threshold-free), the explicit precedent this phase's plan snippet mirrors, so adding thresholds now would be inventing project policy no plan requirement or sibling-repo convention supports, and risks spuriously breaking a later phase's build as coverage naturally fluctuates while the resource surface grows through Phase 9. The "legacy code masks new code" half is not yet actionable: Phase 1 adds zero new source files, so there is no new-code signal to protect from masking until Phase 3+ creates `src/errors/`, `src/logging/`, etc. — narrowing `include` now would mean guessing at paths that don't exist yet rather than fixing an observable Phase 1 problem. Revisit scoping once those directories land. |
| engineer-r1-f1 | Fixed | Added `"src/**/*.test.ts"` and `"src/__tests__/**"` to `vitest.config.ts`'s `test.coverage.exclude` (formatted by `prettier --write`, verified `prettier --check` passes). Re-ran `npm run test:coverage`: the report now lists only real modules (`auth.ts`, `client.ts`, `httpClient.ts`, `rateLimiter.ts`, `tokenStore.ts`, `validation.ts`) — no `.test.ts` file appears, so coverage numbers reflect shipped source, not vitest-4's default (empty) exclude list inflating them with 100%-covered test files. |
| engineer-r1-f2 | Fixed | Added `"test:coverage": "vitest run --coverage"` to `package.json` `scripts` (matches `fuze-api`'s own script name/shape for the same purpose). Ran it: exits 0, prints the v8 coverage table, confirming the installed `@vitest/coverage-v8` dep and the `vitest.config.ts` coverage block are now reachable by name instead of requiring a hand-typed `vitest run --coverage` invocation. |
| typescript-cop-r1-f1 | Fixed | Added `tsconfig.tools.json` (extends the base config, `noEmit`, `rootDir:"."`, `types:["node"]`, `files:["orval.config.ts","tsup.config.ts","vitest.config.ts"]`) and wired it into `typecheck` as the `typecheck:tools` step (same `package.json` edit as architect-r1-f1). Verified with `tsc -p tsconfig.tools.json --noEmit --listFiles`: all three root config files are now listed and typecheck clean (exit 0); `npm run typecheck` now fails if any of them has a type error, closing the gap where neither `typecheck` nor `lint` parsed them. |

Full gate re-verified after all edits: `npm run lint` (0 errors, same 11 pre-existing `no-explicit-any` warnings in untouched old files), `npm run typecheck` (all three passes green), `npm test` (37/37 passing), `npm run build` (ESM `dist/index.js` + `dist/index.d.ts` emitted), `npm run test:coverage` (green, test files excluded from the report), plus the Phase 1 Exit Gate's non-npm checks (`jest.config.js` absent, no `jest`/`ts-jest`/`@types/jest` in `package.json`, all three root configs present, `npx orval --help` exits 0).
