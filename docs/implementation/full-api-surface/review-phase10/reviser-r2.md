## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Added `"./package.json": "./package.json"` to `package.json`'s `exports` map (alongside the existing `"."` entry), the standard companion entry whenever `exports` is introduced, so a consumer/tool that deep-resolves `datto-rmm-api-client/package.json` (build tools, `read-pkg`/`pkg-dir`-style helpers, `require.resolve`) keeps working instead of hitting `ERR_PACKAGE_PATH_NOT_EXPORTED`. Verified `require('./package.json').exports` reflects both entries. |
| engineer-r1-f1 | Fixed | `tests/unit/readme.test.ts` now derives a per-operation assertion from every `OPERATION_MAP` entry (`it.each(OPERATION_MAP)`), locating that method's own table row within its namespace's section (`findMethodRow`, scoped by `namespaceSection`) and asserting the row contains the entry's HTTP verb (`specMethod.toUpperCase()`) and a path matching the entry's `specPath` (placeholder-name-tolerant via `pathPattern`, since the README intentionally uses friendlier param names like `{uid}` where the spec says `{deviceUid}`). This replaces the old "method name appears anywhere in the document" check with a real per-row, per-namespace, verb+path drift guard mechanically derived from the same authoritative table — exactly what the phase notes claimed the test did. All 57 new per-operation cases pass against the current README (`npx vitest run tests/unit/readme.test.ts` — 81/81 green). |
| project-lead-r1-f1 | Fixed | Added an "Unverified shapes" callout under `### client.audit`'s table noting `getPrinter`/`getEsxiHost` are spec-derived-only (the sampled account had no printer/ESXi devices), and a matching "Unverified shape" callout under `### client.sites`'s table noting `updateProxy`/`deleteProxy` are likewise spec-derived-only (the sampled account had no site with proxy settings configured) — discharging design.md's Risks & Mitigations commitment that "README notes them as unverified" for these four operations specifically. |
| project-lead-r1-f2 | Fixed | Ran `npm install --package-lock-only` to resync `package-lock.json`'s root `name`/`version` metadata with the `package.json` `1.0.0` bump. Diffed the result (`git diff package-lock.json`) to confirm only the two `version` fields (`""`.version and top-level `version`) changed — no dependency-graph churn — so the lockfile now correctly identifies the package as `1.0.0` and the next unrelated dependency-bump PR won't carry this catch-up diff. |
| project-lead-r1-f3 | Fixed | Same fix as engineer-r1-f1 (both findings target the same weak per-namespace method-presence assertion): the existing "documents at least one method" check is now scoped to `namespaceSection(readme, ns)` instead of searching the whole document, so an identically-named method row in a sibling namespace (`get`, `list`, `variables`, …) can no longer mask a deletion in the namespace under test. The new per-operation `it.each(OPERATION_MAP)` cases add row-level verb+path coverage on top. |
| typescript-cop-r1-f1 | Fixed | Replaced the Quick Start example's two non-null assertions (`devices[0]!.uid!`, `one.uid!`) with explicit presence checks that throw a clear `Error` when `uid` is absent (`const firstUid = devices[0]?.uid; if (!firstUid) { throw new Error(...) }`, and the same pattern after `client.devices.get`), matching `Device["uid"]: string | undefined` (confirmed in `src/generated/types/device.ts` l.19) and the array-can-be-empty case. The flagship example now models the leniency the rest of the README documents instead of contradicting it, and no longer risks an unhandled `TypeError` on an account with zero devices or a partial record. |

### Verification
`npm run lint`, `npm run typecheck`, `npm test` (547/547, including `tests/unit/readme.test.ts` 81/81), and `npm run build` all pass on the current working tree. `git status --short` shows edits confined to `README.md`, `package.json`, `package-lock.json`, and `tests/unit/readme.test.ts` — no `src/**` behavior touched, consistent with this phase's scope.
