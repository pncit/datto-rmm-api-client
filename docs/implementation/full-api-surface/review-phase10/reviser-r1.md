## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | Verified against `src/http/http-client.ts`: `build403Error` (l.212-228) sets `retryAfterMs` on an `ip-block` 403 when `Retry-After` is present, and `buildRateLimitError` is invoked both on ceiling-exceeded and on retry-exhaustion (`handleResponseError` l.279-299) — the doc understated both. In `README.md`: (1) the `DattoApiError` field-list bullet (l.299-302) now reads "`retryAfterMs` (set on a 429, and also on a 403 `ip-block` when the server sends a `Retry-After`)" instead of "(429 only)"; (2) the 403 branch of the quick-start example (l.283-289) gained a line noting `err.retryAfterMs` may carry the block's wait hint on an `ip-block`; (3) the 429 branch's comment (l.278-281) now covers both conditions — ceiling-exceeded *or* retry exhausted — instead of only the ceiling case. |
| implementation-auditor-r1-f2 | Fixed | Confirmed `package.json`'s `files` (l.49-53) ships only `["dist","README.md","LICENSE"]`, so `src/public-types.ts` is unreachable for an npm-installed consumer. Changed the exported-types pointer (`README.md` l.441-442) to link `src/public-types.ts` at its GitHub location (`https://github.com/pncit/datto-rmm-api-client/blob/main/src/public-types.ts`, matching `package.json`'s `repository`/`homepage` URLs) and added a note that `dist/index.d.ts` is the on-disk equivalent once installed, so the pointer resolves for both a repo browser and an npm consumer. |
