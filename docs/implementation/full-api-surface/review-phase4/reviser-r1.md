## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | `total` no longer derives from the top-level parsed shape. `detectUnknownProperties` (`src/validation/schema-leniency.ts`) now threads a `collectionSize` parameter through every recursive call, set to `parsed.length` whenever the walk enters an array and left unchanged otherwise (starts at `1` for the initial call from `parseLenient`). `DiagnosticsCollector.record` (`src/validation/diagnostics.ts`) takes this as its 4th arg and stores it per-group (`Math.max` across occurrences, defensive); `flush` emits each group's own `total` instead of one call-wide value. This makes the enveloped-list case correct: `{ pageDetails, devices: [...848] }` now reports `total: 848` for a `devices[i].deviceClass` diagnostic, not `1`. Added a dedicated test (`schema-leniency.test.ts`, "reports total against the enclosing array's length for an enveloped list response") reproducing the design's own canonical case (3 widened values / 848 devices, enveloped) and asserting `count: 3, total: 848`. All prior tests' `total` expectations (which already assumed per-array semantics for bare top-level arrays) pass unchanged, since bare-top-level-array and enveloped-array now compute the same way. |
| implementation-auditor-r1-f2 | Fixed | Documented the coupling explicitly in `parseLenient`'s JSDoc (`src/validation/schema-leniency.ts`): a new paragraph states plainly that omitting `logger` skips null tolerance, presence tolerance, and enum degradation (delegates to strict `schema.safeParse`), names this as a real behavioral gate rather than a diagnostics-only knob, and records that it is safe only because every real call site (Phase 6's `BaseResource`) always constructs with the client's always-present `DattoLogger`. I did not add a Phase-6 code change to "ensure" this, since `BaseResource`/the resource-mounting call sites do not exist yet in this phase (plan Phase 6) — out of Phase 4 scope per the coexistence rule. Verified the pattern itself is a faithful port, not a Phase-4-introduced defect: `../fuze-api/src/validation/schema-leniency.ts:352-354` has the identical `if (!logger) return schema.safeParse(data)` fast path, and `../fuze-api/src/client/resources/base-resource.ts:228` always passes `this.logger` (never conditionally omitted) — the precedent this plan explicitly directs porting (plan Phase 4 Step 1: "Port `fuze-api/src/validation/schema-leniency.ts`... verbatim"). Flagging for Phase 6: its `BaseResource` must always pass the client's logger into `parseLenient`, exactly as `fuze-api`'s does. |
| implementation-auditor-r1-f3 | Fixed | `DiagnosticsCollector.flush` (`src/validation/diagnostics.ts`) no longer takes a logger object plus an implicit `debug` call; it now takes a plain sink function `(message: string, meta?: Record<string, unknown>) => void` and calls it once per group. `parseLenient` calls it as `diagnostics.flush((message, meta) => logger.debug(message, meta), context)`. Phase 6's per-item drop path (R7, `warn`) can now reuse this class **unmodified** by calling `diagnostics.flush((message, meta) => logger.warn(message, meta), context)` — no level parameter or widened logger-interface type needed on this class at all, so there is no coupling for Phase 6 to work around. Added a test (`diagnostics.test.ts`, "supports a level-specific sink so callers can reuse flush at a different log level") demonstrating exactly this reuse pattern against a `{debug, warn}` logger, asserting `warn` (not `debug`) receives the call. This is a cleaner fix than the finding's suggested `level: 'debug' | 'warn'` parameter + widened `DiagnosticsLogger` type: it doesn't require every caller's logger type to statically declare both methods (this phase's own call site only ever needs `debug`), and `DiagnosticsCollector` no longer needs to know log-level vocabulary at all. Updated the module doc and `flush`'s JSDoc accordingly. |
| implementation-auditor-r1-f4 | Fixed | Added the recommended invariant comment at `toLenientField` (`src/validation/schema-leniency.ts`, immediately above the function, where the object-case leniency it powers is applied): states that blanket per-field `.nullable().optional()` relaxes a union branch's own discriminator requiredness, that this is sound only while `src/generated/schemas/**` declares no `z.union` (today verified true by grep), and explicitly flags that Phase 9's schema-completeness audit should assert this invariant (union-freedom) so a future spec refresh or Phase-6 override introducing a response union fails loudly instead of silently mismatching branches. Adding the actual Phase 9 assertion is out of Phase 4 scope (no Phase 9 test file exists yet); the comment is the load-bearing, in-code record the finding asked for at the point where the invariant matters. |
