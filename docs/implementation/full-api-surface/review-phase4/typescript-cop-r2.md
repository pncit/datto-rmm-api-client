## typescript-cop — round 2

Reconciled round 1's four findings against `reviser-r2.md`'s disposition and the current diff
(`src/validation/schema-leniency.ts`, `src/validation/diagnostics.ts`, and their tests). Re-read the
whole of both source files fresh rather than trusting the disposition prose, and additionally
compile-checked the new `Lenient<T>` type against the real generated types it targets
(`Device`, `Alert`, `AlertsPage`, including nested/array fields) via `tsc --noEmit` — clean, no
"excessively deep instantiation" or mismatch. Also re-walked `architect-r1-f1`/`f2` and
`engineer-r1-f1..f5`'s fixes (the `collectionKey`/`trackExamined` total-accumulation rework and the
`nodeChildren` extraction) since they touch the same code my round-1 findings covered, even though
those findings are not mine to carry forward.

## Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation |
|----|----------|--------|----------|----------|---------|----------------|
| typescript-cop-r1-f1 | High | Closed | TypeHole | — | — | ratified: `parseLenient` is now overloaded — passing a `logger` returns `z.ZodSafeParseResult<Lenient<T>>`, omitting it returns `z.ZodSafeParseResult<T>` — and the exported `Lenient<T>` mapped type (`schema-leniency.ts:683-687`) honestly adds `\| null` to every object field at every depth, matching `toLenientField`'s actual runtime reach (named fields only, not array elements, not the top-level value itself). Verified by direct `tsc --noEmit` compilation of `Lenient<Device>`, `Lenient<Alert>`, `Lenient<AlertsPage>` (including field-level projections) against the real generated types: clean, no errors. The one documented imprecision (record/index-signature values conservatively marked nullable even though `toLenientField` doesn't reach them) can only widen, never narrow, so it cannot reintroduce the original unsoundness. |
| typescript-cop-r1-f2 | Medium | Closed | Exhaustiveness | — | — | ratified: both `addCatchallRecursive`'s and `cleanAndDiagnoseResponse`'s `default` cases now `throw` a descriptive `Error` naming the unrecognized `def.type` and which function needs a new case, replacing the prior silent pass-through. The fix also caught and corrected a real pre-existing gap (`.transform()`/pipe-`in` nodes previously fell into the old silent default) by giving `transform` its own explicit terminal case. `enumFieldPaths`'s own `walk` default is unchanged (still silent) — that is a deliberate, narrower scope (informational schema-introspection helper, not on the runtime leniency path), not a re-opening of this finding. |
| typescript-cop-r1-f3 | Medium | Closed | TypeHole | — | — | ratified: `getDef`'s return type is now `ZodInternalDef = { readonly type: string } & Record<string, unknown>` instead of `any`; `def.type` is checked directly by the compiler at every switch site, and the redundant `def.type as string` casts are gone. Every other property access still needs its own cast (via the new shared `nodeChildren`/`objectShape` accessors), which is the finding's own accepted tradeoff — a renamed/missing zod-internal property now surfaces through a typed `unknown` path rather than a bare `any`. |
| typescript-cop-r1-f4 | Medium | Closed | TypeHole | — | — | ratified: `groupKey` (`diagnostics.ts:50-56`) now builds the dedup key via `JSON.stringify([message, field, value ?? null])` instead of naive space-joined concatenation, so no wire-derived `field`/`value` content (an arbitrary stripped record key, an arbitrary enum string) can produce a false collision. Covered by a dedicated test ("does not collide two distinct (message, field, value) triples whose text could concatenate identically"). |
| typescript-cop-r2-f1 | Low | Open | PublicTypes | `src/validation/schema-leniency.ts:683-687` (`Lenient<T>`) | The exported `Lenient<T>` type — the type-level contract Phase 6 callers will build on — has no committed regression test. The implementor's notes (§7, §12) describe verifying `Lenient<Device>`/`Lenient<Alert>`/`Lenient<AlertsPage>` compile via temporary, deleted `vitest` files; nothing in `tests/unit/validation/schema-leniency.test.ts` or elsewhere exercises `Lenient<T>` against a real generated type. A future generated-type shape change (e.g. a self-referential/recursive type, or a property whose type structurally confuses the `readonly (infer U)[]`/`extends object` branching) could silently break `Lenient<T>`'s instantiation for real callers with nothing in the suite to catch it before Phase 6 wires it in. | Add a small compile-time-only check pinning `Lenient<T>` against at least one real, representative generated type (object with nested object/array/enum fields, e.g. `Device` or the `getDeviceResolvedAlertsResponse`-shaped type) — either via `vitest`'s `expectTypeOf` (not currently used anywhere in the repo, but a reasonable first use) or the repo's existing `satisfies`-based closed-type pattern (`tests/generated/strict-fixture-types.ts`) adapted to assert on a couple of `Lenient<Device>`'s known-nullable field types. This turns the implementor's own already-performed, already-deleted verification into a permanent regression guard instead of tribal knowledge in a notes file. |

No other new findings: the `collectionKey`/`trackExamined` total-accumulation rework (architect-r1-f1)
and the `nodeChildren` extraction (engineer-r1-f3) introduce no new type holes — every non-null
assertion on a `nodeChildren` slot is guarded by the same `def.type` switch arm that guarantees the
slot is populated, and `DiagnosticsCollector`'s new `examined` map is typed and scoped consistently
with `groups`. `_zod.def` isolation still holds (no second call site outside `schema-leniency.ts`), and
no floating promises or async-correctness issues exist (the module remains fully synchronous).
