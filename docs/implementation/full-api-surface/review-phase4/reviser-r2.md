## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Replaced the `collectionSize`-threaded-number/`Math.max` scheme with a `collectionKey`-based one. `cleanAndDiagnoseResponse` (`src/validation/schema-leniency.ts`, renamed from `detectUnknownProperties` per engineer-r1-f2) now threads the *structural path* of the nearest enclosing array, not a snapshotted length; on entering an array it calls the new `DiagnosticsCollector.trackExamined(path, parsed.length)` (`src/validation/diagnostics.ts`), which *sums* into a per-key running total rather than overwriting. Since an array's own `path` is shared by every element (index-dropping is unchanged) and by every outer iteration that revisits a nested array, `trackExamined` correctly accumulates the true number of items examined at that structural position across the whole call — including two levels deep. `record()`/`DiagnosticGroup` now store the `collectionKey` and resolve `total` from the accumulated map lazily at `flush()`, once every iteration has completed. Added the exact regression case requested: "reports total against every responseActions array examined across the page, for a widening two arrays deep" (`schema-leniency.test.ts`), reproducing `getDeviceResolvedAlertsResponse`'s `{ pageDetails, alerts: [{ priority, responseActions: [{ actionType }] }] }` shape with 50 single-action + 50 dual-action alerts (150 response actions total, 3 widened), asserting `count: 3, total: 150` and `count <= total`. All prior single-array-level tests pass unchanged since a single array's key now sums to the same value the old nearest-array-length gave. |
| architect-r1-f2 | Fixed | Rewrote the `total`/`collectionKey` documentation in both files to describe the actual (now-correct) accumulation semantics instead of the false "Math.max, defensive, same size expected" claim: `diagnostics.ts`'s `trackExamined`/`record`/`flush` docs and `schema-leniency.ts`'s `cleanAndDiagnoseResponse` doc now state plainly that nested arrays are revisited once per outer element, that each visit's length is summed (not maxed) into one running total per structural key, and that this is what makes a doubly-nested widening's `total` correct. No "defensive" language remains — the mechanism is load-bearing and now documented as such. |
| engineer-r1-f1 | Fixed | Same change as architect-r1-f2 above (identical finding: the same JSDoc/`Math.max` inaccuracy in `diagnostics.ts`). The `Math.max`-based "defensive" total is gone entirely, replaced by the `collectionKey`/`trackExamined` sum-based resolution; `count` can no longer exceed `total` for a differing-length nested array, so the doc states the (now true) invariant rather than a caveat about it. Added `diagnostics.test.ts` coverage: "resolves total from the sum of every trackExamined call for the same collectionKey" (three visits of sizes 2/3/1 to the same key resolve to `total: 6`, not `max(2,3,1)=3`), plus a nested-array test in `schema-leniency.test.ts` (see architect-r1-f1) pinning the end-to-end behavior. |
| engineer-r1-f2 | Fixed | Renamed `detectUnknownProperties` to `cleanAndDiagnoseResponse` throughout `src/validation/schema-leniency.ts` (definition, all recursive call sites, and every doc-comment reference, including the module doc and `parseLenient`'s doc). The new name reflects all three responsibilities the finding identified: cleaning (stripping unknown keys), diagnosing (enum-widening/unknown-key detection), and returning the transformed response data `parseLenient` hands back. No external references existed (internal, unexported), so this was a zero-risk rename; all 190 tests still pass unchanged since none reference the function by name. |
| engineer-r1-f3 | Fixed | Extracted the duplicated `_zod.def` navigation into two shared helpers in `schema-leniency.ts`: `objectShape(schema)` (reads `.shape`, previously repeated as `(schema as any).shape`/`(node as any).shape`/`(option as any).shape` at 6 call sites) and `nodeChildren(def)` (a single switch mapping each node kind to its child schema(s)/payload via slot names -- `element`, `options`, `innerType`, `valueType`/`keyType`, `pipeIn`/`pipeOut`, `defaultValue`, `enumValues`). `addCatchallRecursive`, `cleanAndDiagnoseResponse`, and `enumFieldPaths`'s `walk` all now read a node's children exclusively through these two functions instead of each repeating `def.element`/`def.innerType`/`def.options`/`def.valueType`/`def.in`/`def.out`/`def.entries` inline -- a Zod-internal rename or a new node kind now needs updating in exactly one place. As a direct consequence, `enumFieldPaths`'s `walk` no longer contains any literal `any` (it only calls `objectShape`/`nodeChildren`, which are already isolated under one `eslint-disable`/`enable` pair), so its own separate `any`-disabling pragma was removed as dead weight -- a natural byproduct of the extraction, not scope creep. |
| engineer-r1-f4 | Fixed | Same fix as typescript-cop-r1-f4 below (identical finding: naive space-joined `groupKey`). Changed `groupKey` in `diagnostics.ts` to `JSON.stringify([message, field, value ?? null])`, so no component's own content (an arbitrary wire-derived stripped key or enum value) can produce a false collision with another triple regardless of delimiters it contains. Added `diagnostics.test.ts` coverage: "does not collide two distinct (message, field, value) triples whose text could concatenate identically" (`("event","a b","c")` vs `("event","a","b c")`), asserting 2 distinct groups. |
| engineer-r1-f5 | Fixed | Resolved the asymmetry by dropping the lone `visiting`/cycle guard from `enumFieldPaths`'s `walk` (it now matches `addCatchallRecursive`/`cleanAndDiagnoseResponse`, neither of which ever had one) and recording the "Datto's generated schemas are not recursive" invariant once, in `enumFieldPaths`'s own doc comment, rather than defending against it inconsistently in one of three call sites. The comment also notes why a guard there specifically wouldn't help: `addCatchallRecursive`'s cache populates only after a node's recursion returns, so a genuine cycle stack-overflows regardless of any guard. |
| project-lead-r1-f1 | Fixed | Added `tests/generated/schema-union-freedom.test.ts`: it recursively scans every `*.zod.ts` file under `src/generated/schemas/**` for `zod.union(`/`zod.discriminatedUnion(` and fails if any is found, turning the previously one-time, manually-run `grep` into a build-breaking regression guard against the exact committed schemas `toLenientField` runs against in production. Updated `toLenientField`'s invariant comment (`schema-leniency.ts`) to point at this test instead of solely deferring enforcement to Phase 9 (which, per the finding, never actually plans a union-freedom check) -- the invariant is now enforced immediately, in this phase's own suite, not by a future phase that doesn't currently schedule it. |
| typescript-cop-r1-f1 | Fixed | Added an exported `Lenient<T>` mapped/conditional type (`schema-leniency.ts`): for object types it maps every key to `Lenient<T[K]> | null` (homomorphic, so existing `?` optionality is preserved), for arrays it maps element-wise with no added `null` (array elements aren't independently nullable -- only named object fields are, matching `toLenientField`'s actual reach), and primitives pass through unchanged. `parseLenient` is now overloaded: passing a `logger` returns `z.ZodSafeParseResult<Lenient<T>>` (honest about the null leniency that path actually applies), while omitting it returns the untouched `z.ZodSafeParseResult<T>` (accurate, since the strict-`safeParse` fallback applies none of that leniency) -- this ties the type-level signal directly to the same `logger`-gates-behavior invariant already documented from round 1 (implementation-auditor-r1-f2), rather than blanket-widening `T` regardless of which code path actually ran. Verified `Lenient<Device>`, `Lenient<Alert>`, and `Lenient<AlertsPage>` (real generated types, including the enveloped list/nested-object cases) compile cleanly with no "excessively deep" instantiation error. One documented, deliberate imprecision: a `Record<string, X>` value's type is structurally indistinguishable from a named object field's once `T` erases to plain TypeScript, so `Lenient` conservatively marks record values nullable too even though `addCatchallRecursive`'s `record` case doesn't actually wrap them in `toLenientField` -- documented on `Lenient` itself as a type that can only ever widen (never narrow, unlike the bug this fixes), so it is never unsound. |
| typescript-cop-r1-f2 | Fixed | Both `addCatchallRecursive`'s and `cleanAndDiagnoseResponse`'s `default` cases now throw a descriptive `Error` naming the unrecognized `_zod.def.type` and which function needs a new case, instead of silently returning the node/data unchanged. Discovered and fixed a real regression this surfaced during self-testing: `.transform()` (the `in` side of a `.pipe()`, exercised by this module's own "pipe schema" test) is a legitimate, already-supported node kind that was previously falling into the old catch-all default -- added it as its own explicit terminal case (opaque pass-through, since only the pipe's `out` side is ever actually cleaned) rather than letting it hit the new throw. Ran the full suite (190 tests) plus `tsc --noEmit` and `npm run build` after this change; all pass. `enumFieldPaths`'s own `walk` default intentionally still returns silently (unchanged) -- its terminal cases were never exhaustively enumerated (it only cares about `enum` nodes) and it is an informational schema-introspection helper, not part of the runtime leniency path R5/R7 depend on, so throwing there would be scope creep beyond what this finding targeted. |
| typescript-cop-r1-f3 | Fixed | Changed `getDef`'s return type from `any` to `type ZodInternalDef = { readonly type: string } & Record<string, unknown>`. `def.type` is now checked directly by the compiler (every `switch (def.type)` site relies on this); every other property (`def.element`, `def.innerType`, etc.) still requires its own explicit `as` cast at the call site (now via the new shared `nodeChildren` accessor, see engineer-r1-f3), same as before -- but a renamed/missing property now surfaces as `undefined` through a typed `unknown`-based path instead of compiling silently through a bare `any`. Removed the now-redundant `def.type as string` casts throughout (the type is already `string`). Verified with `tsc --noEmit` and the full test suite. |
| typescript-cop-r1-f4 | Fixed | Same fix as engineer-r1-f4 above (identical finding, same location and recommendation). |

No findings were rejected or escalated this round; all eleven distinct findings (architect-r1-f2 and engineer-r1-f1 share one fix; engineer-r1-f4 and typescript-cop-r1-f4 share one fix) were resolved in code. Full verification after all changes: `tsc --noEmit` clean, `eslint src` clean, `npm run build` succeeds, and all 190 tests pass across 16 files, including the new doubly-nested-array test, the rewritten `diagnostics.test.ts` (updated for the `record()`/`trackExamined` API change), and the new `tests/generated/schema-union-freedom.test.ts`.
