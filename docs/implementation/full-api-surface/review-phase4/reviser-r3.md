## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| engineer-r2-f1 | Fixed | Added a `case "transform":` to `cleanAndDiagnoseResponse`'s switch (`src/validation/schema-leniency.ts`) that returns `parsed` unchanged, mirroring `addCatchallRecursive`'s existing `'transform'` terminal case — the node carries no shape to clean or diagnose. The gap was real: `cleanAndDiagnoseResponse`'s `pipe` case recurses into `pipeOut`, and for a bare `z.string().transform(fn)` (no trailing `.pipe(objectSchema)`) `pipeOut` is itself the `transform` node, so it was falling into the newly-added throwing `default` case and raising on a valid response. Added a regression test in `tests/unit/validation/schema-leniency.test.ts` ("does not throw when a bare .transform() schema is parsed directly") that parses `z.string().transform(s => JSON.parse(s))` through `parseLenient` with a logger and asserts it doesn't throw and returns the transformed value — this is exactly the scenario the finding's own root-cause analysis identified as untested by the existing "pipe schema" test (whose `pipeOut` is an object, never a transform). Did not factor a shared `isOpaqueNode` predicate across the two walkers as the recommendation's secondary suggestion proposed: the two switches' terminal sets are structurally identical today (verified by direct comparison) and the recognized-kind lists already live in exactly one place each via `nodeChildren`/`objectShape` (engineer-r1-f3); adding a third shared abstraction purely to guard against a *future* one-sided edit is speculative generality this phase's own "port, don't reinvent" discipline doesn't call for, and the new test now makes such a one-sided edit fail loudly regardless. |
| typescript-cop-r2-f1 | Fixed | Added `tests/generated/lenient-type-pin.ts`, a compile-time-only file (no runtime assertions, never imported by a `*.test.ts`) picked up directly by `tsconfig.test.json`'s `tests/**/*.ts` include glob, so `npm run typecheck` (part of this phase's Exit Gate) fails the build if `Lenient<T>`'s instantiation over real generated types ever breaks. It pins `Lenient<Device>`, `Lenient<Alert>`, and `Lenient<AlertsPage>` against a plain scalar field, a nested named-object field, a nested array-of-objects field, and a nested open-enum field (at two nesting depths), using a standard generic-function `Equal`/`Expect` type-testing idiom (no new dependency). While building this pin, the equality assertion against `Lenient<Device>['deviceClass']` surfaced a real, previously-uncaught unsoundness in `Lenient<T>` itself: because `T` is a naked type parameter, `Lenient<T>` distributes over every union member of a widened response-enum type (`EnumUnion | (string & {})`, the idiom `widen-response-enums.mjs` emits for every response enum in this codebase); TypeScript's `(string & {}) extends object` check is structurally `true` (the intersected `{}` makes it so, even though every value the branch describes is a plain string), so the old `object`-before-primitive branch ordering was mapping `Lenient` over `String.prototype`'s own members for that branch — corrupting the type of `deviceClass`, `alertPriority`, `antivirusStatus`, and every other enum field in the client. Fixed `Lenient<T>` (`src/validation/schema-leniency.ts`) by inserting a primitive-type branch (`string \| number \| boolean \| bigint \| symbol \| null \| undefined`) before the `object` branch, so every primitive union member — including a branded one — short-circuits correctly before the object branch can misfire; documented why the ordering is load-bearing (not incidental) directly on the type. This was necessary, not scope creep: the finding asked for a regression test on `Lenient<T>` against real generated types specifically because no such test existed to catch exactly this class of defect, and writing that test immediately surfaced a live one — leaving the test to assert the buggy (pre-fix) shape would have permanently encoded the bug as "correct" in the very regression guard meant to prevent it. Verified: `npm run typecheck` (all three tsc projects), `npm test` (191/191, up from 190), and `npm run build` all pass. |

No findings rejected or escalated this round; both Open findings (one Medium from engineer, one Low from typescript-cop) were resolved in code, and the typescript-cop fix incidentally corrected a real latent type-soundness defect in `Lenient<T>` that predates this round but had no test surfacing it until now. Architect and project-lead had no Open findings this round (both ratified all prior findings `Closed` with no new ones). Full verification after all changes: `npm run typecheck` clean (`typecheck:src` + `typecheck:test` + `typecheck:tools`), `npm run lint` clean (no new warnings beyond the pre-existing, out-of-scope ones in the untouched old `src/*.ts` surface), `npm run build` succeeds, and all 191 tests pass across 16 files.
