## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r2-f1 | Fixed | Reconciled `implementation-phase7-notes.md` with the shipped tree on the `site-update` axis. §1's "Explicitly Out-of-Scope" no longer claims `rate-limits.ts` is untouched and now lists it as a third necessary exception; §3's Files Touched table now includes `rate-limits.ts` (infra addition) and `write-bodies.ts` (export added), and the `site-resource.ts` row's method list now includes `update`; §5 gained a "Deviation 3" documenting that `SiteResource.update()` is implemented and `'site-update': 100` was added to `WRITE_LIMITS`, crediting `implementation-auditor-r1-f4`; §6's ambiguity bullet was rewritten from "is not implemented" to "is implemented", pointing at Deviation 3; §11's now-stale "has no `WriteOpKey`" risk bullet was removed (the risk is resolved); §13's Final Assertion no longer claims `rate-limits.ts` is untouched and instead credits the `'site-update'` addition alongside the other two documented necessities. No code changed — this was a documentation-only defect in the phase's own record; the code was already ratified Closed by this reviewer in round 2. |
