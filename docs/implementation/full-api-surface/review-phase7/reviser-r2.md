## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | `BaseResource`'s `coerceSchema` doc (`base-resource.ts`) no longer claims a resource method can pass `coerceSchema<Device>(deviceResponseSchema)` to `httpGet`/etc. to get back a clean `Device` — that claim was false (`httpGet` always returns `Lenient<T>` regardless of the schema's own generic). The doc now states `coerceSchema`'s real, current use (the inline cast `schema-overrides/types.ts` performs to bind `deviceSchema`/`alertSchema`) and explicitly says it narrows only a *schema's* declared type, not `BaseResource`'s `Lenient<T>` wrapper, cross-referencing `narrow<T>` (`./narrow.ts`) as the separate, actual per-resource-method mechanism for that. Updated `validateResponse`'s doc (same file) and `narrow.ts`'s own doc to match — both now describe `coerceSchema` (schema-level) and `narrow` (value-level) as two distinct, non-competing helpers for two different jobs, rather than one doc-blessing `coerceSchema` for a job it can't do. |
| architect-r1-f2 | Fixed | Added `@internal` (with a one-line "why exported" note pointing at `schema-mirror-pin.ts`, and a note that Phase 8's `src/index.ts` barrel must export only the `*Resource` classes/`DattoRmmClient`, never `export *` from these files) to the five hand-mirrored item schemas' doc comments: `componentSchema`/`dnetSiteMappingSchema` (`account-resource.ts`), `deviceNetworkInterfaceSchema`/`filterSchema` (`site-resource.ts`), `jobComponentSchema` (`job-resource.ts`). Signals intent at the one place a Phase 8 implementor building the barrel will read first. |
| engineer-r1-f1 | Fixed | Rewrote `device-resource.test.ts`'s "R20 end-to-end" test. Confirmed by direct inspection that `setUdf()`'s response validates against `voidResponseSchema` (`z.unknown()`), whose `cleanAndDiagnoseResponse` default case never calls `diagnostics.record`, so `DiagnosticsCollector.flush` never fires and `setUdf()` alone can never organically drive a log call, masked or not — the old test's `for` loop over sink calls was checking an always-empty list. The rewritten test instead mocks a `get()` response with a genuinely unrecognized top-level property, which organically triggers a real `"stripped unknown response property"` `debug` call through the actual `BaseResource → parseLenient` path, and asserts `sink.debug` was actually called with it — proving the constructor-injected `maskedLogger`, not some other unwrapped logger, is what `BaseResource` truly logs through. Since that diagnostic deliberately omits the stripped value (documented in `objectCatchall`'s own doc, so it can't demonstrate masking a raw value), the test then confirms that same `maskedLogger` reference still redacts a directly-logged raw UDF value, and keeps the original "no raw secret ever appears in any sink call" assertion. The test can now fail (e.g. if `BaseResource` silently used a different logger instance, or if `withUdfMasking` stopped redacting). |
| engineer-r1-f2 | Fixed | Extracted `BASE_URL`, `createMockLogger`, `createTrackedAxios`, and a generic `makeResource(Ctor, logger?)` into `tests/unit/client/resources/test-harness.ts`, and updated all five Phase 7 resource test files (`account`/`site`/`device`/`alert`/`job`-resource.test.ts) to import it, each keeping only a one-line, resource-specific `makeResource()` wrapper. Scoped the fix to the five Phase 7 files the finding's `Location` names; left the Phase 6 `base-resource.test.ts`/`paginate.test.ts` (mentioned only parenthetically as pre-existing, out-of-phase duplication) untouched, per Scope Lock. |
| engineer-r1-f3 | Fixed | Split `datto-rmm-client.test.ts`'s misnamed test into two: the original is renamed to drop the unproven rate-limit clause ("... fetches an auth token and attaches Authorization through the real transport stack"), and a new test ("... a read call is throttled by the real rate limiter through the actual transport stack once the configured read window is exhausted") configures `rateLimit: { readLimit: 1, windowSeconds: 1 }`, issues two real `client.devices.get()` calls, and (using `vi.useFakeTimers()`, mirroring `tests/unit/rate-limit/rate-limiter.test.ts`'s own convention) asserts the second call is not yet resolved before the window rolls and resolves only after `vi.advanceTimersByTimeAsync(1_000)` — genuinely proving a read passes through `MultiWindowRateLimiter` end-to-end via the client, not just asserting it by name. |
| engineer-r1-f4 | Fixed | Same change as `typescript-cop-r1-f2` below (the two findings name the same gap in `schema-mirror-pin.ts`) — see that row. |
| typescript-cop-r1-f1 | Fixed | `objectCatchall` (`schema-leniency.ts`) now reads `getDef(schema).catchall` instead of casting `(schema as any)._zod.def.catchall` directly, restoring the single typed `_zod.def` accessor this module's own header doc requires ("All Zod v4 internal access is isolated here"). No behavior change — `getDef` returns the same `_zod.def` object; verified via `npm run typecheck`/`npm test` (all 379 tests, including the `objectCatchall`-dependent write-body/leniency tests, still pass). |
| typescript-cop-r1-f2 | Fixed | Verified against the generated types (`Component`, `DnetSiteMappingsDto`, `DeviceNetworkInterface`, `JobComponent`, `Variable`, and every type they nest — `ComponentVariable`, `NetworkInterface`, `DevicesType`, `JobComponentVariable`) that none of these five carries an enum field, so none is subject to the Phase-2 enum-widening asymmetry that justifies key-set-only equality. Changed `schema-mirror-pin.ts`'s pins for those five from `Equal<keyof T, keyof z.infer<typeof schema>>` to full `Equal<T, z.infer<typeof schema>>`, which additionally fails on a same-named field's *type* changing (e.g. `Component.id` `number`→`string`), not just a field being added/removed — matching the codebase's own established full-`Equal` idiom (`lenient-type-pin.ts`). Left `Filter`/`filterSchema` alone on the weaker key-set-only comparison, scoped explicitly (in both the file's doc and an inline comment) to the one documented `Filter["type"]` open/closed-enum asymmetry that full equality would otherwise fail on today. `npm run typecheck` passes with all six pins active at their new strength, confirming the five schemas are exact matches today. |

Project Lead's round-1 turn recorded no findings, so no row for it appears above.
