## reviser — round 5

Dispositions for the Phase 2 reviewer turns (architect-r1 f1–f7, engineer-r1 f1–f4). `Fix` for
genuine in-scope gaps (tightening, not padding); `Accept` for the reviewers' two explicit `Defer:`
findings (recorded as deferred, not folded into the design body).

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r1-f1 | Fix | Genuine un-plannable gap. Named the opKey→limit source of truth: a **committed static table** (`src/rate-limits.ts`) seeded from the observed `system/request_rate` contract so the limiter has limits before the first request, plus the **100** fallback for a write opKey with no entry; clarified `requestRate()` is not read at init. |
| architect-r1-f2 | Fix | A security invariant must be structural. Rewrote UDF masking to enforce at the **single logger boundary** — a masking decorator wrapping the injected `DattoLogger` through which all client log calls flow — so no current/future call site can leak an unmasked `udf*` value. Removed the "invoked wherever … could be logged" call-site-discipline framing. |
| architect-r1-f3 | Fix | Worst-rated risk (High/High) deserves a mechanical, not procedural, control. Replaced "a documented sanitization step" with a **committed sanitization script + pre-commit/CI scan** over `spec/` and fixture paths that fails the build on an unsanitized `udf*`/credential-shaped value. |
| architect-r1-f4 | Fix | In-scope brevity tightening (overlaps engineer-r1-f1). Collapsed the triplicated **pageDetails cursor override** to one canonical statement in R3 (schema-override + BaseResource Key Concepts now reference it) and trimmed the enum-widening/codemod restatement in Decision 2 to a pointer at the leniency Key Concept. No behavior change. |
| architect-r1-f5 | Fix | Breaking `1.0.0` surface can't be re-cut without a `2.0.0`; the split must be principled. Reconciled R1 and the Public-surface list to one **pluralization rule** (plural for collection namespaces; singular for the singletons `account`/`system` and for `audit`, a group of audit-*fetch* ops, not a collection), stated at the Public surface so `audit` is no longer the unexplained lone singular. |
| architect-r1-f6 | Accept | Reviewer-flagged `Defer:` and out of scope for a single-client rebuild — extracting shared primitives into a common package touches both clients. Not folded in; recorded as a **conscious, temporary copy** with a consolidation follow-up under Deferred Decisions, exactly as the reviewer recommends. |
| architect-r1-f7 | Accept | Reviewer-flagged `Defer:`; eager full-buffer pagination matches 0.1.x and R3's stated intent, and a streaming variant is additive (no break). Not folded in; recorded as a revisitable tradeoff under Deferred Decisions. |
| engineer-r1-f1 | Fix | In-scope brevity/single-source-of-truth gap (overlaps architect-r1-f4, adds the reproducibility-gate thread). Cursor override → canonical in R3; enum-widening/codemod → canonical in the leniency Key Concept (trimmed ~30 lines to two tight paragraphs); reproducibility gate → canonical in R15 (spec-patch Key Concept and Migration step 2 now reference it). Success Criteria's one-line verifiable restatement kept, as that is a criterion's purpose. |
| engineer-r1-f2 | Fix | Real observability defect at the design's stated scale (848 devices / 1500 alerts). Added a **Leniency diagnostics volume & levels** Key Concept: aggregate/dedupe per page/call rather than per item, and level split — benign strip/widen at `debug`, per-item drop (real data loss) at `warn`. Also removes the per-row masker hot-path concern. |
| engineer-r1-f3 | Fix | Load-bearing R5 guarantee (build-time widened type ⇄ runtime `parseLenient` widening) had no verification while every other R5/R7 guarantee did. Added a Success Criterion: a fixture with an unobserved enum value both type-checks against the widened type and survives `parseLenient` without drop. |
| engineer-r1-f4 | Fix | In-scope DX gap on the frozen surface; reviewer prefers documenting over an alias, matching R18. Committed R18 (and the Success Criteria README line) to a **namespace→endpoint map** making the `account.devices()` vs `devices.get(uid)` split explicit. No new alias added. |
