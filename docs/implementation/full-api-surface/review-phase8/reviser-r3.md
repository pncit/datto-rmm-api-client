## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Removed the `it.skipIf(!specIsCommitted)` guard on `coverage-map.test.ts`'s spec-vs-map set-equality assertion; it now runs unconditionally and its first line is `expect(specIsCommitted, ...).toBe(true)` with a message naming R15 and the resolved `SPEC_PATH`, so a missing committed spec fails loudly instead of silently vanishing. Updated the doc comment above it to state the rationale (R15 guarantees the spec is committed, unlike `reproducibility.test.ts`'s genuine live-egress `skipIf`). The unconditional `it.each` per-operation drive (already independent of `specIsCommitted`) is unchanged. Re-verified: `npm run typecheck`/`lint`/`test` (417/417)/`build` all green. |
| architect-r1-f2 | Escalate | Requirements Gap: `design.md:452-454`'s "Public surface" section still states `src/index.ts` exports "the generated types," contradicting the delivered curated `public-types.ts` (no wildcard re-export, per the approved `plan.md:543-544`). The fix is a `design.md` prose edit — a plan/design artifact outside the reviser's remit, the identical class of discrepancy already routed through the mediator/human in this same directory for `implementation-auditor-r1-f2`/`r1-f4`/`r2-f1` (all resolved by human ruling and a planner-side edit, never by the reviser editing `plan.md`/`design.md` directly). No human ruling was supplied for this specific finding this round. Recommended fix (matches the architect's own): reword `design.md:452-454` to state `src/index.ts` exports a curated subset of entity/response types re-exported by name from `public-types.ts`, never a wildcard, cross-referencing `plan.md:543-544`. Does not block Phase 8's own correctness — the delivered barrel already matches the plan-mandated curated surface, independently verified by `surface-pin.ts`'s compile-time pins and `surface.test.ts`; blocks only the design record's accuracy. |
| engineer-r1-f1 | Fixed | Adopted option (a): renamed every `AuditResource` public method to drop the redundant `Audit`/`Device` noun the `client.audit` namespace already supplies — `getPrinterAudit`→`getPrinter`, `getEsxiHostAudit`→`getEsxiHost`, `getDeviceAudit`→`getDevice`, `getDeviceAuditSoftware`→`getDeviceSoftware`, `getDeviceAuditByMacAddress`→`getDeviceByMacAddress` (`src/client/resources/audit-resource.ts`), matching `client.jobs.getComponents()`/`getResults()` and the `FilterResource.defaults()`/`custom()` convention this same phase established; added a class-doc sentence recording the naming rule so the pattern is documented, not implicit. Updated every call site to match: the five `audit` rows in `src/client/operation-map.ts`, all method calls in `tests/unit/client/resources/audit-resource.test.ts`, the prose reference in `tests/generated/surface-pin.ts`'s comment, `src/client/resources/base-resource.ts`'s `httpGetArray` doc example (now `audit.getDeviceByMacAddress`), and `implementation-phase8-notes.md`'s Files-Touched/Tests sections. Left every generated schema/type name untouched (`getPrinterAuditResponse`, `getDeviceAuditByMacAddressResponseItem`, `GetDeviceAuditSoftwareParams`, etc.) — those are Orval-derived from the spec's own `operationId` and are independent of the resource's own method name, exactly as the finding's recommendation intends. Re-verified `npm run typecheck`/`lint`/`test` (417/417)/`build` all green after the rename. |
| project-lead-r1-f1 | Escalate | Requirements Gap: `plan.md:531` states the package no longer exports the "three" 0.1.x methods, and `design.md`'s Breaking Changes list (lines 476-478) names only `getAccountDevices`/`getDeviceByUid`/`updateDeviceUdfs` — both omit the retired fourth method, `invalidateToken`, which `surface.test.ts` already correctly asserts is absent. Closing this needs two things outside a code fix: (1) a `plan.md`/`design.md` prose correction, the same outside-remit class already routed to the human for `r1-f2`/`r1-f4`/`r2-f1` in this directory; and (2) an authoring decision only the human/planner can make — whether dropping `invalidateToken` with no public replacement (proactive invalidation is now reachable only via the internal `AuthManager.invalidate()`, wired solely to the automatic 401 handler) is a deliberate design choice or a capability gap Phase 10's README migration guide (R18) must flag. No human ruling was supplied for this finding this round. Recommended fix (matches the project-lead's own): correct `plan.md:531` to "four," add `invalidateToken` to `design.md`'s Breaking Changes list, and record the intentional/unintentional determination there. Does not block Phase 8's own code correctness — independently verified that no public method exists to invoke proactive invalidation, matching `surface.test.ts`'s assertion; blocks only the requirements record's completeness ahead of Phase 10's migration-guide drafting. |
| project-lead-r1-f2 | Fixed | Added the three missing files to `implementation-phase8-notes.md` §3 "Files Touched": `src/rate-limit/rate-limits.ts` (dropped the dead `filter-create`/`filter-delete` `WriteOpKey` entries), `src/client/resources/base-resource.ts` (doc-only reword of `httpDelete`'s example off the removed opKey), and `tests/unit/client/base-resource.test.ts` (retargeted the generic `httpDelete` tagging test from the placeholder `"filter-delete"` opKey to the real `"site-variable-set"`/`SiteResource.deleteVariable` call site) — each row carries the same rationale already recorded in this directory's `implementation-auditor-r1-f3` disposition, so the phase record now matches the delivered diff. |
