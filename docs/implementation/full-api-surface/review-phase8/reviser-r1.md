## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f1 | Fixed | Relocated (not deleted, to keep the change reversible/reviewable) `src/docs/implementation/resilient-device-validation/review-plan/architect-r5.md` to `docs/implementation/resilient-device-validation/review-plan/architect-r5.md` via `git mv`, then removed the now-empty `src/docs/**` directory tree. That completed plan's own archive (`docs/implementation/resilient-device-validation/`) already retains only its `design.md`/`plan.md`/consolidated process-artifact files with no `review-plan/` subtree, so this restores the file to a documentation location consistent with that convention. Re-verified: `src/` now contains no `docs/` subtree, and `git grep -n "Result<" -- src/` no longer matches this file (only the legitimate `z.ZodSafeParseResult<...>` occurrences remain — see f2). |
| implementation-auditor-r1-f2 | Escalate | Requirements Gap: the plan's own Phase 8 exit-gate commands (`! git grep -qn "Result<" -- src/`, `! git grep -qn "validationMode" -- src/`, `plan.md:580-581`) are literal substring greps that cannot pass even against correct code — confirmed directly after fixing f1: `git grep -n "Result<" -- src/` still matches only `z.ZodSafeParseResult<...>` in `src/validation/schema-leniency.ts` (a third-party zod type name, unrelated to the retired `Result<T>` contract), and `git grep -n "validationMode" -- src/` still matches one Phase 3 doc comment in `src/client/datto-client-config.ts:34` that documents the schema *rejecting* that field. A scoped recheck (`git grep -n "Result<" -- src/ \| grep -v ZodSafeParseResult` → empty) confirms the R9/R19 intent is genuinely met. Deciding how to reword the plan's own gate commands (or ratify these two matches as documented false positives in the phase record) is not the implementor's call — it's a defect in a plan artifact the reviser doesn't own, not in the code being revised. No human ruling was provided for this round; blocks only the *literal* gate text passing, not R9/R19's actual substance, which is independently verified. |
| implementation-auditor-r1-f3 | Fixed | Removed the two dead `filter-create`/`filter-delete` entries from `WRITE_LIMITS` in `src/rate-limit/rate-limits.ts` (the closed `WriteOpKey` union now types exactly the keys resource code can pass, matching the file's own stated invariant: "every concrete write opKey a resource method actually calls"). Updated `src/client/resources/filter-resource.ts`'s class doc to state plainly that no `WriteOpKey` entries exist for this namespace (dropped the now-stale reference to the removed table entries). Updated `src/client/resources/base-resource.ts`'s `httpDelete` doc, which named `filter-delete` as its example, to cite the two real delete call sites (`SiteResource.deleteVariable`/`deleteProxy`) instead. Updated the one test that used `"filter-delete"` purely as a placeholder opKey (`tests/unit/client/base-resource.test.ts`, generic `httpDelete` tagging test) to use the real `"site-variable-set"` opKey against a real delete path (`SiteResource.deleteVariable`'s), so no test still exercises a nonexistent key. `plan.md:355`'s Phase 5 Step 1 text does list `'filter-create'`/`'filter-delete'` in its illustrative "complete key set" — but that list, like the plan's "75 operations" figure (f4), predates this phase's direct, authoritative enumeration of the committed `spec/openapi.json`, which the plan's own Phase 8 Step 8 instructs deriving the operation inventory from. Three prior review rounds (Phase 6 implementation-auditor-r2/r3) already flagged these as dead and deferred a decision citing file ownership; Phase 8 is the phase that both owns `FilterResource` and mechanically proves (coverage-map test, 57/57) no such operations exist, making this the correct point to close it per this finding's own recommended low-risk mechanism (closed-union typecheck stays honest). `npm run lint`/`typecheck`/`test` (417/417)/`build` all re-verified green after the change. |
| implementation-auditor-r1-f4 | Escalate | Requirements Gap / Research Gap: independently re-verified the committed `spec/openapi.json` has exactly 53 paths / 57 operations (enumerated every `paths[path][method]` programmatically), confirming the notes' §6 finding and the auditor's own count. The plan's Phase 8 Goal (`plan.md:531`) and the design's Problem Statement/R1 state "53 paths / 75 operations." Resolving this needs a fact I cannot establish myself: whether the committed `spec/openapi.json` (fetched once in Phase 2, per the plan's own "Deferred Validation" section, which notes a live re-fetch "cannot run unattended in CI" and requires egress to `*.centrastage.net`) is the complete v2 surface or a partial/truncated fetch relative to Datto's actual API. I have no access to re-fetch the live spec to check. If the committed spec is confirmed complete, the fix is a Requirements-Gap follow-up (correct "75" to "57" in `design.md`/`plan.md` prose — files outside this reviser's remit to edit unilaterally); if it's confirmed truncated, the fix is materially larger (re-run `npm run generate` against a corrected spec, which could add operations Phase 8 hasn't implemented). Either way this needs the human/planner, not a code edit — the code's own guarantee (every operation in the *committed* spec is reachable, mechanically proven by the 57/57 coverage-map test) already holds regardless of the answer, so this does not block Phase 8's own correctness, only the trustworthiness of the "75" figure in prose. |
