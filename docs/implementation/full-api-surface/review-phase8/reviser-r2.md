## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| implementation-auditor-r1-f2 | Fixed | Human ruling: "finding valid; gates have been corrected." Verified against the current tree: `plan.md:580` now reads `! git grep -n "Result<" -- src/ \| grep -v "ZodSafeParseResult"` and `plan.md:581` now reads `! git grep -n "validationMode" -- src/ \| grep -vE '^[^:]+:[0-9]+:[[:space:]]*(\*\|//\|/\*)'` — both word/value-scoped exclusions that drop the legitimate `z.ZodSafeParseResult` occurrences and the Phase-3 doc comment, per the mediator's recommendation. Re-ran both commands against `src/`: each returns no matching lines, so `!` (negation) succeeds and the gate now passes on this correct tree. No further code change needed. |
| implementation-auditor-r1-f4 | Fixed | Human ruling: "finding valid, design and plan have been updated to match spec." Verified: `design.md` (lines 9, 23, 74, 417, 497) and `plan.md` (lines 22, 531) all now state "53 paths / 57 operations"; `grep -n "75 operations" design.md plan.md` returns no matches. This matches the programmatically-counted committed `spec/openapi.json` (53 paths / 57 operations) and the client's mechanically-guarded 57/57 coverage. No further code change needed. |
| implementation-auditor-r2-f1 | Escalate | Requirements Gap: `plan.md:355`'s "complete key set" for `WRITE_LIMITS` still lists `'filter-create': 100` and `'filter-delete': 100`, and `plan.md:569` still uses `filter-delete` as the canonical bodiless-`DELETE` example — both now contradicted by the code, which (per r1-f3, closed last round) proves via the closed `WriteOpKey` union and the 57/57 coverage-map test that no such operations exist. This is the same class of plan-artifact discrepancy as r1-f2/r1-f4: the authoritative text lives in `plan.md`, which is outside this reviser's remit to edit unilaterally, and no human ruling for this specific finding (`implementation-auditor-r2-f1`) was provided this round (only r1-f2 and r1-f4 were ruled). Recommended fix, unchanged from the auditor: drop `'filter-create'`/`'filter-delete'` from the `plan.md:355` key set, and replace the `plan.md:569` bodiless-`DELETE` example with a real delete call site (e.g. `SiteResource.deleteVariable`, opKey `site-variable-set`) so the plan matches the delivered closed union. Does not block Phase 8's own correctness (the code-side guarantee is independently proven); blocks only the plan record's accuracy. |
