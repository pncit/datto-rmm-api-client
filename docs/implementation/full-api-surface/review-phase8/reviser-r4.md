## reviser — round 4

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f2 | Fixed | Applied the human ruling verbatim. Reworded `design.md:452-454` ("Public surface" section) so it no longer says `src/index.ts` exports "the generated types"; it now states `src/index.ts` exports "a **curated subset** of entity/response types re-exported **by name** from" the hand-maintained `src/public-types.ts`, "never a wildcard re-export of the generated types," cross-referencing `plan.md:543-544`. No code change — this was a design-record accuracy fix only, as the ruling scoped it; `src/index.ts`'s actual `export * from './public-types'` (curated) was already correct and unchanged. |
| architect-r2-f1 | Fixed | Tightened `tests/generated/schema-mirror-pin.ts`'s `Filter`/`ActivityLog` pins. Added `type _Filter = Expect<Equal<Omit<Filter, "type">, Omit<z.infer<typeof filterSchema>, "type">>>` and `type _ActivityLog = Expect<Equal<Omit<ActivityLog, "entity">, Omit<z.infer<typeof activityLogSchema>, "entity">>>` alongside the existing `_FilterKeys`/`_ActivityLogKeys` key-set pins (kept, so a change to the enum field's presence is still caught). The new structural pins now inspect everything the key-set-only comparison skipped for `ActivityLog` — the two nested object fields (`site: { id, name }`, `user: { id, userName, firstName, lastName }`) and the scalar types (`date`, `deviceId`, `hasStdOut`, `hasStdErr`) — and the equivalent for `Filter`'s remaining scalar fields, for consistency. Updated the file's doc comment (lines 19-35) to describe the two-pin split (`keyof` for the one enum field + `Omit`-based full structural equality for the rest) instead of claiming whole-schema key-set-only. Re-ran `npm run typecheck` (all three projects) — clean, confirming the tighter pins compile against the current mirror schemas without any latent drift. |
| project-lead-r1-f1 | Fixed | Applied the human ruling verbatim. Corrected `plan.md:531` from "the three 0.1.x methods" to "the four 0.1.x methods (`getAccountDevices`, `getDeviceByUid`, `updateDeviceUdfs`, `invalidateToken`)". Added a new Breaking Changes bullet to `design.md` (after the existing `getAccountDevices`/`getDeviceByUid`/`updateDeviceUdfs` bullet) naming `invalidateToken` as removed with no public replacement, and recording the intentional/unintentional determination: based on `git show`-ing the pre-Phase-8 `README.md`/`src/client.ts` (`invalidateToken()` was a genuine documented capability — "drops the cached access token, forcing a refresh on the next request") and confirming no Decision in `design.md` retires it on purpose, this is an **unintentional capability gap**, not a deliberate design choice — the automatic 401 `onUnauthorized` handling (`AuthManager.invalidate()`, wired in Phase 5/7) it was folded into serves a different case (reacting to an already-expired token) than proactive invalidation after rotating `apiSecret` mid-process. Flagged explicitly for Phase 10's README migration guide (R18) to document as a dropped capability with no replacement. Does not change any runtime code: `surface.test.ts:99`'s `expect(client.invalidateToken).toBeUndefined()` already correctly asserts the retirement; this was a requirements-record completeness fix only, per the ruling's scope. |
