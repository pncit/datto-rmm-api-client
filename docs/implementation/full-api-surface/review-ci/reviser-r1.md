## reviser — round 1

| ID | Disposition | Rationale |
|----|-------------|-----------|
| ci-runner-r1-f1 | Fixed | The `validate` job's "Install dependencies" step failed with `npm error code EUSAGE` / `npm ci can only install packages when your package.json and package-lock.json ... are in sync` (Missing: `@emnapi/core@1.11.2`, `@emnapi/runtime@1.11.2`). Reproduced locally: `npm ci` with the CI's exact npm (`npx npm@11.16.0`, matching the `npm: 11.16.0` reported by the failing run's Setup Node step) fails the same way against the committed `package-lock.json`, while the same command succeeds with the older npm bundled in my local environment (`11.6.2`) — the two npm versions resolve the optional-peer dependency tree for vitest's transitive esbuild/rolldown packages differently, and the committed lockfile matched only the older resolution. Regenerated `package-lock.json` with `npx npm@11.16.0 install` (the CI runner's npm) so the lockfile matches what `npm ci` on that npm version produces: adds the missing `@emnapi/core@1.11.2`/`@emnapi/runtime@1.11.2` entries, drops several stale `"peer": true` markers, and dedupes the `vitest/node_modules/@esbuild/*` platform-binary duplicates that were left over from the prior resolution. No `package.json` dependency changed — this is a lockfile-only fix. Verified with `npm@11.16.0`: `npm ci`, `npm run lint`, `npm run typecheck`, `npm test` (537/537 passing), and `npm run test:repro` all succeed against the regenerated lockfile, matching every step the `validate` job runs. |
