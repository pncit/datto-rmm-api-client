## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | `scrub` (`src/logging/mask.ts`) now only recurses into arrays and plain objects. Added `isPlainObject`, which checks `Object.getPrototypeOf(value) === Object.prototype \|\| === null`; any other object (`Date`, `Error`, `Map`, a class instance) is returned unchanged instead of being rebuilt from `Object.entries`. Added a regression test (`tests/unit/logging/mask.test.ts`, "passes a Date and an Error under a non-UDF key through intact") asserting both the same reference and `Error#message` survive `withUdfMasking`. |
| architect-r1-f2 | Fixed | Changed `src/client/datto-client-config.ts`'s two imports from the `@/` alias to relative paths (`../defaults`, `../logging/logger`), matching every other Phase-3 file's convention. This was the only `@/`-aliased import in `src/`. |
| architect-r1-f3 | Fixed | `withUdfMasking`'s `wrap` in `src/logging/mask.ts` no longer captures a bare `logger.debug`/etc. reference. It now closes over the method name and calls `logger[method](message, ...)`, so the call is always made through the `logger` receiver and `this` resolves to `logger` for any prototype-method-based implementation. Added a regression test (`tests/unit/logging/mask.test.ts`, "preserves `this` for a logger whose methods are prototype methods bound at call time") using a class-based logger whose methods read `this.prefix`/push onto `this.calls`. |
| engineer-r1-f1 | Fixed | Same underlying defect as architect-r1-f1 (over-recursion in `scrub`), independently raised by both reviewers — see architect-r1-f1's rationale; the same edit and the same new test cover it. |
| engineer-r1-f2 | Fixed | Extracted `firstNonEmptyString(record, keys)` in `src/errors/datto-api-error.ts` and had both `extractErrorMessage` (over `ERROR_MESSAGE_KEYS`) and `extractRequestId` (over `REQUEST_ID_HEADERS`) call it instead of each inlining its own scan loop. |
| engineer-r1-f3 | Fixed | `extractErrorMessage` (`src/errors/datto-api-error.ts`) now falls back to `fallbackMessage` for `responseData == null` (covers both `undefined` and `null`, fixing the `null` → literal `"null"` message) and for an empty or whitespace-only string body (fixing the `""` → empty-message case), instead of only handling `undefined`. Added three regression tests in `tests/unit/errors/datto-api-error.test.ts` covering `null`, `""`, and `"   "` response bodies, each asserting the axios fallback message (`"Request failed with status code 500"`) is preserved. |
| typescript-cop-r1-f1 | Fixed | Added `scrubMeta(meta: Record<string, unknown>): Record<string, unknown>` in `src/logging/mask.ts`, implementing the object-branch logic directly with a real `Record -> Record` return type. `wrap` now calls `scrubMeta(meta)` with no cast at the crossing boundary; `scrub` itself stays `unknown -> unknown` and delegates to `scrubMeta` for its own plain-object branch (one internal cast there, but that's `scrub`'s own recursive-unknown handling, not the typed boundary `wrap` crosses). |

No findings from project-lead-r1 (none raised this round).

All four Phase-3 files under review (`src/logging/mask.ts`, `src/errors/datto-api-error.ts`, `src/client/datto-client-config.ts`) plus their tests were updated. Full suite (119 tests), `tsc --noEmit` (both `tsconfig.json` and `tsconfig.test.json`), `eslint src` (no new warnings — the 11 pre-existing warnings are all in untouched pre-Phase-3 files), `prettier --check` on touched files, and `tsup` build all pass clean.
