## reviser — round 4

Scope: the two `Open` findings across this round's Architect/Engineer/Project Lead/TypeScript Cop
turns (`architect-r2.md`, `engineer-r2.md`, `project-lead-r2.md`, `typescript-cop-r2.md`) —
`project-lead-r2-f1` and `typescript-cop-r2-f1`. Architect and Engineer raised no new findings this
round (both re-verified round-1 fixes and ratified them `Closed`, no round-2 findings).

| ID | Disposition | Rationale |
|----|-------------|-----------|
| project-lead-r2-f1 | Fixed | `implementation-phase6-notes.md` §3 and §7 both stated `tests/unit/client/base-resource.test.ts` has "22 tests"; direct enumeration of the committed file shows 25 `it(...)` blocks. Corrected both occurrences to "25 tests". Re-verified the file's aggregate figures: the per-file sum (`base-resource` 25 + `paginate` 12 + `device-overrides` 10 + `alert-overrides` 5 + `pagination` 6 + `write-bodies` 22 = 80) already matched §12's "80 new in this phase" and `npm test`'s actual "323 tests passing" total, so no other total in the file needed re-deriving — only the two per-file mentions were stale. |
| typescript-cop-r2-f1 | Fixed | Rewrote `deviceSchema`/`alertSchema`'s doc comment (`src/schema-overrides/types.ts:55-71`) to state what they actually provide against `httpGet`'s shipped `Promise<Lenient<TResponse>>` signature: `this.httpGet(path, deviceSchema, ctx)` resolves to `Promise<Lenient<Device>>` (the reconciled, open-enum-widened shape, honestly `null`-admitting per field), not `Promise<Device>` "for free" as the stale comment claimed — a caller declaring the clean `Promise<Device>` still re-asserts that explicitly at its own return site, the same `Lenient<T>`-to-`T` narrowing `validateResponse`'s own doc already documents for every other `http*` primitive. `deviceSchema`/`alertSchema`'s actual value is narrower but still real: they remove the closed-enum hazard from that assertion (an un-coerced `deviceResponseSchema` would re-narrow to closed enums even after the cast), they just no longer make the assertion itself unnecessary. Corrected the same claim in `implementation-phase6-notes.md` §4 (the `write-bodies.ts` summary paragraph) and §6 Decision 3, both of which echoed the "gets the type for free" framing. Did not add a wrapper method (the finding's option (b), e.g. `httpGetDevice`) — that would hard-code `BaseResource` (a generic primitive class serving every future resource) to two specific entities, which the plan and design never call for and no Phase 7/8 resource class exists yet to need; option (a) — an honest doc comment matching the already-established re-assertion pattern from Decision 2 — resolves the contradiction without that coupling. No test added for `deviceSchema`/`alertSchema` through `httpGet`: the finding's underlying defect was a doc-only claim (verified by `grep` finding no code path that relied on the false "for free" behavior — no Phase 7/8 resource classes exist), and `Lenient<T>`'s own behavior is already pinned by Phase 4's `tests/generated/lenient-type-pin.ts`; a redundant pin of `Lenient<Device>` specifically would test the same generic mechanism a second time under a different type parameter. Re-verified: `npm run lint` (0 errors, 11 pre-existing warnings, unchanged), `npm run typecheck`, `npm test` (323 tests passing, unchanged — no test assertions touched), and `npm run build` all green. |
