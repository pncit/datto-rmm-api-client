## reviser — round 3

Scope: responses to Step B's four first-round reviewer turns (`architect-r1.md`, `engineer-r1.md`,
`project-lead-r1.md`, `typescript-cop-r1.md`) — this is the first reviser round to answer Step B;
`reviser-r1`/`reviser-r2` answered Step A (`implementation-auditor`) only.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r1-f1 | Fixed | Added `protected async httpGetArray<T>(path, itemSchema, context, params?): Promise<Lenient<T>[]>` to `BaseResource` (`src/client/resources/base-resource.ts`): does the `axios.get` (tagging `{kind:'read'}`) then delegates to `validateArrayResponse`, giving bare-array endpoints (`getByMacAddress`, `getDeviceAuditByMacAddress`, `getStdOut`, `getStdErr`) the same per-item lenient drop `paginate` gives enveloped pages, instead of `httpGet`'s whole-array `safeParse`. Documented in the class's own "Six primitives, not four" doc (mirroring the `httpPut` precedent). Added `tests/unit/client/base-resource.test.ts`'s new `httpGetArray` describe block (3 tests): tags `{kind:'read'}` and validates leniently, drops a malformed item without failing the call, forwards query params. |
| architect-r1-f2 | Fixed | Added `resolveNextPageUrl`/`parseAbsoluteUrl` (`base-resource.ts`): before following a page's `nextPageUrl`, `paginate` now parses it against the axios instance's configured `baseURL` and throws `DattoValidationError('response')` (via the new `paginateGuardError` helper, a hand-built single-issue `ZodError`) if the origin doesn't match, otherwise re-issues the request as `pathname + search` against the shared `baseURL` rather than the raw absolute URL — so the bearer token can never be sent to a host outside the configured `apiUrl`. Added a `paginate.test.ts` case: a `nextPageUrl` pointing at `https://attacker.example.com` is refused. |
| architect-r1-f3 | Fixed | Same change as `engineer-r1-f1` (identical finding, addressed once): `paginate` now tracks visited URLs in a `Set` (throws immediately on an exact repeat — a cycle) and enforces a `MAX_PAGINATION_PAGES = 10_000` ceiling (throws once exceeded, regardless of whether URLs repeat — catches an ever-advancing chain). Added two `paginate.test.ts` cases: a `nextPageUrl` that cycles back to an already-fetched page throws rather than looping forever; an ever-advancing `nextPageUrl` that never repeats and never terminates throws once the cap is exceeded (exercised against a hand-stubbed `AxiosInstance` rather than 10,000 real nock round trips — measured at ~3.4s/1000 real HTTP-mocked requests, making the literal cap impractical to exercise via nock without adding assurance beyond what the stub already proves; the stub calls `paginate` through the real `BaseResource`, only bypassing the HTTP-mock layer). |
| architect-r1-f4 | Fixed | Added pre-coerced `deviceSchema: z.ZodType<Device>` / `alertSchema: z.ZodType<Alert>` exports to `src/schema-overrides/types.ts` (a local, same-file type-only cast — `deviceResponseSchema as unknown as z.ZodType<Device>` — not by importing `coerceSchema`), re-exported from `index.ts`, so `this.httpGet(path, deviceSchema, ctx)` gets `Promise<Device>` as the path of least resistance instead of the closed-enum `deviceResponseSchema` inference. Declined the "consider relocating `coerceSchema`" sub-suggestion: the plan's Phase 6 Step 1 explicitly pins "port from `fuze-api`... Keep `coerceSchema`" inside `base-resource.ts`, and moving it would both deviate from that explicit instruction and require `schema-overrides` to import from `client/resources` (the same inverted-dependency problem the finding itself warns against) — the local cast in `types.ts` gets the same fix without either cost. `coerceSchema` itself is unchanged and remains available for any reconciled type without a named pre-coerced export. |
| architect-r1-f5 | Fixed | Rewrote `implementation-phase6-notes.md` §1 (in/out-of-scope), §3 (Files Touched), §4 (Implementation Summary), §5 (added Deviations 4/5 for the `Lenient<T>` and paginate-guard changes, retitled Deviation 1 "Six primitives"), §6 (rewrote Decision 2 for the shipped `Lenient<T>` behavior, expanded Decision 3 for the new `deviceSchema`/`alertSchema` exports, rewrote Decision 4 to state all 9 write bodies are reconciled), §7 (Tests — current file/test counts and the new `httpGetArray`/SSRF/cycle/cap/unknown-key coverage), §8 (added the SSRF/cap security bullet), §11 (removed the now-resolved `warrantyDate`/"deferred write bodies" bullet), §12 (323/25 test count), §13 (updated Final Assertion) to match the code as it now stands after this round, not just after `reviser-r1`/`reviser-r2`. |
| engineer-r1-f1 | Fixed | Same fix as `architect-r1-f3` above (visited-URL cycle check + `MAX_PAGINATION_PAGES` cap in `paginate`), same two new `paginate.test.ts` cases (cyclic `nextPageUrl`, page-cap-exceeded). |
| engineer-r1-f2 | Fixed | Extracted `requireSomeField<T extends z.ZodType<Record<string, unknown>>>(schema, message?)` in `src/schema-overrides/write-bodies.ts`, applied to all four previously-duplicated call sites (`udfWriteBodySchema`, `updateSiteVariableWriteBodySchema`, `updateAccountVariableWriteBodySchema`, `updateProxyWriteBodySchema`), removing the four copy-pasted `.refine` predicates. |
| engineer-r1-f3 | Fixed | `paginate` now computes `const record = isRecord(data) ? data : undefined;` once (importing the shared `src/util/is-record.ts` guard) and reads `record?.pageDetails` / `record?.[arrayKey]` from it, replacing the two duplicated `(data as Record<string, unknown> | undefined)` hand-casts. |
| engineer-r1-f4 | Fixed | `DeviceUdfInput` now derives from `udfWriteBodySchema` (was the raw generated `setUdfFieldsBody`); `SiteProxyInput` now derives from `updateProxyWriteBodySchema` (was the raw generated `updateProxyBody`) — every `*Input` alias in `write-bodies.ts` now derives from its own override schema, matching its doc comment. |
| engineer-r1-f5 | Fixed | Extracted `describeType(value: unknown): string` (module scope, `base-resource.ts`) — `undefined`/`null`/`typeof value` — and call it from `validateArrayResponse`'s `logger.warn` meta, replacing the inline nested ternary. |
| engineer-r1-f6 | Fixed | Same fix as `typescript-cop-r1-f2` (identical finding, addressed once): added `SiteVariableUpdateInput`/`AccountVariableUpdateInput` type aliases (each `z.infer<typeof …WriteBodySchema>`) to `write-bodies.ts`, exported from `index.ts`. |
| engineer-r1-f7 | Fixed | Added `const BODILESS_WRITE_ARITY = 3 as const;` next to `BodilessWriteArgs`'s definition in `base-resource.ts`; `sendWrite` now dispatches on `args.length === BODILESS_WRITE_ARITY` instead of the bare literal `3`. |
| project-lead-r1-f1 | Fixed | Same fix as `architect-r1-f5` above — the phase-6 notes rewrite covers every section `project-lead-r1-f1` names (§3, §4, §6 Decision 4, §7, §11, §12): all 9 write bodies reconciled (not "`device-udf-set` only, deferred"), 22 `write-bodies.test.ts` tests (not 3), `warrantyDate`'s nullable fix reported as already shipped (not an open Phase 7/8 follow-up), and the current 323/25 test totals. |
| project-lead-r1-f2 | Fixed | Added a "rejects an unknown key" test to `tests/unit/schema-overrides/write-bodies.test.ts` for each of the 8 previously-uncovered schemas (`siteCreateBodySchema`, `deviceJobCreateBodySchema`, `warrantyWriteBodySchema`, `createSiteVariableWriteBodySchema`, `createAccountVariableWriteBodySchema`, `updateSiteVariableWriteBodySchema`, `updateAccountVariableWriteBodySchema`, `updateProxyWriteBodySchema`), mirroring the existing `udfWriteBodySchema` case — R6's unknown-key-rejection guarantee is now CI-gated for every schema in the module (16 → 22 tests). |
| typescript-cop-r1-f1 | Fixed | Implemented option (a) from the finding: `validateResponse`/`validateArrayResponse` now return `Lenient<T>`/`Lenient<T>[]` (no `as T` cast — `parseLenient`'s own `Lenient<T>`-typed `result.data` is returned directly), and every primitive built on them (`httpGet`, `httpGetArray`, `httpPost`/`httpPut`/`httpPatch` via `sendWrite`, `paginate`) now declares `Promise<Lenient<…>>` accordingly. This mirrors Phase 4's own precedent for `Lenient<T>` (the type is now honest at the `BaseResource` boundary, matching how `Device`/`Alert`'s open-enum widening is already reflected in their exported types) rather than re-narrowing silently one layer up. A Phase 7/8 resource method that wants to expose the clean `Device`/`Alert` shape re-asserts that explicitly at its own return site (documented in `validateResponse`'s doc comment and `implementation-phase6-notes.md` §5 Deviation 4/§6 Decision 2). Did not additionally add a `null`-valued-field pin test per the finding's option (b) — that alternative applies only if the team wants the *opposite* (bare `T`) behavior, which option (a) supersedes; existing `Lenient<T>` behavior is already pinned by Phase 4's own `tests/generated/lenient-type-pin.ts`. Re-verified: `npm run lint`/`typecheck`/`test`/`build` all pass (323 tests, 0 lint errors) with no other code depending on the old bare-`T` return type (no Phase 7/8 resource classes exist yet). |
| typescript-cop-r1-f2 | Fixed | Same fix as `engineer-r1-f6` above — added `SiteVariableUpdateInput`/`AccountVariableUpdateInput` exports to `write-bodies.ts`/`index.ts`. |

Verified together: `npm run lint` (0 errors, 11 pre-existing warnings in the untouched old surface,
unchanged from baseline), `npm run typecheck`, `npm test` (323 tests passing, up from 311 — 12 new
in `src/client/resources/base-resource.ts`'s test suites, 6 new in `write-bodies.test.ts`), and
`npm run build` all green.
