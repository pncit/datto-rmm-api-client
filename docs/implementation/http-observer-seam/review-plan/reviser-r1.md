## reviser — round 1

Disposition of the Open findings in `plan-auditor-r1.md` (none routed `Challenge` by triage, so all
four are dispositioned). Plan edits applied per the mediator/human rulings recorded in `triage-r1.md`
and `triage-r2.md`.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r1-f1 | Fixed | Mediator ruling applied. Pinned `fireError` to one signature `fireError(logger, observer, capture, rawError, mappedError: DattoApiError)` — every caller pre-maps. Rewrote Phase 1 S5 prose to drop the internal-mapping description (403→`build403Error` / else→`fromAxiosError`) and state that callers pre-map (shared instance via `mapObserverError(error, build403Error)`, grant via its own `mapped`). Edited the Phase 2 example (line ~258) to pass `mapObserverError(error, build403Error)` rather than the bare `build403Error` mapper function. Phase 2 Step 4 prose already referenced `mapObserverError(...)`, and the Phase 3 example already passes the pre-constructed `mapped` — all three call sites now hand a finished `DattoApiError` in the 5th position and none passes `build403Error`. |
| plan-auditor-r1-f2 | Fixed | Human ruling applied (both parts). (1) Narrowed the Phase 1 exit gate: replaced the whole-file case-insensitive `! grep -iq 'axios' src/http/http-observer.ts` with `! grep -Eq "from ['\"]axios['\"]" src/http/http-observer.ts` plus a `! grep -Eq '\bAxios[A-Z]' src/http/http-observer.ts` type-usage check — precise, tolerates explanatory prose, still catches a real import or axios type leak. (2) Stripped "axios" from the Phase 1 example doc comment (`// always mapped; never a raw axios error` → `// always a mapped DattoApiError; never the raw transport error`). Phase 4's gate carries no axios source grep, so no change there. The narrowed gate now passes the illustrative example (imports are `zod`/`../errors`; no type matches `\bAxios[A-Z]`). |
| plan-auditor-r1-f3 | Accepted | Human ruling: accept the `index.ts`-direct placement and amend the **design** (line 267, and 87/95) to say the observer types export directly from `src/index.ts` alongside `DattoLogger`. The plan already routes all five types through `index.ts` (Phase 1 S3, matching the verified `DattoLogger` precedent) and needs no edit; the package-root surface is identical either way. The design correction is a cross-stage remedy routed by the driver per the triage plan's rulings (triage-r2 Clusters / Chain watch) — I do not edit the design. No plan change made; plan retains `index.ts`-direct. |
| plan-auditor-r1-f4 | Fixed | Mediator ruling applied. Added `npm run build` and `! grep -q 'declare module' dist/index.d.ts` to the Phase 2 **and** Phase 3 Exit Gates — those phases first make axios-importing `src/http/observer.ts` reachable from the `index.ts` value graph (via `http-client.ts` and `auth-manager.ts` respectively), so a leaked `declare module "axios"` is caught there rather than two phases downstream. Per the ruling, did **not** add a blanket `grep 'axios' dist/index.d.ts` (dist legitimately carries 16 `AxiosInstance`/`AxiosError` refs via `BaseResource.axios` / `DattoApiError.fromAxiosError`), and did not add the `http-observer.ts` axios source check to Phase 2/3 (that file isn't edited there); added a note to each gate explaining the placement. |
