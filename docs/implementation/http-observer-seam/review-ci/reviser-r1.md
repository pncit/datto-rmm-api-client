## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| ci-runner-r1-f1 | Fixed | The `validate` job's "Verify version changed" step failed because `package.json` still read `1.0.2`, identical to `main`'s committed version — confirmed via `gh pr checks` (only the `validate` check reported, `fail`) and the failing-step log (`package.json version unchanged (1.0.2 -> 1.0.2)`). This PR is a purely additive feature (new optional `httpObserver` config, five new exported types — no breaking changes per the plan's Executive Summary/Non-Goals), so per semver a minor bump is correct: bumped `package.json`'s `version` from `1.0.2` to `1.1.0` and mirrored the same value in both `version` fields of `package-lock.json` (root package entry and the `packages[""]` entry) with a minimal, version-only diff. Verified locally: `npm run lint`, `npm run typecheck`, `npm test` (590/590 passing), `npm run test:repro`, and `npm run build` all pass unchanged with the bumped version; the base branch (`main`) still shows `1.0.2`, so `CUR != PREV` will now hold and the gate will pass. (Unrelated: a local `npm ci` attempt in this sandbox fails with `Missing: @esbuild/win32-x64@0.28.1 from lock file` — confirmed this occurs identically against the *unmodified* origin `package-lock.json` on this branch before my edit, i.e. a pre-existing local npm/platform mismatch in this sandbox, not something introduced by this change or reachable by the CI job, which runs `.nvmrc`-pinned Node/npm via `actions/setup-node` and never got past the earlier "Verify version changed" step in the failed run.) |
