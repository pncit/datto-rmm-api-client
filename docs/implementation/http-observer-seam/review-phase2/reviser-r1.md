## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| engineer-r1-f1 | Fixed | Per the human's Option (A) ruling: replaced the observer request interceptor's manual `` `${requestConfig.baseURL ?? ""}${requestConfig.url ?? ""}` `` concatenation with `instance.getUri(requestConfig)` (`src/http/http-client.ts:388`), which runs axios's own `buildFullPath`/`combineURLs` and appends the serialized `params`, so the observed `url` matches exactly what axios dispatches, including the query string. Amended `design.md:191` and `plan.md:306`/`325` to specify the `getUri`/serialized-params composition (design.md:102's "exactly as dispatched" contract wording is now accurate and was left as-is). Added two new tests in `tests/unit/http/http-client.test.ts`: a `params`-bearing GET asserting the observed `url` includes the serialized query string, and a `paginate`-shaped first-page GET (mirroring `BaseResource.paginate`'s `this.axios.get(startPath, { params: pageParams, ... })` first-page call) asserting the same. Also added corresponding bullets to plan.md's Tests section for this phase. `npm test` and `npm run typecheck` both pass. |
| engineer-r1-f2 | Fixed | Subsumed by the f1 fix per both the author's note and the human ruling: `instance.getUri(requestConfig)` delegates `combineURLs`/`buildFullPath` (correct slash-joining) and absolute-`url` detection to axios itself, so the observed URL can no longer diverge from the dispatched one via either edge case (trailing/leading-slash `//`, or an absolute `url` ignoring `baseURL`). No separate code change beyond the f1 fix; design.md/plan.md amendments listed under f1 cover this too. |
| architect-r1-f1 | Fixed | Added a test ("fires onRequest, onError(401), onRequest, onResponse(200) for a transparently-retried 401 with an onUnauthorized hook") to the observer `describe` block in `tests/unit/http/http-client.test.ts`: instruments a client with an `onUnauthorized` hook and a full observer, `nock`s a 401 then 200, and asserts the event-kind sequence `request, error, request, response` with `errorEvent.statusCode === 401` and `responseEvent.statusCode === 200` — pinning that a silently-recovered 401 still fires exactly one terminal `onError` before the retry's `onResponse`. Written on the tightened `ObserverEvent` tuple type (no new `as` cast). `npm test` passes (green, including this new assertion). |
| typescript-cop-r1-f1 | Fixed | Replaced the two `Array<[kind, unknown]>` capture arrays and their four `events[N]![1] as DattoHttp*Event` casts with a module-level discriminated-union tuple type `ObserverEvent = ["request", DattoHttpRequestEvent] \| ["response", DattoHttpResponseEvent] \| ["error", DattoHttpErrorEvent]` plus three small narrowing helpers (`requestPayload`/`responsePayload`/`errorPayload`) that narrow via `event[0] !== kind` control-flow analysis (TS 4.6+ discriminated-tuple narrowing) rather than a cast. Applied the same type to the new params/paginated-first-page/401-sequence tests added in this round, so no fresh `as` casts were introduced anywhere in the file. Verified `grep -n "as DattoHttp" tests/unit/http/http-client.test.ts` returns nothing and `npm run typecheck` is clean. |
