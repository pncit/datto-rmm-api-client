## reviser — round 1 (design revision)

Triage routed all five findings `Remediate`; none `Challenge`, none `Ruled`. Applied
root-cause-first in the plan's order A (f1,f2) → B (f3,f5) → C (f4), all as direct
edits to `design.md`.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| design-auditor-r1-f1 | Fixed | Decision 5 and the Schema-and-wiring paragraph now pin the mechanism: the observer request interceptor is registered **first** in `createHttpClient` so that under axios LIFO ordering it runs **last** — after the rate-limit interceptor and after `AuthManager.attachTo`'s later-registered Bearer interceptor — which is what makes "post-throttle, post-auth" true. Added a Success Criterion and Verification test asserting the observed shared-instance request carries `Authorization: Bearer`. In-scope mechanism gap, fixed by tightening. |
| design-auditor-r1-f2 | Fixed | Decision 5 now states `onRequest` **captures** method/url/headers/body and **stashes** it (with the dispatch timestamp) on per-attempt internal state via the `axios-augment.d.ts` `rateDescriptor` precedent, and that `onResponse`/`onError` reuse the stash rather than re-reading `response.config` (which by then holds the serialized body and normalized headers). Schema-and-wiring and a rejected alternative reinforce it; added a Success Criterion + Verification asserting terminal `requestBody`/`requestHeaders` equal what `onRequest` observed. |
| design-auditor-r1-f3 | Fixed | Reworded R5 to be non-contradictory: form/urlencoded = serialized string; JSON request = pre-serialization object (explicitly not the literal wire bytes); JSON response = parsed object; "never pre-redacted" kept; the contradictory "never pre-parsed away from the wire form" clause removed. Aligned the Key Concepts "Wire fidelity" bullet and the `onRequest.body` payload comment to match. |
| design-auditor-r1-f4 | Fixed | Added a Success Criterion and matching Verification test: an injected pre-dispatch throttle delay is excluded from `durationMs` (timestamp taken after `rateLimiter.acquire`), the guarantee Decision 5 makes. Dovetails with the f1/f2 stash-after-acquire mechanism. |
| design-auditor-r1-f5 | Fixed | Relocated the grant Basic-header caveat from Risks row 2 into where the contract is defined: Decision 5 now states `Authorization: Bearer` is present on shared-instance requests but the grant's `Authorization: Basic` (`public-client:public`, non-secret, applied by axios from the per-request `auth:` option) is absent by design, with the API key riding in the captured body. Mirrored on the `onRequest.headers` payload comment; added the "grant captured headers omit `Authorization`" assertion to Success Criteria and Verification. Risks row 2 retained as the risk record. |
