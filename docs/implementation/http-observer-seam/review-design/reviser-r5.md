## reviser — round 5 (design revision)

Applied the triage-r5 plan in root-cause-first order (K → L). Both Open findings
were routed `Remediate` (untagged); neither was routed `Challenge`, so both carry a
disposition row. No finding was a Requirements/Research gap, so no `Escalate`.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| engineer-r3-f1 | Fix | Genuine in-scope framing gap: the capture-and-stash is a client-internal step (Decision 2's `observer.ts` primitive), but Decision 5 (`:187`) narrated it as `onRequest`'s side-effect and Decision 4 rule 2 (`:174`) gated the shared-instance `onError` on "fired `onRequest`," while the three callbacks are independently optional (R1). Decoupled by tightening the existing prose, not padding: Decision 5 now states the stash is written at the dispatch point whenever `httpObserver` is present, independent of which callbacks the consumer supplied, with `onRequest` (if present) merely *invoked* from that same point; rule 2 now gates on "the attempt reached dispatch (stash written)" and explicitly not on a supplied `onRequest` callback; the rationale's "neither attempt fired `onRequest`" reworded to "reached the observer's dispatch-point capture" for consistency. No mechanism change (both named non-dispatched paths, the `!isAxiosError`-guard placement, and the wire-status/post-2xx sub-rules — the Cluster J rules — are untouched). Carried into the plan via a new Success Criterion + a `npm test` assertion: an `onError`-only consumer still gets a populated terminal `onError` on a dispatched non-2xx attempt. |
| architect-r3-f1 | Fix | Genuine in-scope surface inconsistency: `DattoHttpHeaders` (`:98`) was referenced by name in every public event field but absent from the enumerated four-type export set, and Key Concepts (`:89`) used the inline `Record<…>` form — two representations, no stated intent. Chose Option A (export the alias) — consistent with the design's existing rationale for exporting the named payload types (an importable name to annotate a standalone helper), here a header helper. Reconciled all three sites plus the two count sites: Key Concepts (`:87`, `:89`), the payload-block enumeration (`:95`), Success Criteria (`:240`), and Verification (`:258`) now all state **five** public types and reference the alias; the inline form at `:89` is retained only as the alias's definition-in-parentheses. Tightening the existing surface to internal consistency, not new capability. Deviated from the plan's option-neutral stance by picking A; rationale as above. |
