## reviser — round 1

Implemented per `triage-r1.md`'s root-cause-first cluster order. All ten findings were routed
`Remediate` (none `Challenge`), so every one gets a disposition row below.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| typescript-cop-r1-f1 | Fixed | Cluster 1. Rewrote `eventsOf` in `tests/integration/http-observer.test.ts` to eliminate every `as …[]` cast at its five call sites (`:93→104`, `:101→~121`, `:146/149→168/171`, `:168→190`). **Deviation from the triage's literal suggestion, stated rationale:** the triage's/typescript-cop's proposed single generic signature (`function eventsOf<K extends ObservedEvent["kind"]>(...): Extract<ObservedEvent, { kind: K }>["event"][]`) does not actually compile against this repo's `tsc` (5.9.3, strict) — confirmed by reproducing it standalone: TypeScript computes two structurally-unrelated representations of the indexed-access-on-generic-conditional type at the declaration site vs. the `filter().map()` call site and rejects the assignment (`TS2322`/`TS2719`, "Two different types with this name exist, but they are unrelated" — a known limitation with a 3-member discriminated union). Used three concrete overload signatures (`kind: "request"` → `DattoHttpRequestEvent[]`, etc.) over one implementation signature instead — same outcome (zero casts at every call site, a `kind` typo fails to compile since only the three literal overloads exist), verified compiling. |
| engineer-r1-f2 | Fixed | Same defect as typescript-cop-r1-f1 (Cluster 1); same fix, same file. |
| typescript-cop-r1-f2 | Fixed | Cluster 1. Replaced the `(terminal[0]!.event as DattoHttpErrorEvent)`/`(terminal[1]!.event as DattoHttpResponseEvent)` casts (429→retry→200 test) with `if (first.kind !== "error") throw …` / `if (second.kind !== "response") throw …` narrows the compiler follows, after first checking `!first \|\| !second` (avoids mixing `!`-assertions with discriminant narrowing, which does not propagate through a non-null-asserted expression). `npx tsc --noEmit` / `typecheck:test` and `vitest run tests/integration/http-observer.test.ts` both pass. |
| typescript-cop-r1-f3 | Fixed | Cluster 1. Replaced `grantRequest!.body as string` with an explicit `if (!grantRequest) throw …` then `if (typeof grantRequest.body !== "string") throw …` runtime guard before constructing `URLSearchParams`, per the recommendation. |
| project-lead-r1-f1 | Fixed | Cluster 2. Added an assertion, in the first test, that the account request's own `onRequest` event carries `headers.Authorization === "Bearer tok-1"` — proving the real `AuthManager.attachTo` interceptor and the observer interceptor compose in the documented order against the real assembled client, closing R9's bearer-token half. Verified the assertion is not a tautology: temporarily changed the expected value to `"Bearer wrong"` and confirmed the test fails with the real `"Bearer tok-1"` received, then reverted. |
| engineer-r1-f3 | Fixed | Cluster 2. Added an assertion, in the same first test, that the grant request event's `headers` carries no `authorization`/`Authorization` key (Phase 3's intentional Basic-auth omission, now proven end-to-end). Verified the assertion actually exercises production code: temporarily added an `Authorization` header to `AuthManager`'s grant-request `captureRequest` call in `src/auth/auth-manager.ts`, reran the suite, watched the new assertion fail, then reverted the file. Corrected `implementation-phase4-notes.md` §7/§8 to describe the new assertions accurately (previously §8 misattributed the Basic-auth-omission coverage to "the lazy-refresh test," which never inspected headers at all; it's now attributed to the grant test, where the assertion actually lives). |
| engineer-r1-f1 | Fixed | Cluster 3. Same defect as architect-r1-f2; same fix, same location. |
| architect-r1-f2 | Fixed | Cluster 3. Rewrote the `onResponse`/`onError` bullets in `README.md`'s "Observing HTTP exchanges" section to state explicitly that terminal events carry `method`/`url` and the **renamed** `requestHeaders`/`requestBody` (not `headers`/`body`), and that `onError` also carries `durationMs` (previously named only on `onResponse`). Verified every field name in the two bullets now matches `src/http/http-observer.ts:33-76` exactly, and no bullet references unrenamed `headers`/`body` on a terminal event. |
| architect-r1-f1 | Fixed | Cluster 4. Added the API secret to both enumerations: `README.md`'s "Raw, un-redacted delivery" warning now reads "...the API key and API secret in the OAuth token grant's request body," and the mirror JSDoc warning in `src/http/http-observer.ts:84-89` was aligned identically. Both now name all three credentials the grant body/headers actually carry per `auth-manager.ts:145-148`. |
| project-lead-r1-f2 | Fixed | Cluster 5. Added a one-line cross-reference in "Upgrading from 0.1.x" item 4, immediately after "There is no `axiosInstance` config option," pointing a migrating 0.1.x reader at [Observing HTTP exchanges](#observing-http-exchanges-httpobserver) as the supported replacement for axios-injection-based observability. Confirmed the anchor resolves against the section's actual heading slug (`## Observing HTTP exchanges (\`httpObserver\`)` at `README.md:380`). |

**Full verification run:** `npm run typecheck` (src + test + tools), `npm test` (590/590 passing,
40/40 files — unchanged count, only assertions/typing tightened within the existing 5 integration
tests), `npm run lint` (clean), `npm run build` (clean; `dist/index.d.ts` unaffected — no `src/**`
type-shape change, only two doc-comment words added). `grep -n " as Datto" tests/integration/http-observer.test.ts` returns nothing.

**Files touched this round:** `tests/integration/http-observer.test.ts`, `README.md`,
`src/http/http-observer.ts` (JSDoc-only, per the triage's explicit scope allowance for Cluster 4),
`docs/implementation/http-observer-seam/implementation-phase4-notes.md` (§7/§8 corrected to
describe the new assertions accurately).
