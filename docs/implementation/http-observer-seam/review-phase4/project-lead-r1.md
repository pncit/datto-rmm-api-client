## project-lead — round 1

### Requirements Coverage
(Phase 4's declared requirements per the plan: R2, R3, R4, R6, R8, R9 — "end-to-end verification of behavior wired in Phases 1–3.")

| Requirement | Status | Notes |
|-------------|--------|-------|
| R2 (fire once per physical attempt; retries never collapsed) | Fully Met | `429 → retry → 200` test asserts the account-URL terminal subsequence is exactly `[onError(429), onResponse(200)]` through the real assembled client. |
| R3 (OAuth grant/refresh observed) | Fully Met | Grant test asserts the request event's `body` round-trips through `URLSearchParams` to the exact `grant_type`/`username`/`password` values, and the grant's own `onResponse` fires with `statusCode: 200`. |
| R4 (each pagination page observed) | Fully Met | 2-page `devices()` walk asserts exactly 2 request + 2 terminal events filtered to the devices-path URL. |
| R6 (`onResponse` for 2xx, `onError` for non-2xx/no-response) | Fully Met | Demonstrated by both the 429/200 test and the lazy-refresh-401 test. |
| R8 (`onError.error` is the raw, unmapped error) | Fully Met | Both failing-case tests assert `not.toBeInstanceOf(DattoApiError)` and `axios.isAxiosError(...) === true` on the observed error, while the SDK itself still throws `DattoApiError` to the caller. |
| R9 (raw, un-redacted delivery — bearer token in headers, API key in body) | Partially Met | The API-key half is verified (grant body). The bearer-token half is never verified against the real assembled client anywhere in this phase or any other — see finding f1. |

### Findings

| ID | Severity | Status | Category | Location | Finding | Recommendation |
|----|----------|--------|----------|----------|---------|-----------------|
| project-lead-r1-f1 | High | Open | Requirements | `tests/integration/http-observer.test.ts` | R9 requires the observer to receive raw payloads "including bearer tokens" in request headers, and this phase's own Goal is to prove the cross-layer scenarios that "only exist once both transport layers are instrumented." The real `AuthManager.attachTo` Bearer interceptor is registered on the shared instance only in `DattoRmmClient`'s constructor (`authManager.attachTo(axiosInstance)`, called after `createHttpClient`) — this is the *only* place in the codebase where the real interceptor-registration order (observer-first/runs-last vs. the real, later-attached Bearer interceptor) is exercised; every unit test (`tests/unit/http/http-client.test.ts`) substitutes a hand-added mock Bearer interceptor to simulate `attachTo`, never the genuine one. None of the 5 assembled-client tests in this new suite assert that an observed resource request's `headers.Authorization` actually carries `Bearer <token>` — the design's own Risk table entry ("Interceptor/instrumentation ordering captures headers before the auth header is attached") explicitly calls for a test asserting the bearer header is present, and this phase is the only one positioned to prove it against the real object graph. | In the first test ("observes the token grant end-to-end…"), also capture the account-request's `onRequest` event (filter `requests` by `ACCOUNT_PATH`) and assert its `headers.Authorization` equals `` `Bearer ${the grant's access_token}` `` (e.g. `"Bearer tok-1"`), proving the real `attachTo` interceptor and the real observer interceptor compose in the documented order end-to-end. |
| project-lead-r1-f2 | Medium | Open | BehaviorIntent | `README.md` (§"Upgrading from 0.1.x", item 4) | The design's entire premise (Problem Statement / Executive Summary) is that `httpObserver` "restores a capability that existed in 0.1.x (inject-an-axios-instance-and-add-interceptors)…unblocking adoption of 1.0.x for compliance-bound consumers." The "Upgrading from 0.1.x" section's item 4 ("Config fields changed") states "There is no `axiosInstance` config option" with no cross-reference to `httpObserver` — the exact reader this feature exists for (a 0.1.x consumer who injected an axios instance for compliance observability) hits this line while migrating and is given no pointer to the replacement capability, undermining the feature's stated adoption-unblocking purpose. | Add a one-line cross-reference in item 4 (or immediately after it) pointing to the new [Observing HTTP exchanges](#observing-http-exchanges-httpobserver) section as the supported replacement for the axios-injection-based observability use case. |
