## reviser — round 1

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Human ruling: design amended upstream (commit `67a6060`, design.md Decision 5) to mandate composing the grant's observed `url` via the grant client's own resolver. Implemented per the amended design: `performRefresh` (`src/auth/auth-manager.ts`) now composes the captured `url` as `this.grantClient.getUri({ url: GRANT_PATH })` instead of `` `${this.config.apiUrl}${GRANT_PATH}` ``, so it runs axios's own `buildFullPath`/`combineURLs` and matches exactly what `grantClient.post` dispatches, including under a trailing-slash `apiUrl`. Added a test (`tests/unit/auth/auth-manager.test.ts`, "composes the observed url via the grant client's own resolver...") constructing the manager with a trailing-slash `apiUrl` and asserting the observed `requestEvent.url` is the single-slash form with no `//auth`; the existing slash-free-`BASE_URL` assertion (now-line ~345) still passes unchanged. |
| engineer-r1-f2 | Fixed | Human ruling: the separately-requested design change was reviewed and declined as unnecessary (design.md was already correct following the architect-r1-f1 amendment) — the declination rejects the duplicate *design* edit, not the underlying *code* fix. The one code change that resolves architect-r1-f1 (grant URL now composed via `this.grantClient.getUri({ url: GRANT_PATH })`) is exactly the remedy this finding asked for at the same site (`auth-manager.ts:162`) and closes it too; no second/independent change was made. |
| architect-r1-f2 | Fixed | Hoisted `const GRANT_CONTENT_TYPE = "application/x-www-form-urlencoded";` beside `GRANT_PATH` in `src/auth/auth-manager.ts` and referenced it from both the `grantClient` constructor's `axios.create({ headers })` and the `captureRequest({ headers })` call, so the observed Content-Type can no longer drift from the dispatched one. Grep confirms the literal string now appears exactly once in the file. |
| engineer-r1-f1 | Fixed | Same change as architect-r1-f2 (duplicate finding, same two lines): `GRANT_CONTENT_TYPE` constant hoisted and referenced at both sites in `src/auth/auth-manager.ts`. |
| architect-r1-f3 | Fixed | Added a test in `tests/unit/auth/auth-manager.test.ts` ("fires onRequest/onResponse exactly once for a burst of concurrent getToken calls sharing one grant round-trip") that fires three concurrent `getToken()` calls against a cold cache with a shared observer registering all three callbacks, asserts the nock scope for a single grant POST is satisfied, and asserts the collected event kinds are exactly `["request","response"]` — pinning the single-flight/observer invariant (one event pair per physical grant, not per caller) on the grant path. |
| engineer-r1-f3 | Fixed | Updated both error-path tests in `tests/unit/auth/auth-manager.test.ts` (401 and transport-failure) to register all three observer callbacks (previously `onError` only) and assert the captured event-kind sequence is exactly `["request","error"]`, proving `onResponse` does not also fire — mirroring the malformed-2xx test's existing exclusivity check. Added an `errorPayload` helper (alongside the existing `requestPayload`/`responsePayload`) to narrow the tuple without an `as` cast, and preserved every existing assertion (`statusCode`, raw-error identity, `axios.isAxiosError`, `logger.warn`) against the narrowed payload. |
| typescript-cop-r1-f1 | Fixed | Replaced `new URLSearchParams(requestEvent.body as string)` in `tests/unit/auth/auth-manager.test.ts` with a compiler-visible narrowing (`if (typeof requestEvent.body !== "string") throw new Error(...)`) followed by the unguarded `requestEvent.body` reference — no `as` cast remains on `requestEvent.body` anywhere in the file. `npm run typecheck`/`tsc --noEmit` and `npm test` (585 passed) both confirm. |
