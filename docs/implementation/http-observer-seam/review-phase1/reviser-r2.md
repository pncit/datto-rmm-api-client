## reviser — round 2

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Cluster 1 root cause. Replaced the shared, field-agnostic `observerCallbackSchema = z.custom<(event: never) => unknown>()` with a per-field generic factory `function observerCallbackSchema<Fn>() { return z.custom<Fn>((v) => typeof v === "function").optional(); }`, instantiated as `observerCallbackSchema<DattoHttpObserver["onRequest"]>()` / `["onResponse"]` / `["onError"]` in `dattoHttpObserverSchema` (`src/http/http-observer.ts`). Keeps the non-wrapping `z.custom` identity pass-through (R7/R9) while restoring per-field `z.infer` precision, so `DattoRmmClientConfig["httpObserver"]` now types each callback with its concrete `DattoHttp*Event` parameter. Verified in `dist/index.d.ts`: `onRequest: z.ZodOptional<z.ZodCustom<((event: DattoHttpRequestEvent) => void) | undefined, ...>>` (no `never`). Added the parity/no-`never` compile pin (see architect-r1-f1's own recommendation) in `tests/generated/surface-pin.ts`, and removed the `as never` workaround in `tests/unit/client/config.test.ts` (now passes a real `DattoHttpRequestEvent`). |
| project-lead-r1-f1 | Fixed | Same Cluster 1 fix as architect-r1-f1 (BehaviorIntent/R1 face) — the per-field generic schema; no separate change needed beyond the one already made. Re-verified: `createDattoRmmClient({ ..., httpObserver: { onRequest: (event) => event.method } })`-shaped inline literals now type-check with the parameter inferred as `DattoHttpRequestEvent`, not `never` (proved by the new `_OnRequestParamPin`/`_OnResponseParamPin`/`_OnErrorParamPin` type pins in `surface-pin.ts`, which fail if the round-1 `never`-erasure regresses). |
| typescript-cop-r1-f1 | Fixed | Same Cluster 1 fix (public-types/export-hygiene face). Added `_SchemaObserverParityPin` in `tests/generated/surface-pin.ts` asserting `z.infer<typeof dattoHttpObserverSchema>` and `DattoHttpObserver` are mutually assignable, so the schema and the hand-authored interface cannot silently drift apart going forward. |
| typescript-cop-r1-f2 | Fixed | Cluster 1, internal-helper face. Made `invokeObserver` generic over the event type — `export function invokeObserver<E>(logger, callbackName, fn: ((event: E) => void) \| undefined, event: E): void` (`src/http/observer.ts`) — and removed the `(fn as (event: unknown) => unknown)(event)` cast; the body now calls `fn(event)` directly (`const returned: unknown = fn(event);`). Each of the three `fire*` sites now calls it with an explicit type argument — `invokeObserver<DattoHttpRequestEvent>(...)`, `invokeObserver<DattoHttpResponseEvent>(...)`, `invokeObserver<DattoHttpErrorEvent>(...)` — so the compiler pins callback↔event pairing at all three call sites; swapping which callback/event is passed at any site no longer compiles. Updated `observer.test.ts`'s `invokeObserver` unit tests (which previously hand-cast `fn` through `(event: never) => void`) to use a concrete `TestEvent` type instead, and the schema-parsed `invokeObserver` tests to pass real `DattoHttpRequestEvent`/`DattoHttpResponseEvent` objects (their `fn` is no longer `never`-typed post-fix, so the old ad hoc `{ some: "event" }` literals failed excess-property checking against the now-concrete event type — replaced with valid instances of each event shape). |
| architect-r1-f2 | Fixed | Cluster 2. Added a `describe("fireRequest", ...)` block (3 tests: event-field assembly, no-op when `observer` absent, no-op when `onRequest` absent) and a `describe("fireResponse", ...)` block (3 tests: event-field assembly including `AxiosHeaders`→plain-record normalization and `durationMs >= 0`, no-op when `observer` absent, no-op when `onResponse` absent) to `tests/unit/http/observer.test.ts`, mirroring the existing `fireError` suite. All seven Phase 1 primitives now have a direct unit test. |
| engineer-r1-f1 | Fixed | Duplicate of architect-r1-f2 (same mechanism/root cause) — resolved by the same `fireRequest`/`fireResponse` test additions described above. |
| typescript-cop-r1-f3 | Fixed | Cluster 3. Extracted a `safeWarn(logger, callbackName, message)` helper in `src/http/observer.ts` that wraps `logger?.warn(...)` in its own `try { ... } catch { /* logger itself misbehaved; nothing further to do safely */ }`, and routed both swallow paths in `invokeObserver` (the synchronous `catch` and the `.then(undefined, ...)` rejection handler) through it. Added a regression test ("guards a throwing logger.warn so it neither escapes nor leaves an unhandled rejection") in `observer.test.ts` covering both the synchronous-throw path and the rejected-promise path with a `warn` implementation that itself throws, using the existing `process.on("unhandledRejection", ...)` harness; asserts no throw escapes and no unhandled rejection occurs. |
| engineer-r1-f2 | Fixed | Cluster 4. Changed `captureRequest`'s `method: (args.method ?? "get").toUpperCase()` to `method: (args.method || "get").toUpperCase()` (`src/http/observer.ts`) so an empty-string method (not just `null`/`undefined`) also falls back to `"GET"`, matching the published "uppercased, non-empty" contract. (No new dedicated `method: ""` test was added beyond the existing `??`/`\|\|`-covering `captureRequest` suite — the existing "defaults to GET when method is undefined" test plus this one-line change is the full scope the finding calls for; happy to add an explicit `""` case if reviewers want it re-verified directly.) |
| architect-r1-f3 | Fixed | Cluster 4. Changed `normalizeHeaders`'s return from `{ ...(raw as Record<string, string \| string[] \| undefined>) }` (the `DattoHttpHeaders` alias re-spelled inline) to `{ ...(raw as DattoHttpHeaders) }`, casting through the already-imported alias so `DattoHttpHeaders` stays the single source of the header shape (`src/http/observer.ts`). |

Verification run after all fixes: `npm run typecheck` (src + test + tools), `npm test` (565/565 passing across 39 files), and `npm run build` all green. Confirmed exit-gate greps: `dist/index.d.ts` contains `DattoHttpObserver` and now shows the concrete `DattoHttpRequestEvent`/`DattoHttpResponseEvent`/`DattoHttpErrorEvent` parameter types (no `never`) on `onRequest`/`onResponse`/`onError`; no `declare module`; no `ObserverCapture`/`__dattoObserverCapture` leak; `src/http/http-observer.ts` stays axios-free (`from 'axios'` / `\bAxios[A-Z]` greps both empty); `src/http/observer.ts` remains un-re-exported from `src/index.ts`. Sanity-checked the new `surface-pin.ts` regression pins by temporarily reverting to the round-1 shared-schema shape: `npm run typecheck` correctly fails (`_OnRequestParamPin`/`_OnResponseParamPin`/`_OnErrorParamPin` all report "Type 'false' does not satisfy the constraint 'true'"), then passes clean again once restored — confirming the pins would catch a third-round recurrence of the same mechanism.
