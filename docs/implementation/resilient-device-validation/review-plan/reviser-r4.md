## reviser — round 4

Mode B, Phase 2 feedback. Performed an internal self-review (phasing, specificity, exit gates, edge
cases, hidden complexity, dependencies, design alignment) and folded its results into this revision
with no separate output. Then addressed every `Open` finding from the round-2 Architect and Engineer
turns. All fixes are edited directly into `plan.md` (no revision markers). Findings from prior rounds
are all `Closed` and not re-adjudicated.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r2-f1 | Accept | Moved `DevicesEnvelopeSchema`/`DevicesEnvelope` out of `client.ts` into a new **un-barrelled** module `src/internal/devicesEnvelope.ts` (a sibling of the already-non-barrelled `validation.ts`). `src/index.ts` barrels only `client/config/result/schemas`, so the schema stays off the public surface, while both `client.ts` and the Phase 2 fixture-acceptance test import the **real** schema from it — no test-only re-export (which would violate R4) and no inline copy (which would defeat the test). Updated the Assumptions, Implementation Notes, Phase 2 Step 2, the two code snippets, the import-cleanup step, and the fixture-acceptance test note. |
| architect-r2-f2 | Accept | The file-list R4 grep is blind to public-surface growth via a new top-level `export` in `client.ts`/`config.ts` (both barrelled by `index.ts`'s `export *`). Added a second exit-gate guard — `git diff HEAD -- src/client.ts src/config.ts \| grep -qE '^\+export ' && fail` — that fails Phase 2 if either barrelled module gains a `+export ` line. Class methods aren't top-level `export`s, so it doesn't false-positive on method edits. Kept guard (a) (which also trips on any `index.ts` change, mechanically preventing the new internal module from being barrelled). |
| architect-r2-f3 | Accept | The `Array.isArray` guard sits *inside* `validateItems`, but in `off` the throw would occur upstream during `extractor(page)` when `page` is `null`/a primitive. Made the devices extractor optional-chained (`(p) => p?.devices ?? []`) in `getAccountDevices` (Step 4 + snippet) and documented the off-path null-safety in Step 3. Added a Phase 2 test asserting a `null` body and a primitive body both return `{ ok: true, value: [] }` without throwing, exercising the extractor guard the existing well-formed-page test could not reach. |
| engineer-r2-f1 | Accept | `validate()`'s `warn` branch no longer dumps the raw multi-line `result.error.message`; it now logs the failing path (`result.error.issues[0]?.path?.join(".") \|\| "(root)"`), mirroring `toProblemError`, so the sole warn-mode `getDeviceByUid` caller gets a structured "which field drifted" line consistent with the strict path. Updated the Implementation Notes, Phase 1 Step 1, the `validate()` snippet, the Phase 1 `validate()` warn test (asserts the path is named and the raw blob is absent), and added a Phase 2 `getDeviceByUid` warn-mode log test. `validate()` is generic (no `entityLabel`), so device identity in the single-value warn line comes from the caller's context (the operator invoked `getDeviceByUid(uid)`); R6's "log through `logger.warn`" is preserved. |
| engineer-r2-f2 | Accept | Added a `private logger: LoggerLike = config.logger ?? defaultLogger` field, resolved once in the constructor (reusing the value already passed to `HttpClient`); `getAllPages` and `getDeviceByUid` now read `this.logger` instead of each re-deriving `config.logger ?? defaultLogger`. Added a dedicated Phase 2 Step 1 for the field, an Implementation Note, updated both method notes and both snippets, and noted the required `LoggerLike` import in `client.ts`. |
| engineer-r2-f3 | Accept | Reworded Phase 2 Step 5 (formerly Step 4) prose to match the code example: build `const problem = toProblemError("Device", e, res.value, 0)` **once**, log `problem.detail`, then `return { ok: false, error: problem }` — no second `toProblemError` construction. |
| engineer-r2-f4 | Accept | Dropped the duplicative `` `Device validation failed for ${deviceUid}: …` `` prefix (which double-printed "Device" and the uid, since `problem.detail` already reads `Device uid={uid} failed validation at path: …`). The log now uses the non-duplicative `` `getDeviceByUid: ${problem.detail}` `` form. Updated Step 5 prose, the snippet, and the `getDeviceByUid` test assertion (must not contain "Device" twice). |
| engineer-r2-f5 | Accept | Extracted the shared literals into `export const VALIDATION_ERROR_TYPE = "validation-error"` and `VALIDATION_ERROR_STATUS = 400` in `validation.ts` (non-barrelled), consumed by **both** `toProblemError` and the envelope hard-fail branch in `client.ts` (which can't call `toProblemError` due to its distinct title). Updated Phase 1 Step 1, the `validation.ts`/`toProblemError` snippets, Phase 2 Step 3 + envelope snippet, and the import-cleanup step. |
