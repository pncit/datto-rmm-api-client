## reviser — round 2

Disposition of PlanAuditor round-2 `Open` findings. Plan edited in place (no revision markers).

| ID | Disposition | Rationale |
|----|-------------|-----------|
| plan-auditor-r2-f1 | Fixed | Rewrote the Phase 2 off-mode test bullet (formerly "Off, malformed envelope passthrough") as "**Off, per-device passthrough (R8)**": it now specifies a **well-formed page whose `devices` is an array containing a divergent device**, asserting the divergent device flows through untouched with no envelope check and **no logger calls** — the actual R8 passthrough contract. Added the one-line note that a *non-array* `devices` in `off` is an inherited best-effort edge (`items.push(...extractor(page))` with `extractor = (p) => p.devices ?? []` throws on a non-array spread / spreads string chars), so R8's "no fail on shape" does not cover it and the design's non-array example is deliberately tested only under strict/warn where the envelope hard-fails first. Removes the crash/garbage-assert ambiguity. |
| plan-auditor-r2-f2 | Fixed | Added a strict "**cross-page warnings accumulation**" test to Phase 2: page1 `[valid1, divergent1]` (→ page2 via `nextPageUrl`), page2 `[valid2, divergent2]` (terminal, falsy `nextPageUrl`) → `result.ok === true`, `value` contains exactly `valid1` and `valid2`, `warnings.length === 2` (one per divergent device), `logger.error` called twice — proving the `while (nextUrl)` loop concatenates `valid` and `warnings` across successful pages rather than returning only the last page's. |
| plan-auditor-r2-f3 | Fixed | Changed both fenced R4 guards from `git diff --name-only` to `git diff --name-only HEAD` so staged/committed protected-file edits are detected, not just unstaged working-tree changes, closing the vacuous-pass hole. Added an inline comment in each gate explaining the `HEAD` choice. |

Self-review (fresh-eyes pass): Re-read the revised Phase 2 test list end-to-end. Confirmed the new off-mode note's mechanical claim matches the plan's own `getAllPages` snippet (L243 `items.push(...partition.valid)` in strict/warn; the off branch reaches `extractor(page)` unguarded) and the design's R8 scoping. Confirmed the new cross-page test's terminal-page mechanic (falsy `nextPageUrl` ends the walk) is consistent with the auditor's own fixture note that last-page `nextPageUrl` is `""`. Verified the two prose bullets under each exit gate that reference "the `git diff` guard" remain accurate after the `HEAD` change (they describe the guard generically, no wording drift). No further issues folded in.
