## reviser — round 6

Mode B, Phase 2 feedback. Read the latest Architect (architect-r4: **no new findings**; architect-r3-f1/f2
re-verified `Closed`) and Engineer (engineer-r4) turns. Performed an internal self-review across all
seven axes (phasing, specificity, exit gates, edge cases, hidden complexity, dependencies, design
alignment) and folded the resulting consistency fix (a stale `detail`/`raw` phrase left behind by the
f2 edit) into this same revision. Addressed both still-`Open` Engineer findings below; Architect had
none to disposition.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| engineer-r4-f1 | Accepted | The design names the mid-walk discard as an explicit success criterion (design L184) and again in its Verification list (design L196: "a multi-page walk whose later page's envelope is malformed (hard fail discarding earlier valid devices)"), yet the plan's tests only covered single-page envelope hard-fail and multi-page **all-success** accumulation — the distinct *discard* path (`return { ok: false }` mid-`while`, throwing away the accumulator) was unverified. Added a Phase 2 test: page1 `[valid1, divergent1]` with `nextPageUrl` → page2 malformed envelope (`devices: "nope"`) → `result.ok === false`, `error.type === "validation-error"`, `error.title === "Malformed devices page envelope"`, **no** partial `value`, and a `logger.error` matching the envelope message. Because page1's divergent device also fires a per-device `logger.error` before page2 is fetched, the test asserts a message match on `"Malformed devices page envelope"` rather than a strict call-count of 1 (a note I added to keep the assertion robust). |
| engineer-r4-f2 | Accepted | The envelope `ProblemError` set `detail: parsed.error.message` — the raw multi-line serialized `ZodError` blob — diverging from the concise, path-named `detail` every other `validation-error` site produces via `toProblemError`, and duplicating what already lives in `raw`. Made the envelope `detail` concise and path-named (`` `Malformed devices page envelope (path: ${envelopePath})` ``), reusing the exact `envelopePath` string now computed **once** for the log line, so all three `validation-error` sites share one `detail` convention and the full `ZodError` lives **only** in `raw`. Updated Step 3 prose, the code snippet, and the malformed-envelope test (now asserts `error.detail` starts with the concise prefix, names the path segment, and contains no newline). Self-review folded in a consequent fix: the Step 3 phrase "preserved in the `ProblemError`'s `detail`/`raw`" was corrected to "`raw`" only, since `detail` no longer carries the serialized error. |
