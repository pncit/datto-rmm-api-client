## reviser — round 5

Mode B, Phase 2 feedback. Performed an internal self-review (phasing, specificity, exit gates, edge
cases, hidden complexity, dependencies, design alignment) and folded its results into this revision
with no separate output. Then addressed every `Open` finding from the round-3 Architect and Engineer
turns. All fixes are edited directly into `plan.md` (no revision markers). Prior-round findings are
all `Closed` and not re-adjudicated. Note: architect-r3-f1 and engineer-r3-f1 are the **same**
off-path `nextPageUrl`-dereference bug from two reviewers; one fix resolves both.

| ID | Disposition | Rationale |
|----|-------------|-----------|
| architect-r3-f1 | Accept | The r2-f3 fix null-safed only the extractor, not the sibling walk-advance read. On a `null` off-mode body, `nextUrl = page.pageDetails?.nextPageUrl` throws `TypeError` because the `?.` sits after `pageDetails`, not after `page`. Changed the advance to `nextUrl = page?.pageDetails?.nextPageUrl` in Step 3 prose (L208), the snippet, and rewrote the off-path null-safety narrative (L209) to name **both** dereference sites (extractor + `nextPageUrl` read) as requiring `page?.`. In strict/warn `page = parsed.data` is a validated non-null object, so `page?.` is a harmless no-op there. |
| engineer-r3-f1 | Accept | Same defect as architect-r3-f1, resolved by the same edit. Additionally strengthened the "Off, `null`/primitive page body does not throw" test note: it must exercise the **`null`** case specifically (a primitive like `"s"` auto-boxes so `"s".pageDetails` is `undefined` and never throws — a string-only test would pass even with the buggy `page.pageDetails?.…`), so the test now actually catches the bug and the Phase 2 `npm test` exit gate is meaningful. |
| architect-r3-f2 | Accept | The prose (Implementation Notes L37, Phase 2 Step 1 L200) instructed adding `private logger: LoggerLike = config.logger ?? defaultLogger` as a **field initializer**, which cannot reference the bare constructor parameter-property `config` and fails `npm run build` with TS2663. Reconciled the prose to the (correct) snippet: declare an **uninitialized** `private logger: LoggerLike` field and **assign `this.logger = config.logger ?? defaultLogger` in the constructor body**. Also clarified the snippet comment (L245–247) to say the same explicitly. |
| engineer-r3-f2 | Accept | The envelope-failure log interpolated the raw multi-line `parsed.error.message` — the exact anti-pattern removed from the per-device path (r1-f1/f3) and `validate()`'s `warn` seam (r2-f1) — at the loudest, protocol-level failure. Changed the log to a single-line, path-named message: `` `Malformed devices page envelope at ${nextUrl} (path: ${parsed.error.issues[0]?.path?.join(".") || "(root)"})` ``, in both Step 3 prose and the snippet. The full serialized error stays in the `ProblemError`'s `detail`/`raw`, so the structured record is unchanged while the log line is now consistent with every other log in the feature. |
| engineer-r3-f3 | Accept | The both-optional envelope lets a 200 JSON object lacking **both** `pageDetails` and `devices` (`{}`, `{ error: "unauthorized" }`) parse as an empty page — a silent `{ ok: true, value: [] }` with no log — which the design's Decision-2 rhetoric (L139) says it rejects. Chose option (b): keep `devices` optional (matching the unchanged `DevicesPageSchema` and the design's own conceptual envelope, L89–93, so legitimate zero-device pages are not regressed) and **document the scope of the hard-fail explicitly** in Step 2 — it catches non-object bodies, present-but-wrong-type `devices`, and unreadable `pageDetails`, but treats absent-both-keys as an empty page. Noted this is an accepted residual gap (auth failures normally surface as non-2xx and are short-circuited by `res.ok` upstream, so a 200 non-devices-page object is rare), added a **pinning test** (`{}` and `{ error: "unauthorized" }` → `{ ok: true, value: [] }`, no `logger.error`) so the behavior is a verified contract, and recorded an evidence-driven envelope-tightening follow-up under Deferred Validation. |
| engineer-r3-f4 | Accept | `extractIdentity` checks `id` before `uid`, and every valid `Device` carries a numeric `id`, so `detail` reads `Device id={id} …`, not `uid={uid}` as the L215 prose (and the r2-f4 rationale) claimed. Corrected the L215 wording to describe `extractIdentity`'s id-first behavior (`id=` common case; `uid=` then `index N` fallbacks) and noted this is acceptable under R2 (either identity permitted), with the instruction to keep test assertions aligned to `id=`. Kept the id-first order (account-devices path benefits from the always-present `id`). |
| engineer-r3-f5 | Accept | `getAllPages` now always returns `warnings` (empty `[]` when nothing drifted), changing truthiness vs. the prior omitted-key shape while release-note item 1 tells consumers to "inspect `result.warnings`". Chose "always present, even when empty" (stable, simpler shape; `Result.warnings` is optional so it compiles) and documented the consequence in Step 3, the README item-1 bullet, and a new shape-guard test (clean page → `warnings` is `[]`, length 0): consumers must check `result.warnings.length`, not truthiness (an empty array is truthy). |
