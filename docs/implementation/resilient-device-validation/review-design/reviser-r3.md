## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|------------------------------------------|
| architect-r1-f1 | Fixed | The envelope hard-fail *mechanism* ("direct `safeParse`, not the mode-branching `validate()` seam") now lives only in Decision 2. Trimmed the "Generic `getAllPages` plumbing" concept to validate the envelope "per Decision 2," deleting its restatement of the mechanism. R5/R8/Breaking Changes/Success retain only their requirement/observable-outcome statements (no mechanism re-explanation), so there is one normative source. |
| architect-r1-f2 | Rejected | Each of the three mode-interpretation sites is already fully and singly specified where it belongs — `validate()` (Decision 4 / Key Concepts), the per-item helper (Key Concepts bullets), and the envelope path (Decision 2) — and the architect's own note affirms the `validate()`/`validateItems()` split is a justified boundary, not accidental duplication. Adding a meta-invariant sentence naming them as "the only" interpretation sites is additive documentation the design is decisive and plannable without; the point is real but does not make the design wrong. Non-Goal 4 already forbids new modes, bounding the surface a maintainer could shift. |
| architect-r1-f3 | Fixed | Added a clause to "Generic `getAllPages` plumbing": `pageDetails.nextPageUrl` is read from the envelope-parse result in `strict`/`warn` and directly off the raw page in `off`, and is not carried by the item extractor — removing the ambiguity about the loop's two paths. |
| engineer-r1-f1 | Fixed | Stated explicitly in the plumbing concept that a mid-walk envelope hard-fail returns `{ ok: false, error: { type: "validation-error" } }` and discards all earlier-page `valid`/`warnings` (consistent with today's abort, since pagination cannot continue past an unreadable `nextPageUrl`). Added a Success criterion and a new-test row pinning the multi-page-then-envelope-fail discard case. |
| engineer-r1-f2 | Fixed | Amended the flood risk row to cover the returned payload as well as logging: `warnings[]` is unbounded by design, one full-`ZodError`-bearing `ProblemError` per divergent device, mirroring the log signal rather than capping/summarizing it. Closes the accepted-as-is stance for the Planner. |
| engineer-r1-f3 | Fixed | Same consolidation as architect-r1-f1: the `safeParse`-not-`validate()` mechanism and the `warn`-hard-fail behavioral change are now stated once (Decision 2 for the mechanism; Breaking Changes item 2 for the `warn` change), with R5/R8/Success referencing the observable outcome tersely rather than re-explaining. |
