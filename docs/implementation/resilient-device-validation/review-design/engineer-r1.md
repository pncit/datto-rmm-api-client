## engineer — round 1

Design-review mode, entering after three design-auditor rounds + two reviser rounds that
converged the previously-open findings (getAllPages plumbing, warn passthrough, envelope
mode-gating, single-value logging, envelope `safeParse` hard-fail, R8/Breaking-Changes
consistency). I re-read those as closed and did **not** re-litigate them. My pass is on axes
the prior turns did not center: the multi-page/envelope interaction with the resilience goal,
the size/shape of the returned `warnings[]` channel, and revision-driven duplication. I
grounded the pagination behavior against `src/client.ts:40–68` (the current walk `return`s on
failure inside the `while`, discarding items collected so far).

The core design is sound and well-scoped; these are tightenings, not redirections.

## Findings

| ID | Severity | Status | Category | Where | Finding | Recommendation |
|----|----------|--------|----------|-------|---------|----------------|
| engineer-r1-f1 | Medium | Open | Completeness | Decision 2 / Key Concepts ("Generic `getAllPages` plumbing", L97) / R1 | The design specifies per-page accumulation of `valid`/`warnings` across the walk and a hard-fail on a malformed envelope, but never states what happens to **already-accumulated valid devices from earlier pages** when a *later* page's envelope hard-fails. Today the walk `return`s `{ok:false}` mid-loop and discards everything collected (`client.ts:59–63`). Preserving that means: page 1 and 2 yield 500 valid devices, page 3 returns an HTML error body → the whole call fails `{ok:false}` and all 500 valid devices are thrown away. That is a defensible reading of "envelope = protocol error," but it sits in visible tension with R1 ("one divergent device never prevents retrieval of the account's other devices") and the vision of graceful degradation, and a Planner/tests cannot infer the intended outcome from the current text. | State explicitly that a mid-walk envelope hard-fail discards all accumulated `valid`/`warnings` and returns `{ok:false}` (consistent with today's abort) — or, if partial salvage is intended, say so. One sentence in Decision 2 or the plumbing concept. Add a Success-criteria/test row for the multi-page-then-envelope-fail case so the discard-vs-salvage choice is pinned. |
| engineer-r1-f2 | Low | Open | Operability | Risks & Mitigations (L209) / Decision 1 / R2 | The "log flood" risk is acknowledged and mitigated for the **logging** channel, but the same mass-drift scenario also inflates the returned `Result.warnings[]`: one `ProblemError` per divergent device, each carrying the full `ZodError` in `raw` (R2, L115). On a Datto-wide schema change that drifts every device in a large account, `warnings[]` becomes an array of thousands of full `ZodError` objects held in memory and handed to the caller — a materially larger, unbounded return payload than any prior outcome. The design treats the flood purely as a logging concern. | Note in the risk table (or Decision 1) that `warnings[]` is unbounded and grows one full-`ZodError`-bearing entry per divergent device; state whether that is accepted as-is (proportionate, mirrors the log signal) or warrants a cap/summarization. Even an explicit "accepted, unbounded by design" closes the gap for the Planner. |
| engineer-r1-f3 | Low | Open | Clarity/Duplication | Key Concepts L97 / Decision 2 L132–136 / R5 L43 / R8 L46 / Breaking Changes L171 / Success L182 | Iterative revision has left two points restated in ~5–6 places each: (a) "envelope validation is a direct `safeParse` hard-fail, *not* the mode-branching `validate()` seam," and (b) "`warn` now hard-fails on a malformed envelope where it previously returned `{ok:true, value:[]}`." The repetition is faithful (no contradictions remain — the auditor rounds ensured that), but it inflates the document and raises the maintenance cost of any future change to that behavior: six sites must move together. | Make **Decision 2** the single normative statement of the envelope-hard-fail mechanism and the `warn` behavioral change, and have R5/R8/Breaking Changes/Success reference it tersely rather than re-explaining the `safeParse`-not-`validate()` reasoning in full each time. Pure tightening; no semantic change. |
