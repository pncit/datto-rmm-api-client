## reviser — round 3

| ID | Disposition | Rationale |
|----|-------------|-----------|
| gate-runner-r1-f1 | Fixed | Root-caused the gate failure: `deviceSchema.test.ts` and two cases in `devicesMethod.test.ts` were failing because the shared fixtures (`src/__tests__/fixtures/device.json`, `devicesPage.json`, `devicesPage1.json`, `devicesPage2.json`) don't conform to the unmodified, protected `DeviceSchema` — every device fixture had only `udf.udf1` set (the schema requires all of `udf1`..`udf30`, each `string \| null`) and `lastSeen`/`lastReboot`/`lastAuditDate`/`creationDate` were ISO-8601 date strings where the schema requires `z.number()`. Verified with `git worktree` that this is **pre-existing**, not something Phase 1 (or 2) introduced: checking out `12effce` (the plan-approval commit, before any Phase 1 code existed) into a scratch worktree and running `npx jest src/__tests__/deviceSchema.test.ts src/__tests__/devicesMethod.test.ts` reproduces the identical 3 failures with the identical Zod `invalid_union`/`invalid_type` issues on `udf27`–`udf30` and `result.ok === false`. Since `DeviceSchema` is a protected file the plan forbids editing in this phase (and the design gives no reason to loosen it — the schema is correct per the API contract), the fix is to correct the four **fixture** files to match the existing schema: filled in `udf2`–`udf30` as `null` on every device object, and converted the four date fields from ISO strings to epoch-millisecond numbers (e.g. `"2024-07-16T12:00:00Z"` → `1721131200000`), computed via UTC epoch conversion so the represented instants are unchanged. No test assertions were altered — `deviceSchema.test.ts`'s `expect(parsed).toEqual(device)` and both `devicesMethod.test.ts` assertions (`hostname`, `antivirus.antivirusStatus`, pagination request order) pass unmodified against the corrected fixtures. Re-ran the exit gate: `npm run build` exits 0; `npm test` — all 4 suites / 14 tests pass; `git diff --name-only HEAD` shows only the four fixture JSON files changed (`src/__tests__/fixtures/{device,devicesPage,devicesPage1,devicesPage2}.json`) — `schemas.ts`/`result.ts`/`index.ts`/`validation.ts` untouched this round, so the R4 guard holds. |

Verification: `npm run build` exits 0; `npm test` → `Test Suites: 4 passed, 4 total`, `Tests: 14 passed, 14 total`; `git diff --name-only HEAD | grep -qE '^src/(schemas|result|index)\.ts$'` finds no match (R4 guard passes).
