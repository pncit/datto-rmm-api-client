## reviser — round 4

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| architect-r1-f1 | Fixed | Extracted the duplicated first-issue-path computation into an exported `firstIssuePath(error: ZodError): string` in `src/validation.ts`, and switched both `validate()`'s `warn` branch and `toProblemError()` to call it instead of inlining `result.error.issues[0]?.path?.join(".") || "(root)"` a second/third time. Exported (not module-private) so Phase 2's envelope hard-fail in `client.ts` can import and reuse it rather than hand-copying a third time, per the plan's own call-out. |
| architect-r1-f2 | Fixed | Added a doc comment on `extractIdentity` in `src/validation.ts` stating plainly that identity extraction is best-effort and limited to `id`/`uid`, that any other key falls back to `index N`, and that a caller with a differently-keyed entity should use the new `identityOverride` parameter on `toProblemError` (see architect-r1-f3) instead of expecting `extractIdentity` to discover its key. Makes the "generic reuse" claim honest about what is and isn't injectable. |
| architect-r1-f3 | Fixed | Widened `toProblemError` with an optional trailing `identityOverride?: string` parameter (`src/validation.ts`); when supplied it's used verbatim in place of `extractIdentity(item)`, falling back to the existing `extractIdentity(item) ?? \`index ${index}\`` chain when omitted. All current call sites (`validateItems`) are unaffected since they don't pass it; this is additive and lets a future single-value caller (Phase 2's `getDeviceByUid`) inject its own known identity instead of getting a meaningless `index 0`. |
| engineer-r1-f1 | Fixed | Hoisted the `Array.isArray(items)` guard in `validateItems` (`src/validation.ts`) above the `mode === "off"` check so a non-array `items` returns `{ valid: [], warnings: [] }` unconditionally in `strict`/`warn` too, not just `off`. Updated the JSDoc's "Never throws" claim to explicitly cover this. Added a new test ("strict/warn, items deliberately not an array returns empty result without throwing") exercising both modes to pin the fix. |
| engineer-r1-f2 | Fixed | Same fix as architect-r1-f1 (identical finding from a different lens) — the shared `firstIssuePath()` helper eliminates the verbatim duplication, including the `"(root)"` sentinel, between `validate()`'s `warn` branch and `toProblemError()`. |
| engineer-r1-f3 | Fixed | Added module-level constants `VALIDATION_WARN_PREFIX = "Validation warning"` and `VALIDATION_ERROR_PREFIX = "Validation error"` in `src/validation.ts` and updated all three log call sites (`validate()`'s `warn`, `validateItems()`'s `warn`, `validateItems()`'s `strict`) to build their message off the shared constant, giving one consistent, greppable prefix across the single-value and array seams. |
| engineer-r1-f4 | Fixed | Added two tests to `src/__tests__/validation.test.ts`: (a) a `validateItems` strict case with an item carrying `uid` but no `id`, asserting `detail` contains `uid=abc-123` (exercises the previously-untested `extractIdentity` uid branch); (b) a `validateItems` call with no trailing `logger` argument, asserting it resolves via the `defaultLogger` default without throwing (mirrors the existing no-logger `validate()` test, closing the asymmetric coverage gap). |
| project-lead-r1-f1 | Fixed | Updated `implementation-phase1-notes.md`: §1 now records the fixture fix as a post-review addendum instead of listing it as out-of-scope; §3 "Files Touched" adds the four fixture JSON files with the rationale for the change; §11 replaces the "unresolved risk, flagging for awareness" framing with a "resolved during review" note describing what was fixed and by whom (`reviser-r3`); §12 command outputs updated to reflect the current green `npm test` (17/17) and the widened file scope. |
| typescript-cop-r1-f1 | Fixed | Replaced `(valid[0] as any).extra` with `(valid[0] as Record<string, unknown>).extra` in `src/__tests__/validation.test.ts:124`, consistent with the narrowing style used elsewhere in the file and in `extractIdentity`. |

Verification: `npm run build` exits 0; `npm test` → `Test Suites: 4 passed, 4 total`, `Tests: 17 passed, 17 total`; `git diff --name-only HEAD | grep -qE '^src/(schemas|result|index)\.ts$'` finds no match (R4 guard passes) — this round's edits are confined to `src/validation.ts`, `src/__tests__/validation.test.ts`, and `implementation-phase1-notes.md`.
