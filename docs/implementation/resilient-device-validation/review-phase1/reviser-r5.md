## reviser — round 5

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| project-lead-r2-f1 | Fixed | Updated `implementation-phase1-notes.md`: §5 now records the four `reviser-r4` structural additions (`firstIssuePath` extraction, `toProblemError`'s `identityOverride` param, `VALIDATION_WARN_PREFIX`/`VALIDATION_ERROR_PREFIX` constants, hoisted `Array.isArray` guard) as documented deviations from the plan's illustrative snippet, instead of asserting "No deviations." §7 now says 13 tests (not 10) and lists the 3 tests `reviser-r4` added (uid-branch identity, non-array guard for strict/warn, no-logger `validateItems` overload) with attribution to the findings they close. §12 now breaks the 17-test total down per suite (1+2+1+13) so the count is traceable rather than a bare assertion. |
| project-lead-r2-f2 | Fixed | Added a paragraph to `implementation-phase1-notes.md` §5 explicitly flagging `toProblemError`'s `identityOverride` parameter as unused by any call site in this phase and unused by the plan's own Phase 2 `getDeviceByUid` example (`toProblemError("Device", e, res.value, 0)`, 4 args). Kept the parameter itself rather than reverting it: it's optional/additive (doesn't affect the plan-specified call site or any existing behavior) and `architect-r2` already re-verified and ratified it (`architect-r1-f3` → Closed) as closing a genuine array-vs-single-value reuse gap in the builder. Reverting a review-ratified fix from a different reviewer's lens to satisfy this finding would just re-open that one — documenting the surface as optional/unexercised, per this finding's own second recommendation, resolves the ScopeCreep concern without relitigating a closed architecture finding. |
