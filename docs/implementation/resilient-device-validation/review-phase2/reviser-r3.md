## reviser — round 3

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| engineer-r1-f1 | Fixed | Both Phase-2 `validation-error` log sites in `src/client.ts` now share the `VALIDATION_ERROR_PREFIX` prefix (imported from `./validation.js`), matching `validateItems`'s existing `` `${VALIDATION_ERROR_PREFIX}: …` `` shape: the envelope hard-fail now logs `` `${VALIDATION_ERROR_PREFIX}: ${MALFORMED_ENVELOPE_TITLE} at ${nextUrl} (path: ${envelopePath})` ``, and `getDeviceByUid`'s catch now logs `` `${VALIDATION_ERROR_PREFIX}: getDeviceByUid: ${problem.detail}` ``. All three `validation-error` sites are now greppable on one shared head, and `validation.ts:15-16`'s claim that "client.ts's envelope hard-fail can import the same prefixes" is now literally true (verified: `client.ts` imports `VALIDATION_ERROR_PREFIX`). Confirmed no test asserts the old un-prefixed log strings verbatim; `npm test` (37/37) and `npm run build` pass. |
| engineer-r1-f2 | Fixed | Added a single module-level `const MALFORMED_ENVELOPE_TITLE = "Malformed devices page envelope";` in `src/client.ts` and referenced it in the `ProblemError.title`, the `detail` template, and the log line (previously hand-copied three times), so the phrase has one source of truth the same way `firstIssuePath` is the single source of truth for the failing path. `npm test` (37/37) confirms no observable string changed. |
| typescript-cop-r1-f1 | Fixed | Replaced every `const r = result as any;` (14 sites) in `src/__tests__/devicesMethod.test.ts` with a compile-time-narrowing pair: added `assertOk<T>`/`assertFail<T>` assertion-signature helpers (`asserts result is Extract<Result<T>, { ok: true/false }>`) and call the matching one in place of the runtime-only `expect(result.ok).toBe(true/false)` check, then `const r = result;` picks up the narrowed type. This surfaced real latent type gaps the `any` cast had been hiding, which I also fixed rather than re-casting: (1) `r.warnings` is legitimately optional on `Result<T>`'s ok variant, so length/index reads now go through a local `?? []` (either inline or a `const warnings = r.warnings ?? [];`) instead of assuming presence untyped; (2) the "earlier pages' devices are discarded" assertion on the `ok:false` branch (`r.value` — a field that doesn't exist on that variant per the type, which is itself a stronger, compiler-enforced version of the assertion) now checks `"value" in r` to be `false`, which is a strictly more precise runtime check (catches an accidental leaked key even if set to a non-undefined value) than the previous `toBeUndefined()`; (3) `r.value.find(...)` on `Device[]` correctly types as `Device \| undefined`, so the one `off`-mode find-then-read test now binds the result and reads `?.deviceClass`. Did not touch the two pre-existing `(result as any).value` casts in the two tests that predate this phase (`devicesMethod.test.ts:78,101` — outside the diff vs. `main`, and outside this finding's stated scope of "17 new instances vs 2 pre-existing in the original two tests"); fixing those would be scope creep beyond what this finding raised. `npm run build` and `npm test` (37/37) pass with no `any` remaining on the `Result<T>` narrowing path. |
