## reviser — round 4

| ID | Disposition | Rationale (and, for Fixed, what changed) |
|----|-------------|-------------------------------------------|
| engineer-r2-f1 | Fixed | Confirmed against `plan.md` (Phase 2 Step 5) that the plan itself explicitly mandates `getDeviceByUid`'s `id=`-first identity (not `uid=`) as an accepted, deliberate outcome ("the log/warning names id=, not uid=, for the common case … That is acceptable (R2 permits either id or uid); just keep the prose and test assertions aligned to id="), and that the plan's own `toProblemError` signature (plan.md Phase 1 Opinionated Implementation Notes) never included an `identityOverride` parameter at all — it was an unwired addition layered on top of the plan by an earlier round, never actually consumed by any caller. Given the plan already settles the *behavior* (keep `id=`, do not wire a `uid=` override into `getDeviceByUid`), the correct fix is the finding's second alternative: removed the dead `identityOverride` parameter from `toProblemError` entirely (rather than wiring it through with a `uid=` value that would contradict the plan's explicit, reasoned choice), and deleted the two JSDoc passages (on `toProblemError` and `extractIdentity`) that falsely cited `getDeviceByUid` as an `identityOverride` consumer. Also fixed the second half of the finding — "drop the bare 0" — by giving `toProblemError`'s `index` parameter a default (`index = 0`), so `getDeviceByUid`'s call site (`client.ts`) now reads `toProblemError("Device", e, res.value)` with no meaningless positional literal, while `validateItems` (`validation.ts:98`) is unaffected and still passes its real array index explicitly. Added an inline comment at the `getDeviceByUid` call site recording the id-vs-uid tradeoff and pointing at the plan's rationale, so a future reviewer sees the reasoning in-place rather than rediscovering the same question. No test changes needed: the existing `getDeviceByUid` strict test already asserts `id=1` (matching the plan-mandated id-first behavior), so it was never asserting the now-removed override path. `npm run build` and `npm test` (37/37) pass; both R4 guards and the README doc-landing guard still pass. |
